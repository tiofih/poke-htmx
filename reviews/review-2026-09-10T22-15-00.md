# Review — 0081 tuning round 3 (S7, narrow)

**VEREDITO: Requer ajuste** — 1 high funcional (key-type mismatch em views) + gaps formais S3/G2.
Scope: `bedd78b..HEAD` (passos 1–13: lose 0/5XP, heal cap 1.5x, curva L1-3 60, mart labels, rename, dead-branch, keys). Sem edit, sem commit.

## Achados (pior primeiro)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| high | `views/team.erb:30`, `views/team_manage.erb:5`, `views/_center.erb:2-5` | Lookup com `poke.id.to_s` / `m.id.to_s` contra `@member_levels` com chaves Integer (`server.rb:59-61` `member_levels_map` usa `member.id`; `Pokemon#id` é `Coercible::Integer` em `lib/pokemon.rb:8`). `Hash#fetch(int.to_s)` nunca acerta hash de ints → nível exibido sempre cai no fallback 1; no `_center`, `avg_lvl` sempre = `BASE_LEVEL` (hints por item e "Nível médio" sempre base). O `.to_s` só está correto em `HealService#average_level` (chaves de `levels_for` vêm do PG como String). Passos 12/13 (0f49796/295111e) trocaram o lado errado. Nenhum teste cobre o display (só `team_routes_test` de budget). | Normalizar num ponto só: `member_levels_map` → `[member.id.to_s, ...]` e manter `.to_s` nas 3 views (ou reverter as views p/ chave inteira). Cobrir com 1 teste de rota: time com `grant` real → `GET /team` contém `Nível <n>` (n>1). |
| medium | `views/_center.erb:2-5` | Lógica de `avg_lvl`/multiplier duplicada do `HealService#average_level` na view — duas fontes da verdade, já divergiram (este é o vetor do bug high). | Extrair: controller passa `@avg_level` (via service) ou presenter; view só interpola. |
| medium | `sessions/0081-tuning-economia-progressao.md:44-49` + `lib/reward_rule.rb:7` | S3: `lose_xp` 20→0 (passo 4) →5 consolo (70a3992) não consta em nenhum critério (C1 fala só de `levels_for`). Mudança formal de critério sem registro em §4/§7 nem reaprovação anotada. Arquivo ainda marca tudo `pendente` / Implementação Pendente com fase 2 feita. | Registrar S3: adicionar C1b (`xp_for(:lose)=5`, teste `reward_rule_test`) ou declarar consolo fora de critério com aceite do usuário + data; atualizar status da fase 2 (sem preencher §7). |
| medium | `views/_center.erb`, `views/_mart.erb`, `views/battle.erb`, `views/index.erb`, `views/team.erb`, `views/team_manage.erb` vs `sessions/0081...:37-39` + G2 | G2 literal quebrado: escopo diz "fora: visual", mas 6 views mudadas. Conteúdo é coerente com tuning (breakdown da cura, labels mart, guard `xp_gained`, rename, key fix) e playtest/design aceitaram copy — mas o check G2 (`git diff --stat -- views/…` vazio) falha. | Ou alargar G2/§3 p/ "copy de tuning sem layout" (com reaprovação), ou reverter o que for cosmético puro. Não deixar critério vermelho silencioso. |
| low | `lib/experience_curve.rb:6` | `return 60 if level <= 3` — número mágico sem constante/nome; relação win 50 XP vs threshold 60 (nv1→2 = 2 vitórias) não documentada. | `EARLY_XP = 60` + 1 linha de rationale (curva achatada L1-3 p/ acelerar onboarding). |
| low | `SESSIONS.md` (linha "Próxima sessão (2026-09-10)") | Diz `0082 → 0083 → 0086 (0081… aguarda validação)` — fila pula a validação pendente da 0081; §8 da 0081 lista 0084/0085 que não existem na tabela. | Ajustar ordem p/ `0081-validação → 0082…` e alinhas as listas. |
| info | working tree | `public/style.css`, `test/pokemon_routes_test.rb`, cassettes dirty (fora do tuning) → G1 desta revisão é provisional: suíte/lint não re-executados aqui; vereditos de verde vêm dos commits, não de run limpo atual. | Implementador: stash/descartar estranhos, rodar `./scripts/test` + `./scripts/lint` + `check_docs` + `checar-sessao 0081` e anexar evidência antes da validação. |

## Coerência S3 (tuning como sistema)

- Derrota: `lose_levels` 0 + consolo 5 XP (10% do win) + copy sem "0 XP" + guard `battle.erb:93` — coerente entre si; falta só o registro formal (achado medium acima).
- Heal-tax spiral: `cost(missing, avg)` com teto 1.5x + `preview_cost` acompanha + breakdown no Center + `cost_per_HP` cap ≤ poção (`test_capped_cost_per_hp…`) — coerente; `levels_for` em lote evita N+1. Corretude do cálculo OK; só o display do avg está errado (bug high).
- Curva: `xp_for`→`grant` no reward path, `grant_levels` isolado p/ seed/teste (comentário legacy ok), sweep mede caminho real (sem diff no script — ok, ele lê o caminho de produção), desconto L1-3 só pede nome/rationale.
- Mart/rename: `Comprar ×n`, `+HP`, `¥/HP`, `display_name` no sell, `Pontos de montagem (não é dinheiro)` — usabilidade, sem mudança de regra; testes atualizados em par.

## Contenção do diff

`lib/` + `test/` + views de copy: contido. `db/`, `lib/opponent_generator.rb`, `lib/team_budget.rb`, `scripts/sweep-balance.rb`, `server.rb`: intocados. Fora do tuning no range: só `sessions/0082,0083,0086` (refinamentos) + `SESSIONS.md`/`draft-backlog.md` — esperado.
