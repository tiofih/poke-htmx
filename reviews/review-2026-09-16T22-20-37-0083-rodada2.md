# Revisao escopada — sessao 0083 (`ui-polish`), rodada 2

**Escopo:** SO os 2 commits de correcao da rodada 1 — `70e000a`
(`views/layout.erb`, `public/style.css`, `test/layout_test.rb`, `test/home_view_test.rb`) e
`1dff881` (`sessions/0083-ui-polish.md`).
Leitura minima (`git show`, `sed -n`, `rg -n`) + 4 mutacoes temporarias, **todas revertidas**
(`git status --short` final identico ao baseline: so `reviews/…-0083.md` e
`test/cassettes/ServerMartTest/`, ambos untracked pre-existentes). Nenhum arquivo de producao
editado; nenhum commit feito.

## VEREDITO: **Aprovado**

0 blocker / 0 alta / 0 media / 2 baixa / 3 info — as **2 medias** da rodada 1 estao fechadas e
as 2 baixas de teste (C3 convencao unica, C4 escape hatch) tambem. Rodada 2 entra com veredito
fechado para a fase 3.

| Sev rodada 1 | Achado | Status rodada 2 |
| --- | --- | --- |
| media | hint do CTA sumia em `<=720px` e o `title` era inalcancavel | **Fechado.** `layout.erb:36-37` removeu o `title`, adicionou `aria-describedby="cta-hint"` + `<span id="cta-hint">`; `style.css:2053` **compacta** (`max-width:none; font-size:11px`) em vez de `display:none`; `:focus-visible` em `style.css:2051`. Mutacao M1 (reintroduzi `display:none`) → **RED** |
| media | game over pedia "cure o time" sem dinheiro | **Fechado.** `layout.erb:28-34` tem 3 ramos com `@game_over` primeiro; coerente com `views/team.erb:8-12` (notice de game over + "Recomeçar jornada"). Mutacao M2b (`if @game_over` → `if false`) → **RED** |
| baixa | `.pill.warn`/`.pill.danger` mortas, 2 convencoes | **Fechado.** Bloco `style.css:573-580` removido; `rg -n '\.pill\.(warn\|danger)' public/style.css views/` = 0 (so o texto do assert em `test/home_view_test.rb:111`); unico consumidor e `views/team.erb:23` (`pill--<%= pill_mod %>`) |
| baixa | C4 com escape hatch (`cost.nil? \|\| cost < add`) | **Fechado.** `test/home_view_test.rb:139-141` = `refute_nil cost` + `refute_nil add` + `assert_operator cost, :<, add`. Mutacao M3 (`class="poke-cost` → `pcost`) → **RED** |

## Achados residuais (pior primeiro)

| Sev | Local | Problema | Recomendacao |
| --- | --- | --- | --- |
| baixa | `views/layout.erb:36` + `public/style.css:2051` | O `:focus-visible` do `.btn--gated` e **inalcancavel por teclado**: `tabindex="-1"` tira o CTA da ordem de tabulacao, entao so foco programatico chegaria la — e foco por script normalmente **nao** casa `:focus-visible`. A regra do item 6 existe mas e codigo morto na pratica. | Se o gate nao deve ser alcancavel, remover a regra e dizer isso no comentario; se deve ser anunciado, trocar por `tabindex="0"` (ou `<button type="button" disabled>`) e manter o outline. Item barato: decidir e registrar no §8. |
| baixa | `test/home_view_test.rb:68-72` (C2) | O critério "sem overflow horizontal" continua provado por **presenca de texto CSS** (`@media 420px … grid-template-columns:1fr`), nao por medicao — a rodada 1 ja apontava isso e a rodada 2 mitigou so a metade do breakpoint (420 cobre 360 e 375). Qualquer outro overflow em 375/360px passa batido. | Manter o limite honesto que o proprio teste documenta (manual/e2e) e marcar C2 como `manual` no §8, ou mover a medicao para o teste de layout/e2e. Nao bloqueia. |
| info | `views/layout.erb:29` | O hint de game over cita so "Recomece a jornada", mas `views/team.erb:9` tambem oferece "Venda itens no Poke Mart" — o CTA suprime a opcao de recuperacao nao-terminal. | Se quiser simetria, encurtar para "Jornada encerrada." (o texto completo ja esta na pagina). Sem impacto funcional. |
| info | `test/layout_test.rb:220` / `test/home_view_test.rb:259` | `ui_polish_block` duplicado identico nos 2 arquivos de teste (mesmo regex, mesmo `File.read`). | Mover para `test_helper.rb`/`TestSupport` quando alguem tocar esses arquivos de novo. |
| info | `test/layout_test.rb:161-180` | O teste de game over asserta o **texto** do hint, nao `aria-disabled`/`btn--gated`; o gate do estado terminal e implicito (o `<span>` so renderiza `if cta_gated`). | Nada a fazer, so registrar que a combinacao "gated + game over" e coberta por implicacao. |

## Itens 4-7 do pedido (respostas diretas)

- **Item 4 — perda de cobertura no caso `cta-hurt`: nao.** Antes (`8e9a88e`) o caso "ferido" assertava
  `aria-disabled` + "Cure o time antes de batalhar." num estado que **era** game over (time cheio +
  HP 0 + saldo 0) — ou seja, assertava a copy defeituosa apontada pela media 2. Depois do
  `@wallet.grant("cta-hurt", 10_000)` (`test/layout_test.rb:115`) ele passa a exercitar "ferido COM
  saldo" (a copy correta do ramo `else`), a cobertura do ramo "time vazio" segue no caso `cta-empty`
  (linhas 89-98) e o estado terminal ganhou teste **novo** (`:161`). Cobertura aumentou, nao caiu.
  O helper `@wallet` e `fill_team` vem de `test/server_test_helpers.rb:20,28` — arquivo **nao tocado**
  pelos commits, uso difundido em outros 10 arquivos de teste; nenhuma mudanca de producao.
- **Item 5 — `max-width:420px`:** leitura + `rg` mostram que a unica regra de `.filter-grid` apos
  `style.css:432-437` (base `1fr 1fr`) e a media em `:2059`; acima de 420px o grid segue `1fr 1fr`
  (e nao ha regra posterior de `.filter-grid` que sobreponha — as ultimas ocorrencias sao 2057/2059).
  360 e 375px caem na media. **Nao asseveravel por unit test:** que a coluna unica elimine *toda* a
  causa de scroll-x em 375px (medicao real manual/e2e) — o media `(max-width:480px)` de `.pokemon-grid`
  em `:2174` toca outro componente.
- **Item 6 — `.btn--gated`:** `pointer-events:none` segue em `style.css:2050` (nao ativavel por
  ponteiro) e `tabindex="-1"` segue em `layout.erb:36` (fora da tabulacao), agora com
  `aria-disabled` + `aria-describedby`. `:focus-visible` existe mesmo no bloco 0083 (`:2051`) — ver
  achado residual 1 (morto na pratica). Coerencia com `views/team.erb:9,21`: o hint de game over
  ("sem dinheiro para curar… Recomece a jornada") concorda com o notice `notice--error` e com a pill
  `pill--danger` "Game over".
- **Item 7:** escopado `./scripts/test test/layout_test.rb test/home_view_test.rb test/design_system_test.rb`
  → **42 runs / 692 asserts / 0 failures / 0 errors / 0 skips** (bate com o declarado).
  `./scripts/lint` → **136 arquivos, 0 offenses**. Suite completa **aceita** (1186 runs / 6305 asserts
  declarados; nao reexecutada). `check_docs`/`checar-sessao` aceitos como declarados.

## Evidencia das mutacoes

| # | Mutacao | Resultado |
| --- | --- | --- |
| M1 | `style.css:2053` volta a `.cta-hint { display: none; }` → `./scripts/test test/layout_test.rb` | **RED** — `1 failures`: "o hint do CTA gated nao some em nenhuma largura (revisao S7)" (10 runs/85 asserts) |
| M2 | inverte a **ordem** dos ramos com o par corpo-condicao preservado (`zero?` antes de `@game_over`) | **GREEN (10 runs/93 asserts/0 failures)** — mutacao semanticamente equivalente (game over implica time derrotado, nunca `team_size == 0`), logo nao e um teste valido de "ordem"; a ordem **nao** e o que o teste fixa |
| M2b | `if @game_over` → `if false` (derruba o ramo terminal) | **RED** — `1 failures` (10 runs/89 asserts). O que o assert prova e **a existencia do ramo**, e isso e real |
| M3 | `class="poke-cost` → `class="pcost"` em `views/pokemon_list_item.erb:8` | **RED** — `1 failures`: "o custo aparece no .pcard-meta (C4)" (10 runs/108 asserts) |

O assert de ordem do C4 (`cost < add`) continua verificando algo real: compara os indices de string
de `class="poke-cost` e `class="pcard-add` dentro do `.pcard-meta` — se o custo sair de dentro do
bloco (M3) falham os `refute_nil`; se trocar de posicao com o form, falha o `assert_operator`
(o markup gerado seria o espelho visual do flex `space-between`).

**G2:** `git diff --stat HEAD~2..HEAD -- lib/ db/ config/ server.rb Gemfile Gemfile.lock` e
`git diff --stat HEAD -- lib/ db/ config/ server.rb Gemfile Gemfile.lock` = **vazios** (0 linhas).
`git show --stat` de `70e000a` = `views/layout.erb`, `public/style.css`, `test/*.rb`;
`1dff881` = `sessions/0083-ui-polish.md`.
