# Review — Sessão 0082 center-modal-heal (C1–C2, S7 round 1)

**VEREDITO: Aprovado** — C1 e C2 atendidos, G2 contido, G1 suíte verde (1096 runs, 0 failures). Ressalva low: lint repo-wide com 26 offenses pré-existentes (nenhuma introduzida pelo diff). Pronto para validação do usuário (fase 3).

## Evidência verificada

- Diff `2942ffb^..HEAD`: 5 arquivos, +49/−1 — `server.rb`, `views/_center.erb`, `views/_center_modal.erb`, `test/modal_routes_test.rb`, `test/team_routes_test.rb`.
- `render_result_notice` (server.rb:764) só é chamado por `heal_team` (inbound total 1) — sem over-broadcast para add/remove/move.
- `render_team_fragment_with_notice` → `prepare_team_fragment_data` → `center_data` (server.rb:991–1002, 542–546) garante `@heal_cost`/`@balance` frescos antes do `oob_center_modal` — sem risco de nil no `_center.erb`.
- C1: `test_heal_via_modal_closes_and_rerenders` cobre OOB do modal + `"Time já curado"` + `"Saldo insuficiente"` com form disabled.
- C2: `test_heal_rerenders_balance_oob` cobre wallet 150, OOB `team-view` + `nav-badge`, `"Saldo ¥150"`, `"Custo total: ¥0"`.
- G2: `git diff --stat` em `lib/heal_cost_policy.rb lib/heal_service.rb lib/reward_rule.rb db/` vazio — nenhuma regra de economia/migração tocada.
- G3: `./scripts/checar-sessao 0082` → ok estruturalmente consistente.
- G1 (re-executado neste review): `./scripts/test test/modal_routes_test.rb test/team_routes_test.rb test/mart_routes_test.rb` → 108 runs, 814 assertions, 0 failures; suíte completa → 1096 runs, 5423 assertions, 0 failures.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Low | repo-wide lint (26 offenses; ex. `server.rb:76` Metrics/AbcSize, `test/team_routes_test.rb:445` Metrics/AbcSize) | G1 "lint 0" não é atendido no repo, mas as 2 offenses nos arquivos tocados estão fora dos hunks do diff (linhas pré-existentes); o diff não introduz offense nova | Não barrar a 0082 por dívida pré-existente; registrar débito de lint fora da sessão e seguir para validação |
| Low | `server.rb:770-772` (`oob_center_modal` via `String#sub` em `'id="center-modal"'`) | Se a markup do partial mudar (atributo/ordem), o `sub` falha silenciosamente e o modal deixa de atualizar via OOB | Manter como está (teste assert cobre a string OOB); se o partial for reestruturado, trocar por flag explícita de OOB no partial |
| Info | `server.rb:767` + `views/_center_modal.erb:8-10` | C1 implementa o ramo "re-renderiza" (modal permanece aberto com `@notice`), não o ramo "fecha"; critério permite ambos, mas a escolha de UX fica implícita | Confirmar na validação (fase 3) se manter o modal aberto com aviso é o comportamento desejado |
| Info | working tree (uncommitted): `public/style.css`, `docs/draft-backlog.md`, `test/pokemon_routes_test.rb`, cassettes | Arquivos sujos/não-rastreados alheios à 0082 convivem no worktree | Não incluí-los no commit da sessão; `git status` antes de commitar a validação |

## Notas (sem severidade, contexto)

- `<%= @notice %>` em `_center_modal.erb:9` segue o idioma de todos os outros modais/fragments (`_mart_modal`, `_evolution_modal`, `team.erb`) — nenhuma vulnerabilidade nova; XSS dependeria de conteúdo de notice, que é interno ("curado por…").
- `hx-target="#team-view"` no form + blocos OOB na resposta segue o padrão htmx (OOB extraído antes do swap principal), consistente com `add_team_success`/`budget_blocked_response`.
