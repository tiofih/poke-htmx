# Review 0086 round 5 (S7 narrow: 9ffeb8b chronological + modal cap + aa8e2ac per-line fx) — 2026-09-11

Scope: `08a6d0e..HEAD` (Passos 8-9); C1 chronological + pacing, C5 per-line
effects + modal capped at 3s, skip + reduced-motion order, G1. No edits, no commits.

## VEREDITO: Requer ajuste

- C1 flip coherent in app + unit/view tests: `views/battle.erb:24` reverses
  presenter groups to chronological R1-on-top, `step_delay` accumulates
  oldest→newest, `--log-delay` grows downward
  (`group_index * 0.25 + step_delay`); proven by renamed
  `test_battle_log_groups_entries_by_round_chronological`
  (`test/battle_view_test.rb:86`, asserts ascending).
- Modal cap coherent: `--log-total: min(acc,3s)` + full pacing kept in
  `--log-total-full` (`battle.erb:44`); `.res-overlay` gated on
  `var(--log-total)`; proven by extended
  `test_battle_result_gated_modal_after_log` (`<= 3.0` + full-var asserts).
- Per-line fx coherent: `.fx` token inside each `.log__entry` carries its own
  `--log-delay` (`battle.erb:198`), synced flash/KO/heal/chip keyframes in
  `style.css`; fighter `li` deliberately state-final-only (refute asserts in
  `test_juice_effects_sync_per_line`); arena shake via `--step-delay` on
  `.arena`. Proven by `test_battle_arena_carries_step_delay_and_per_line_fx`.
- Skip + reduced-motion order correct: skip `:has` rules (incl. new
  `.log__entry .fx/.chip`) sit BEFORE the final reduce net, which zeroes
  `--step-delay`/`--log-total` with `!important` and `animation:none` on all
  juice selectors incl. `.fx/.chip/.arena` — proven by
  `test_skip_and_reduce_cover_step_delay` +
  `test_result_modal_reduced_motion_last`.
- G1: `git diff 08a6d0e..HEAD -- lib/battle_engine.rb lib/battle_service.rb
  lib/battle_log_presenter.rb lib/battle_juice_presenter.rb db/ server.rb`
  EMPTY; targeted
  `battle_view + style_responsive + battle_juice_presenter + battle_log_presenter`:
  **63 runs, 550 assertions, 0 failures** (this review). Session defines only
  G1 — no G2/G3 exist.
- Blocker (§S7, one-line fix in implementador): the C1 proving e2e still
  asserts the superseded order (Medium below) — it contradicts the shipped
  behavior and would fail on run.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Medium | e2e/specs/battle-log.spec.ts:36,54 | C1 proving e2e still named `round headers newest-first...` and asserts descending (`sort((a,b) => b - a)`) while the app ships chronological since `9ffeb8b` (DOM is ascending) — spec contradicts the criterion it proves and fails if executed; `9ffeb8b` updated only the view test | Flip the assertion to ascending + rename to `round headers chronological...` (one-line fix); e2e is outside `./scripts/test` so the suite stays green, but S1 names this spec for C1 |
| Low | views/battle.erb:44 | `.arena` `--step-delay` is the uncapped full `acc` while the modal `--log-total` is capped at 3s — on long logs the arena shake fires after (behind) the already-revealed modal | Cap arena delay at the same ceiling (`[acc, 3.0].min`) in a future pass — visual nuance only |
| Low | public/style.css:936-937 (`.fighter.is-hit`, plus damage `::after`, KO) | Fighter-card flash/damage-number/KO now fire immediately on page load with no staging (Passo 9 removed all `--step-delay` from `li`), so every hit card flashes simultaneously at t=0 while the log staggers in — intentional per "estado final only", enforced by refute asserts | Accept, or drop the `li` animations entirely (keep only per-line `.fx`) in a future pass — transient 0.4s, one-shot |
| Low | public/style.css:985-987 (`.fighter.is-attacking .shot`) | Projectile now fully unsynced (fixed `0.15s`, `--step-delay` removed; round-4 `calc()` note moot) — fires on load regardless of its log line, ≥900px only | Leave (card-bound, viewport-gated); gate on the line delay only if a future session re-syncs cards |
| Low | sessions/0086-battle-log-juice.md:46-51 | C1/C4/C5 S1 names don't match real tests: C1 names presenter `test_entries_grouped_by_round_chronological` (doesn't exist — presenter still has `..._newest_first`, chronological proof is `battle_view_test.rb:86`); C4 names `test_log_tiered_pacing_delays` (actual `test_battle_log_pacing_uses_tiered_step` + `..._cumulative_per_entry`); C5 names `test_effect_sync_step_delay` (actual `test_juice_effects_sync_per_line` + `test_battle_arena_carries_step_delay_and_per_line_fx`) | Align the three rows to real test names at validation (S2) — doc-only |
| Info | views/battle.erb:197-198 | `--log-delay` duplicated inline on `li` and `span.fx` with the identical value (`.fx` would inherit the custom property from the `li`) | Drop the span's inline style and inherit — byte savings per line, cosmetic |
| Info | views/battle.erb:84 (carried) | Modal close `<label>` without keyboard semantics, focus never moved into `role="dialog"` — unchanged since round 4, still out of narrow scope | Future a11y pass; acceptable under CSS-only constraint |

## Residual

One Medium (stale e2e order assertion — fix + S1 alignment, then re-review is
trivial). All else non-blocking, ready for user validation (phase 3, S2 table
per criterion) once the e2e is flipped. No engine/routes/db touched; S3 §7
note (C1-flip + modal cap + per-line fx, "C2–C5 intactos") matches the delta.
