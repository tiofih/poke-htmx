# Revisao escopada — sessao 0078, commit `3770ba2`

**Escopo:** verificacao do unico commit `3770ba2` ("Sessao 0078: escopa o alinhamento do
modal de fim de batalha e ancora o refute do fragmento", 5 arquivos). Leitura minima
(`git show`, `rg -n`, `sed -n`) + teste por mutacao do escopo CSS. Nenhum arquivo de
producao editado; mutacoes revertidas (arvore identica ao commit).

## VEREDITO: **Aprovado**

blocker 0 / alta 0 / media 0 / baixa 0 / info 2

## Evidencia por item

| # | Item | Resultado |
| --- | --- | --- |
| 1 | Seletor antigo sumiu; nenhum novo atinge `_strike_result.erb` | ok — `public/style.css:1765` e a unica regra de `modal-head` no bloco 0078 (base `:1521/:1529` intacta); `views/_strike_result.erb:12` nao tem `.end-state-modal`, entao o seletor novo nao casa esse markup |
| 2 | Mutacao A: seletor de volta para `.res-screen .modal-head` | **RED** — `./scripts/test test/design_system_test.rb` → 22 runs/479 asserts/1 failure em `test/design_system_test.rb:89`. Revertido (`git diff` vazio) |
| 3 | Mutacao B: manter o escopado e **adicionar** um `.res-screen .modal-head` nu | **RED** — 22 runs/481 asserts/1 failure em `test/design_system_test.rb:91` (`refute_match` de vazamento nao e vazio). Revertido (`git diff` vazio) |
| 4 | `refute_match` de `test/battle_end_states_test.rb:76-77` executa antes das contagens e e ancorado | ok — o fragmento `result` (`:74`) e o `#result-box` de `views/battle.erb:88`, que contem o `modal-head` de `:96` (sem `<h2>`) e o `state-head` (com o unico `<h2>`, provado pelo `assert_equal 1, result.scan("<h2")` de `:80`); um segundo `<h2>` no `modal-head` dispara o `refute` **e** o count |
| 5 | Afirmacao de "sem efeito diferencial" em `:1760`/`:1761` | **concordo** — `.res-screen { padding/border-top }` e `.res-screen:first-of-type` dependem so da classe e da posicao (nos dois views o overlay e o primeiro `div` filho de `#result-box`, precedido apenas do `input` checkbox), logo aplicam identico nas duas views |
| 6 | Superficie do commit | ok — 5 arquivos (`public/style.css`, `views/battle.erb`, 2 testes, `sessions/0078-*.md`); nada em `lib/`, `server.rb`, `db/`, `Gemfile*`; diff CSS aditivo (2 linhas trocadas por comentario + 1 regra), sem reformatacao; nenhum arquivo de `reviews/` commitado (os 4 relatorios estao untracked) |

## Achados

| Severidade | file:line | Problema | Recomendacao |
| --- | --- | --- | --- |
| info | test/design_system_test.rb:89-92 | As duas asserções mutaram RED apenas em rodadas separadas: a `assert_match` de `:89` roda primeiro e aborta o metodo, deixando o `refute_match` de `:91` sem execucao naquela rodada | nenhuma acao — comportamento esperado do Minitest; a rodada B comprovou o `refute` isoladamente |
| info | — | Numeros nao re-verificados por orcamento: suite completa (1180 runs/6217 asserts), lint (136 arquivos) e a contagem escopada de 47 runs/746 asserts | confiar nos numeros do commit; neste review so `test/design_system_test.rb` foi executado (2 rodadas de mutacao, verde no HEAD) |

Nenhum achado de codigo. Escopo corrigido de fato: `views/_strike_result.erb` volta a
receber a regra base `.modal-head` (title a esquerda, respiro preservado) e o alinhamento
`flex-end` fica restrito ao modal `.end-state-modal` de `views/battle.erb:95`.
