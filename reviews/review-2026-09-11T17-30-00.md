# Review 0086 round 3 (S7 narrow: c2f2c74 tiered pacing + Pular) — 2026-09-11

Scope: `96a4c8b~1..HEAD` (Passos 1-5) with focus on `c2f2c74` (C4 pacing + Pular);
C1–C4 + S3 coherence, cumulative pacing math, skip + reduced-motion order, G1.
No edits, no commits.

## VEREDITO: Aprovado

- C4 math correct: `views/battle.erb:155-177` — `step_of` boundaries exact
  (R1–3=1.0, R4–9=0.5, R10+=0.2; `r >= 10 → 0.2`, `r >= 4 → 0.5`, else 1.0),
  accumulation oldest→newest (`reverse_each`) while display stays newest-first,
  so both the per-group stagger (`(size-1-group_index)*0.25`) and the cumulative
  `step_delay` grow toward the newest entry — monotonic, same direction as the
  0069 stagger. Within-round consecutive diff == tier step proven by
  `test_battle_log_pacing_uses_tiered_step` (`assert_in_delta`, 0.001).
- Skip CSS-only coherent: checkbox sibling before `.battle-log`
  (`battle.erb:166-167`), kill-rule `.log-skip-input:checked ~ .battle-log
  .log__entry { animation: none !important }` at `style.css:1824`, ordered
  BEFORE the final reduced-motion net (`style.css:2209-2243`, last rule in file)
  — proven by `test_log_skip_disables_pacing_before_final_reduce`
  (skip_index < reduce_index). No specificity conflict in practice (both resolve
  to `animation: none`).
- C2 intact: final net covers new (`.log__entry`, `.log-round-head`,
  `.log__entry--defeat`) + legacy selectors with `!important`
  animation+transition; defeat line (no `data-round`, no `--log-delay` per Passo 4
  fix) still covered via `.log__entry--defeat` class.
- C1/C3 intact under c2f2c74: delta is pacing accumulation + skip control only;
  headers/`data-round`/chips/consumption copy untouched.
- S3 coherent: session §7 records C4 addition dated 2026-09-11 (tier values +
  `c2f2c74` + "C1–C3 intactos"); §4 C4 row + §7 C4 validation row exist.
- G1: `git diff 96a4c8b~1..HEAD -- lib/battle_engine.rb lib/battle_service.rb
  db/ server.rb` EMPTY; presenter diffs read-only (copy/stock replay from
  `engine.items`, no value changes); targeted
  `battle_view + style_responsive + battle_log/juice_presenter`: **55 runs,
  483 assertions, 0 failures**; lint 26 offenses = pre-existing baseline,
  NONE in 0086 files.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Low | sessions/0086-battle-log-juice.md:50 | C4 S1 names tests/attrs that don't exist: `test_log_tiered_pacing_delays` (actual style test is `test_log_skip_disables_pacing_before_final_reduce`; tier step is proven in `battle_view_test.rb`), `data-log-index` attribute (no such attr in `battle.erb` — entries carry `data-round` + `--log-delay`), e2e `tiered pacing + skip` (no pacing/skip test in `e2e/specs/battle-log.spec.ts`) | Align C4 row to the four real tests (`test_battle_log_pacing_is_cumulative_per_entry`, `test_battle_log_pacing_uses_tiered_step`, `test_battle_log_has_skip_control`, `test_log_skip_disables_pacing_before_final_reduce`); either add the e2e pacing/skip test or drop the e2e claim — doc-only, no code change |
| Low | views/battle.erb:155-177 | Cumulative delay unbounded: long battles make the newest (top, newest-first) line wait the full sum (e.g. ~10 rounds ≈ 12s+) with `both` fill hiding entries until their delay; Skip button mitigates but is the only escape for non-reduced-motion users | Keep (tiers approved in T103); consider a cap (`min(acc, N)`) or per-round-only accumulation in a future session — not this one |
| Info | repo lint baseline | 26 offenses repo-wide, zero in 0086 files (`battle_view`, `style_responsive`, presenters clean) | Hold "no new offenses"; debt stays in its own pass |
| Info | views/battle.erb:189-193 | Defeat line intentionally outside pacing (no `data-round`, delay 0) — animates immediately while the grouped log staggers | Intended per Passo 4; noted only |

## Residual

Nenhum bloqueador. Ready for user validation (phase 3, S2 table per criterion).
Doc-only follow-up: fix the C4 S1 test names (finding 1) at validation time.
