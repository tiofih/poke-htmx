# Review — sessão 0078, rodada 3 (S7, teto) — escopada ao commit `be93e68`

Escopo: **somente** o commit `be93e68` ("Sessao 0078: titulo unico no fim de batalha e reaprovacao S3
da variante state-card", 5 arquivos, +10/-4) e se ele introduziu defeito novo. A sessão não foi
re-auditada.

## VEREDITO: Requer ajuste — 0 blocker / 0 alta / **1 média** / 1 baixa / 2 info

| Severidade | file:line | problema | recomendação |
| --- | --- | --- | --- |
| **média** | `public/style.css:1763` | `.res-screen .modal-head{justify-content:flex-end;margin-bottom:0}` não fica restrito à variante `state-card`. `views/_strike_result.erb:11-13` é **caminho vivo** (`server.rb:1095`, `POST /battle/strike` no fim; coberto por `test/battle_strike_routes_test.rb:77,118`) e também usa `.res-screen`, com o `h2` como **único** título dentro do `modal-head`. Sobrepondo o base `.modal-head{justify-content:space-between;margin-bottom:1em}` (`public/style.css:1521-1527`), o título daquele modal sai da esquerda e encosta no `×` (o close continua à direita) e o respiro do cabeçalho some — regressão visual em caminho vivo, sem teste. O comentário de `:1762` ("o titulo fica so no .state-head") é falso para o `_strike_result.erb`. O vazamento de `.res-screen` para esse arquivo já existia (rodadas anteriores), mas esta propriedade visível é nova neste commit | Escopar à variante: classe própria no `.modal` de `views/battle.erb:95` + `.end-state-modal .modal-head`; ou `.modal-head:has(+ .state-card)`; ou `.res-screen .modal:has(.state-card) .modal-head` |
| baixa | `test/battle_end_states_test.rb:78` | `refute_match(/<div class="modal-head">\s*<h2/, body)` é global no `body`, enquanto a view irmã viva mantém legitimamente `<div class="modal-head"><h2>` (`views/_strike_result.erb:13`): qualquer resposta futura que traga os dois fragmentos falha por motivo alheio ao fim de batalha da 0078 | Ancorar no fragmento já extraído (`result`), como as asserções de contagem, ou usar o hook de classe do achado média |
| info | `test/battle_end_states_test.rb:76-79` | Mutação executada (reinserido `<h2><%= end_title %></h2>` no `modal-head`): **RED** confirmado — `6 runs, 60 assertions, 1 failures`, `BattleEndStatesTest#test_results_card_rewards` "Expected: 1 / Actual: 2" (`battle_end_states_test.rb:76`), idêntico ao RED declarado. A contagem não é vácua. O `refute` de `:78` não chega a ser exercitado (a 1ª asserção aborta o teste); sua não-vacuidade é por construção do regex contra o markup mutado. Mutação revertida | nenhuma |
| info | `sessions/0078-open-design-battle-1a1.md:68` | Nota S3 tem data (2026-09-16), §7 (`:91`) segue vazio e não há Done — ok. Mas atribui a reaprovação a "rodada 3 do loop S7" (o revisor), quando quem reaprova é o usuário (S3) | Citar o canal/data da reaprovação do usuário |

## Evidências

- **Risk principal (Center/Mart)** — sem blocker: `views/_center_modal.erb:4` (`<h2>Poke Center</h2>`) e
  `views/_mart_modal.erb:4` (`<h2>Poke Mart</h2>`) não foram tocados pelo commit e não carregam `.res-screen`
  (`rg res-screen views/` só em `views/battle.erb:94` e `views/_strike_result.erb:11`), então a regra nova não
  os alcança. `./scripts/test test/battle_end_states_test.rb test/battle_routes_test.rb test/mart_routes_test.rb`
  → **98 runs / 604 asserts / 0 failures / 0 errors**.
- **Duplicata eliminada**: `views/battle.erb:96-97` — `modal-head` só com o `.modal-close`; único `<h2>` do fim é
  o de `state-head` (`:102`). CTAs (Novo confronto / Poke Center / Poke Mart) e restart de game-over intactos
  (`battle_end_states_test.rb` cobre os 4 estados, verde). Guardas novas verdes.
- **CSS aditiva e no bloco certo**: bloco `0078` começa em `style.css:1755`; as 2 linhas novas entram em `:1762-1763`,
  sem reformatação/reordenação (diff de +2/-0 no arquivo).
- **G2 / docs**: `git show --stat be93e68` = `docs/draft-backlog.md`, `public/style.css`, `sessions/0078-...`,
  `test/battle_end_states_test.rb`, `views/battle.erb` — nada em `lib/`, `server.rb`, `db/`, `Gemfile*`.
  O item stale `.side-title:1778` saiu de `docs/draft-backlog.md:392`. O arquivo da sessão teve 1 linha alterada
  (a nota S3), sem §7/Done.
- **Arvore limpa**: mutação revertida; `git status --short` não mostra arquivo rastreado modificado.

## Nota de processo

A única pendência é uma linha de seletor CSS; um re-review escopado de `style.css:1763` + os 3 testes citados
resolve. Se o orquestrador julgar o vazamento para `_strike_result.erb` fora do escopo da 0078 (ele já vinha das
rodadas anteriores), a disposição alternativa é reclassificar como baixa e registrar no `docs/draft-backlog.md`
— mas o comentário de `:1762` precisa então ser corrigido, porque afirma algo falso para aquele arquivo.
