# Review — 0086 round 11 (T151 strike gates + per-entry juice + li wrapper, S7)

Scope: a93bdb3 only (`server.rb`, `views/_jx_gates.erb`, `views/_strike_log_entry.erb`, `views/battle.erb`, `public/style.css`, `test/battle_strike_routes_test.rb`). C8 stone order + G1 engine untouched. No edits, no commits.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Medium | views/battle.erb (controls) | Post-finish Batalhar button still live (carried round-10): strike uses `hx-swap="none"`, T151 OOBs only log/gates/fighters/modal — `#play-btn` never re-renders, post-finish clicks return `""` no-op. State safe, UX dead button. | Next pass: OOB-disable/hide `#play-btn` when `result[:finished]`; assert disabled/absent after finish in `battle_strike_routes_test.rb`. |
| Low | e2e (no file change) | No browser proof of the new `<template>` OOB wrapper: ruby tests assert markup strings only; `e2e/specs/battle-log.spec.ts` (`+1 .log__entry` per Batalhar) not re-run after the wrapper change. htmx 2.0.3 unwraps `<template>` OOB content (documented table-row fix pattern, matches commit message), so risk is low — but the exact regression the wrapper fixes is only provable in-browser. | Run `battle-log.spec.ts` in user validation (fase 3); if green, C8 browser-proof restored. |
| Low | server.rb (strike_side_delay) | Per-strike delay hardcoded `0.0` both sides and `.arena --step-delay` stays at full-render `acc`: shake `animation-delay` stale, C5 stagger dead for strikes (previously noted intentional). Harmless but the comment claims scoping without mentioning the stale arena var. | Keep; one-line comment noting arena `--step-delay` intentionally static during strikes. |
| Low | server.rb (strike_gates) | `attack = entry[:action] != :item` relies on raw attack entries carrying no `:action` (nil, see `BattleEngine#log_entry` — only item entries set `action: :item`). Correct today and consistent with `BattleJuicePresenter`, but works by absence rather than contract. | Keep; consider `entry[:action].nil? \|\| entry[:action] != :item` comment or explicit `:attack` default if engine log shape is ever touched. |
| Low | views/battle.erb (play-btn trigger) | `hx-trigger` still listens `next-round from:body` (carried round-10); strike flow only emits `next-strike`. Dead listener, misleading. | Remove it or comment compat reason. |
| Note | dirty tree | `sessions/0086-battle-log-juice.md` text still at Passo 0-4 (§6/§8 pre-strike), cassette churn + `test/pokemon_routes_test.rb` modifications uncommitted. G1 holds at HEAD a93bdb3, not on dirty tree. | Clean tree + sync session doc before validation sign-off. |

## Checked and clean (no finding)

- Gates flip per click: `strike_gates_oob` renders raw-entry aspects (`hit/dmg` on damage, `ko` on KO, `shot` off only for `:item`, `hp` on damage/heal, `shake` on damage); carrier `#jx-gates` ships all-off in full render; CSS `:has(> #jx-gates[...])` mirrors aggregates without arena re-render. Tests: `test_strike_oob_flips_arena_gates_for_current_entry`, `test_battle_arena_ships_jx_gates_carrier_off`.
- Replay per strike only: `strike_juice(engine, entry)` scopes `BattleJuicePresenter` to `[entry]` — verified compatible with raw entry keys (`attacker` int fallback, `target_name`/`attacker_name` present); `fainted?` still uses current engine HP so KO gray persists while `ko` gate fires once. Test asserts prev attacker/target classes absent across distinct damaging strikes.
- One li per strike: `<template hx-swap-oob="beforeend:#battle-log">` wrapper; `li` no longer carries OOB attr. Tests assert wrapper + no direct li OOB. (Browser leg: see Low above.)
- Skip/reduced-motion intact: skip kills same target elements with `!important`; final `prefers-reduced-motion` block (`animation: none !important` on `.fighter*`, `.arena`, `.bar-fill`, `.log__entry*`) beats ID-specificity gate rules. No new animation escapes.
- G1: `lib/ db/` untouched by a93bdb3; G2/G3: per-task report 12 strike + 116 suite green, 0 new failures (1 pre-existing item copy) — taken as reported, dirty tree excluded.
- Prior round-10 lows not in T151 scope (modal markup duplication, `format_single` rindex fallback, full 6v6 `innerHTML` per strike) unchanged — no regression, still future passes.

## VEREDITO: Aprovado

Per-strike gates + scoped juice + template wrapper meet the brief with tests; stone order append-only preserved; a11y (skip/reduced-motion) provably intact; engine untouched. Residuals: one carried medium (dead button after finish) + lows, none corrupting state. No S7 re-loop required — proceed to user validation (fase 3), with e2e `battle-log.spec.ts` as the manual-gate item.
