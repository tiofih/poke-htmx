# Review — 0086 round 2 do S7 (Passo 32: cor por tipo no `.log__entry .fx` + doc/comentário) — 2026-09-14

Escopo re-revisado: `147ad98` (Passo 32, `public/style.css` + `test/style_responsive_test.rb`) e `39b42eb` (doc de sessão + comentário em `views/_strike_result.erb`), HEAD `39b42eb`; base `a93bdb3`. Round anterior: `reviews/review-2026-09-14T10-08-24.md` (`Requer ajuste`, 1 major + 3 minors). Nenhum arquivo editado.

**Evidência executada nesta rodada (2026-09-14):**
- `./scripts/test` → **1171 runs, 6107 assertions, 0 failures, 0 errors, 0 skips** (bate com o claim do doc).
- `./scripts/lint` → 136 files, **no offenses**.
- `git diff --stat a93bdb3..HEAD -- lib/battle_engine.rb lib/battle_service.rb db/` → **vazio** (G1).
- `git log a93bdb3..HEAD -- test/cassettes/` → **vazio** (sem cassette churn commitado).
- `git log a93bdb3..HEAD -- REQUIREMENTS.md` → **vazio**; `SESSIONS.md` só foi tocado por `f77493d` (S3 C9–C15, pré-round-12), não pelos dois commits do round 2.

## Verificação item a item

**1. O MAJOR está resolvido nos DOIS caminhos — sim.**
- *Entrada (`.log__entry .fx`)*: o elemento `<li class="log__entry" data-move-type="…">` (`views/battle.erb:199-200`, `views/_strike_log_entry.erb:8-9`) casa `[data-move-type="<tipo>"] { --fx-color: var(--t-<tipo>) }` (`public/style.css:1986-2003`). Especificidade `(0,1,0)` **igual** à de `.log__entry { --fx-color: var(--t-normal) }` (L939); a regra nova vem depois na fonte → vence na cascata. `.fx`, filho, herda `--fx-color` e o consome em `background: radial-gradient(circle, var(--fx-color), transparent)` (L2014). Cor por tipo restaurada.
- *Arena (`.shot`)*: as 18 regras do carrier `.arena:has(> #jx-gates[data-move-type="…"])` (L1962-1979) seguem intactas; `.shot` as consome via `var(--fx-color, var(--t-normal))` (L1011). Nada do Passo 29 foi removido.
- *Sem terceira regra sombreando*: as únicas escritas de `--fx-color` são L931/L939 (defaults), L1962-1979 (arena) e L1986-2003 (entrada) — a última é de L2003, nada depois; os consumos L1011 e L2014. `var(--fx-color)` continua **genuinamente lida** (não há var morta; o gotcha `gotchas/0086-round7-gotchas.md:8` é sobre `--jx-*`, não `--fx-*`).
- *Efeito colateral auditado*: `#jx-gates` também carrega `data-move-type`, então o seletor global escreve `--fx-color` nele também — mesmo valor já propagado ao `.arena`, sem conflito nem vazamento (o `.shot` não é descendente de `#jx-gates`).

**2. O teste re-anchorado é realmente vermelho sem o fix e verde com ele — sim (prova empírica).**
Extraí o CSS real e rodei o **mesmo** regex do teste (`/^[ \t]*\[data-move-type="<t>"\][^{]*\{[^}]*--fx-color:\s*var\(--t-<t>\)/m`, semântica Ruby `/m` = dotall):
- CSS **com** o bloco do Passo 32 → **18/18 tipos casam**.
- CSS **sem** as linhas 1981-2003 (bloco + comentário) → **0/18** (todos os 18 falham).
- Regex **antigo** (sem `^[ \t]*`) contra o CSS sem o bloco → **18/18** (ou seja, o ancorage é exatamente o que tira o ponto cego anterior, que casava o seletor do carrier).
O regex `^[ \t]*\[` não casa a linha `.arena:has(> #jx-gates[data-move-type="…"]) { … }` (que começa com `.arena`), só a regra standalone — o `refute_match(/\.log__entry\[data-move-type=/)` (L356) continua sem hit. A asserção verifica a presença do token por tipo **na entrada**, conforme exigido pelo round 12.

**3. Nenhuma asserção enfraquecida em `a93bdb3..HEAD`.** No round 2 a única mudança de asserção existente foi o **fortalecimento** do anchor (`^[ \t]*`). Varri todas as linhas `-` de `test/` no range: as remoções são renomeações/substituições de asserts por versões iguais ou mais fortes (ex.: `test_strike_appends_exactly_one_log_line` ganhou `refute_includes … OOB`; `test_strike_button_*` passou a validar atributos do `<button>` específico + `refute_nil`), e a troca de `"usou Pocao"` por `"usou 1 Pocao"` + `"restam 0"` é a alteração S3 autorizada (C3). O swap do assert de shake em `test_juice_effects_sync_per_line` acompanha a mudança de comportamento C10 (shake no card, com `refute` da arena) — não é enfraquecimento.

**4. Registro no doc de sessão — preciso (sem conclusão marcada).**
§Status = `Implementação (fase 2, TDD) | **Concluída** — Passos 1–32 verdes … aguardando validação do usuário` e `Validação (fase 3) | Pendente`; §6 tem as linhas **29, 30, 31, 32**; §7 tem a **nota S3 datada 2026-09-14** com evidência G1 real (`git diff --stat a93bdb3..HEAD -- lib/battle_engine.rb lib/battle_service.rb db/` → vazio; suíte 1171/0; lint 0 — reproduzi os três). As células da tabela §7 seguem **vazias/pendentes** (nenhum `Aprovado`/`Done`), o que respeita S2/S3 e a regra "parar na fase 3". O texto do S3 descreve corretamente o mecanismo (mapping migrou ao carrier no Passo 29, Passo 32 devolve à entrada).

**5. Higiene — limpa nos dois commits.** `147ad98` e `39b42eb` tocam somente `public/style.css`, `test/style_responsive_test.rb`, `views/_strike_result.erb` (comentário) e `sessions/0086-battle-log-juice.md`. Sem engine/XP/gate/rotas/CSRF/JS, sem cassettes, sem `REQUIREMENTS.md`, sem docs untracked staged. O corpo/markup de `views/_strike_result.erb` é **idêntico** entre `6fb9322` e `39b42eb` (só as linhas de comentário mudam) e o comentário agora dá o motivo correto (`outerHTML` + `disabled`), sem a alegação falsa sobre `delete`.

**6. Info carregado (gap de review) — não bloqueia.** `a93bdb3..HEAD` = 9 commits; o round 12 revisou só 29–31; Passos 21–28 (`8542a33`…`5cf6c4a`) seguem sem arquivo em `reviews/` (o round 11 cobre só `a93bdb3` = Passo 20; rounds 8/10/11 cobrem fases anteriores). É lacuna de **registro de processo**, não defeito de código: C8–C15 têm testes verdes, a suíte/lint fecham verdes e o round 12 já revisou as interações do Passo 29 com o CSS do Passo 26 que geraram o major. Como a fase 3 (validação) é do usuário e o veredito de fase 2 é o desta rodada, aceito como **Info documentado** — recomendo registrar a decisão de escopo ou agendar um review retroativo de 21–28 antes de fechar a sessão.

## Achados (worst-first)

| Severidade | file:line | problema | recomendação |
|---|---|---|---|
| **Low** | `test/battle_strike_routes_test.rb:90` | A mensagem do assert ainda afirma `"htmx 2.0.3 sem delete"` — a **mesma** informação falsa que o round 12 mandou remover do comentário do `_strike_result.erb`. `delete` é swap válido do htmx 2.x (inclusive `hx-swap-oob="delete"`). Não afeta o teste (é só string de mensagem), mas propaga a alegação errada no output da suíte. | Trocar por mensagem factual, ex.: `"swap do botao e OOB (outerHTML + disabled)"`. |
| **Low** | `sessions/0086-battle-log-juice.md:9` + §6 (`101-117`) | O Status diz "Passos 1–32 verdes", mas a tabela §6 só lista os Passos **0–4 e 23–32** — 5–22 não estão nela (o trabalho existe: notas S3 + commits + reviews 8/10/11, e `./scripts/check_docs` passa). Claim não rastreável só pela tabela do plano. Gap **pré-existente**, não introduzido pelo Passo 32. | Enumerar 5–22 (ou uma linha remetendo às notas S3/commits) para o "1–32" ser verificável no próprio doc. |
| **Info** | — | Cobertura de review dos Passos 21–28 (ver item 6). | Não bloqueia o sign-off; registrar a decisão de escopo ou agendar review retroativo. |

## Checado e limpo (sem achado)

- Major do round 12 resolvido nos dois caminhos com especificidade/ordem de fonte conferidas; nenhuma terceira regra sombreia; `--fx-color` segue consumida (L1011, L2014) — sem var morta.
- Teste do Passo 32 é genuinamente sensível ao fix (0/18 → 18/18); o regex antigo era cego (18/18 sem o bloco).
- `views/_strike_result.erb`: markup byte-idêntico; comentário factual.
- Suíte 1171/0 e lint 0 reproduzidos; G1 vazio; `REQUIREMENTS.md` intocado; sem cassette churn; sem scope creep.
- Rede `@media (prefers-reduced-motion: reduce)` continua a ÚLTIMA regra do arquivo (verificado), e o bloco novo fica bem antes dela.
- §7 sem nenhum resultado preenchido; nenhuma fase marcada como Done/validada.

## VEREDITO: **Aprovado** — 0 high / 0 medium / 2 low / 1 info

O major (cor por tipo sombreada no `.log__entry .fx`) está **efetivamente corrigido e verificado de forma independente** (cascata + prova empírica do teste), sem regressão no caminho do `.shot`. Os três minors do round 12 foram resolvidos (doc/S3, comentário do `delete` no view, sync de status/plan). Restam dois achados **Low** não-bloqueantes (mensagem falsa sobre `delete` num teste; traceabilidade da tabela §6) e o Info de cobertura de review 21–28, que **não bloqueia** o sign-off — a validação é do usuário. Nenhum blocker.
