# Review 0082 round 5 (S7 narrow: T81 ee57645 two-button split) — 2026-09-11

Scope: `ee57645` (Curar vs Curar e batalhar) on chain 2942ffb..HEAD; C1–C3 + S3 coherence, no auto-battle on plain heal, G1–G3. No edits, no commits.

## VEREDITO: Aprovado

- Two buttons behave: `views/_center.erb:20-26` renders `Curar time` (plain `POST /team/heal`) + `Curar e batalhar` (hidden `heal_and_battle=1`, same endpoint, `hx-swap="none"` both). Disabled parity on both (`@heal_cost.zero? || @balance < @heal_cost`); shared inline reason above (C1 intact).
- No auto-battle on plain heal: `server.rb:772-778` only appends `oob_battle_view_forced` when `heal_and_battle_requested?` (`params[:heal_and_battle].to_s == "1"`); plain heal returns team fragment + `oob_close_center_modal` + `oob_team_view` + `oob_nav_badge` only. Proof: `test_heal_from_battle_refreshes_battle_view_with_new_confront_enabled` now asserts `refute_includes battle-view` + team pill `Pronto p/ batalhar`; `test_heal_outside_battle_omits_battle_view_oob` green.
- Explicit opt-in works: `test_heal_and_battle_from_battle_starts_fresh_confrontation` (heal_and_battle=1 from battle) asserts forced `battle-view` OOB + enabled `hx-post="/battle/play"` + no disabled gate. `test_center_modal_offers_heal_and_heal_and_battle` asserts both buttons render.
- C1/C2 intact: modal-close chain (`oob_close_center_modal`, `server.rb:792-794` slot pattern) + OOB balance untouched by `ee57645` (delta is `heal_and_battle:` kwarg + forced-OOB helper + second form + tests). `test_heal_via_modal_closes_and_rerenders` + `test_heal_rerenders_balance_oob` (team_routes:1417) present.
- S3 coherent: session §8 records split (Curar heal-only, no auto-battle; second button navigates) dated 2026-09-11; matches code. C3 S1 name mismatch only (finding, doc-only).
- G1: targeted `modal + battle` 83 runs / 1 failure = pre-existing RNG `test_battle_end_shows_gameloop_ctas` (seed-dependent, fails with stash per task, unrelated to heal). All 4 heal tests green. G2: economy diff empty (`lib/heal_cost_policy.rb lib/heal_service.rb lib/reward_rule.rb`, 2942ffb..HEAD). G3: `checar-sessao 0082` verde + this verdict.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Low | sessions/0082-center-modal-heal.md:48 | C3 S1 names `test_heal_and_battle_button_navigates` which does not exist; actual coverage is `test/battle_routes_test.rb:347 test_heal_and_battle_from_battle_starts_fresh_confrontation` + `:385 test_center_modal_offers_heal_and_heal_and_battle` | Align C3 row to the two real test names; no code change |
| Low | test/battle_routes_test.rb:323 | `test_heal_from_battle_refreshes_battle_view_with_new_confront_enabled` name is stale post-split: it now asserts `refute_includes battle-view` (no refresh) | Rename to `..._omits_battle_view_oob` (or `..._shows_ready_without_forcing_view`); test body already correct |
| Low | server.rb:776,784-790 | `heal_and_battle` path uses `oob_battle_view_forced` unconditionally, bypassing `battle_view_requested?`; clicked from index (no `#battle-view` in DOM) the OOB has no target — harmless (htmx ignores) but untested | Either gate forced-OOB on battle context too, or add `heal_and_battle outside battle` test asserting harmless no-op; keep index button if confrontation entry from index is desired |
| Low | server.rb:774-778 | Plain heal from battle invalidates engine (`settings.battle.invalidate`) but omits view OOB, so visible `#battle-view` keeps stale disabled confront until next navigation/reload — intended per split, but implicit contract | Document in session §8 (one line: plain heal resets state, view refreshes on explicit confront); no code change |
| Info | test/battle_routes_test.rb:411 | `test_battle_end_shows_gameloop_ctas` RNG failure, pre-existing, blocks literal "suite 100% green" | Keep recorded as pre-existing; no action in 0082 |
| Info | repo lint baseline | `lint` 26 offenses pre-0082 baseline; G1 "lint 0" never literal while debt (`server.rb:76` AbcSize et al.) stands | Hold "no new offenses" for 0082; debt in own pass |

## Residual

Nenhum bloqueador. Slice ready for user validation (phase 3). Foreign worktree hunks (`public/style.css`, `test/pokemon_routes_test.rb`, cassettes, `docs/draft-backlog.md`, untracked docs/reviews) stay out of 0082 — land separately.
