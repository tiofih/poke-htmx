# Review — sessão 0081 C1-C3 (S7 rodada 1)

- **VEREDITO: Requer ajuste** (1 high bloqueia o `Aprovado`)
- Escopo revisado: `0a95d43~1..143bfe3` — 8 arquivos (`lib/reward_rule.rb`, `lib/heal_cost_policy.rb`, `lib/heal_service.rb`, `lib/battle_service.rb` + 4 testes). G2 contido confirmado: diff vazio em `db/ views/ public/ lib/opponent_generator.rb lib/team_budget.rb scripts/`.
- G1 não re-executado nesta revisão (sem container DB aqui); conta com o verde declarado (63 battle+progression+reward, 22 heal, 13 reward). Re-rodar `./scripts/test` + `./scripts/lint` antes da validação.
- Sweep já mede o caminho real (`scripts/sweep-balance.rb:29,54-55` usa `rule.xp_for` + `ExperienceCurve`), por isso a ausência de diff em `scripts/` é ok para C3 — falta só anexar a saída como evidência na validação (S2).

## Achados (pior primeiro)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| high | `lib/reward_rule.rb:7` + `lib/battle_service.rb:240` | C1 (`lose_levels` 0, "derrota sem nível") contradiz C3: o caminho real de recompensa usa `xp_for`, e `lose_xp` segue 20 — derrota ainda concede XP que acumula e sobe nível pela curva. O teste `test/battle_service_test.rb:768` cristaliza isso (`lose` → +20 XP/membro). `levels_for` não tem mais nenhum chamador em prod, então C1 hoje só vale no caminho morto. | Decidir um dos dois e ajustar critério + código + teste: (a) derrota realmente sem progressão → `lose_xp: 0` (+ teste `xp_for(:lose) == 0`); ou (b) derrota dá XP mas não nível direto → reescrever C1 como "sem `grant_levels` direto" e renomear o teste. Sem isso, C1 está verde por construção da fixture (1020 XP não cruza o limiar no teste, mas acumula em sequência). |
| medium | `test/battle_service_test.rb:768,786` + `:443` | Nomes de teste presos à semântica antiga: `test_grant_finished_xp_increments_one_on_lose/draw` hoje afirmam XP (20/25), não "+1 nível"; comentário `:443` ainda documenta o bypass D3 B ("`grant_levels` coerente com `levels_for`") como arquitetura vigente. | Renomear para `..._grants_lose_xp` / `..._grants_draw_xp` e atualizar o comentário `:443` para o caminho `xp_for → grant` (ou remover se obsoleto). |
| medium | `lib/progression_repository.rb:45` | Bypass `grant_levels` segue como API pública produtiva (set direto de nível + `xp` retroativo via `cumulative_xp_for`), com suíte própria em `test/progression_repository_test.rb:184-252`. "Fim do bypass" vale só para `battle_service`; restam duas fontes da verdade para nível (set direto vs. derivado da curva). | Decidir explicitamente: remover `grant_levels` de prod (mover setup de teste para `grant` com `xp_for`, ou helper de seed) ou registrar no §8 que ele permanece como API de seed/teste fora do caminho de recompensa. Do jeito atual, qualquer chamador futuro reabre o bypass sem violar teste algum. |
| low | `lib/heal_service.rb:36-41` | `average_level` faz 1 `progression.get` por membro (N+1) e o fallback `&.fetch(:level) \|\| BASE_LEVEL` mascara `nil` como nível 5 silenciosamente. | Aceitável para time ≤ 6; se tocar de novo, buscar níveis em lote (ou via `team.all` já hidratado) e logar/falhar em `nil` inesperado em vez de assumir 5. Não bloqueia. |
| low | `lib/heal_cost_policy.rb:17-20` | `cost` aceita `missing_hp` negativo (`cost(-5)` → custo negativo); só não ocorre porque `missing_hp` fixa em 0 antes. | Clampar `missing_hp.to_i` em `[0, ...]` dentro de `cost` (uma linha) para a API pública ser segura por si. Não bloqueia. |

## Notas (não-achados, para não reabrir à toa)

- `HealCostPolicy#cost` com default `average_level = BASE_LEVEL` preserva compat: `cost(10)` → 5 e `cost(10, rate 2)` → 20 seguem verdes; `preview_cost`/`heal` acompanham o escalado (teste novo `test_heal_charges_scaled_cost` coerente: 20 HP × 0.5 × 10/5 = 20).
- Guarda anti-duplo-grant (`@granted_xp_engines`) preservado e re-testado em XP (1050, não 1100).
- `battle_service.rb:463` (`xp_gained` via `xp_for`) já era XP-compatível; sem ajuste pendente.
