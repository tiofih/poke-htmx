# Review 0089 — Bloco B (Passos 6–8): 4 e2e de battle-log + copy do `end_state`

Data: 2026-09-16 · Escopo fechado: `48a6774` (Passo 6), `dfd3741` (Passo 7), `85f48af` (Passo 8). **Bloco A fora do escopo** (outro revisor, `db/`).

Método: `git show`/`git diff` dos 3 commits, `rg -n` com contexto cirúrgico, mutação do `reward_lead` de `over` (revertida por `git checkout`), testes escopados + os 4 arquivos do gate, `./scripts/lint`, `./scripts/check_docs` (declarado aceito), e2e real (`docker compose restart web` → `npx playwright test specs/battle-log.spec.ts --reporter=line`). Nada editado/commitado fora deste arquivo; `git diff --stat -- lib/ views/ server.rb test/ e2e/` vazio no fim e o container `web` deixado no ar.

## VEREDITO: Aprovado (0 blocker / 0 alta / 1 média / 2 baixa / 2 info)

C7 e C9 são provados de verdade: os 4 e2e passam com `buildBudgetTeam` (9 passed), e a derivação do `end_state` é ponto único de fato — a mutação deixa os **dois** testes RED (modal e render full), algo que o mapa duplicado do Passo 7 não garantia. A média é de redação/edge-case (draw mascarado por `over`) e não bloqueia a fase 3; as demais são residuais.

## Média

| Severidade | file:line | Problema | Recomendação |
| --- | --- | --- | --- |
| média | `lib/battle_end_state_presenter.rb:20,27-29` (consumido em `views/battle.erb:126-127`) | `state` devolve `"over"` **antes** de olhar o vencedor e `REWARD_LEADS["over"] = "Derrota"` é incondicional: um game over que terminou em **empate** (double KO — `battle_engine.rb:216` devolve `winner = nil` com os dois times zerados, estado exercitado por `test/battle_end_states_test.rb:49` via `finish_draw`; time sem dinheiro para curar) imprime badge/título "Game Over — Sem dinheiro para curar" mas copy "Derrota — +XP". C9 diz "`draw` emite Empate", então a letra do critério não é cumprida nessa combinação (nenhum teste cobre `over`+`draw`). | Derivar o lead também do vencedor quando o engine não tem vencedor (`reward_lead` → "Empate" se `@engine.winner.nil?`), ou registrar na sessão/C9 que `over ⇒ Derrota` é a decisão explícita (narrowing do critério). |

## Baixa

| Severidade | file:line | Problema | Recomendação |
| --- | --- | --- | --- |
| baixa | `e2e/specs/battle-log.spec.ts:121-127` | A única prova e2e do C9 é condicional: `if ((await page.locator('#result-box .rewards').count()) > 0)` + `if (badge.includes('Oponente'))`. Se a rodada terminar sem o bloco de recompensa (ou com vitória), os asserts de copy são **pulados silenciosamente** e o teste passa vazio — não pegaria a regressão que o C9 corrige. A prova forte são os 2 Minitest, mas o critério lista este e2e como evidência. | No ramo de derrota, assertar presença explícita (`.rewards` count ≥ 1) antes de checar o texto, ou fixar o fixture de derrota. |
| baixa | `views/battle.erb:127` e `views/_strike_result.erb:24` | A frase de **vitória** ("Seu Time ganhou … XP por Pokémon … +¥…") continua duplicada literalmente nas duas views; só o lead de derrota/empate foi para o presenter. Uma edição futura de copy em uma só view reabre exatamente a divergência que o C9 fechou. | Só quando houver próxima mudança de copy: mover a sentença inteira (ou o lead "ganhou") para o presenter. |

## Info

| Severidade | file:line | Problema | Recomendação |
| --- | --- | --- | --- |
| info | `lib/battle_end_state_presenter.rb:12,20` | `REWARD_LEADS["loss"]` repete o literal "Derrota" que já existe em `STATES["loss"][:label]`; duas fontes do mesmo texto no mesmo arquivo (hoje idênticas). | Opcional: `REWARD_LEADS["loss"] = STATES["loss"][:label]` ou comentário apontando a dependência. |
| info | ambiente (não é código) | A 1ª rodada escopada deu **122 runs / 9 failures / 18 errors** com `PG::TRDeadlockDetected`, `TeamRepository::TeamFullError` e `DuplicateError` — dois `rake test` no mesmo container `web`/`db` (o outro revisor rodava em paralelo) truncando/inserindo durante o teste alheio; isso poluiu justamente os testes do C9 (`BattleEndStatesTest#test_game_over_reward_copy_matches_badge` **errorou** por TeamFullError em vez de falhar por assert). A 2ª rodada, sozinha, deu **122 runs / 879 asserts / 0 failures / 0 errors**. | Registrar no processo: com 2 revisores em paralelo, serializar `./scripts/test` (ou DB separado) — senão um RED real pode virar `Error` e passar por ruído. |

## Verificações pedidas (evidência)

1. **Passo 6** — `rg -n 'buildTeamOfSix' e2e/` = 0; `:41, :67, :91, :102` usam `buildBudgetTeam` (`:147` def, +5 call sites pré-existentes intactos). `buildBudgetTeam` fixa 6 nomes do catálogo (`CHEAP_TEAM`) e asserta `#nav-badge` `${i+1}/6` em cada add, então a intenção "6 membros para abrir a arena" é preservada e determinística. Os 4 testes não dependem de starter específico: `round headers` só olha `data-round`/chips do log, `reduced-motion` só `getComputedStyle` + tail do CSS, `auto toggle` só o modal OOB, `consumption` só chips de item e a copy do badge — nenhuma asserção por tipo/atributo de Pokémon. Comentário do helper e o header `:8` ("real catalog UI by exact name (buildBudgetTeam below)") estão corretos; nenhuma menção órfã ao helper removido.
2. **Fonte única (Passo 8)** — `rg -n 'reward_lead|end_state' views/ lib/battle_end_state_presenter.rb` mostra só o presenter instanciado nos dois views (`_strike_result.erb:7`, `battle.erb:90`); zero mapa/case duplicado nos `.erb` e zero resíduo de `end_title|end_slug|end_label` (`rg` exit 1). Nenhum método morto: `state` (`battle.erb:99,154`), `slug`/`title`/`winner_name`/`reward_lead` (ambos), `label` (`battle.erb:99`) — todos consumidos. Os 4 estados derivam certo (`over` > `draw` > `win`/`loss`) e `STATES.fetch` falha alto em chave nova.
3. **Mutação** — `"over" => nil` em `REWARD_LEADS`: `BattleEndStatesTest#test_game_over_reward_copy_matches_badge` RED isolado (`battle_end_states_test.rb:77`, "Expected \"…Seu Time ganhou 10 XP…\" to include \"Derrota\"") e `BattleStrikeRoutesTest#test_strike_game_over_loss_copy_matches_badge` RED (`battle_strike_routes_test.rb:103`). **RED nos dois**; revertido (`REWARD_LEADS` volta com `"over" => "Derrota"`).
4. **Markup intacto** — `git show 85f48af` só troca a origem da interpolação: `data-od-id="end-<slug>"`, `data-od-id="res-<slug>"`, `class="state-card"`, `state-head`, `state-pill <%= state %>` (mesmos valores win/loss/draw/over de antes), `mini-arena`, `winner-badge`, `result-card`, CTAs (Poke Center / Novo confronto / Vender itens / Recomeçar jornada), juice, `data-side` e `#battle-view`/`#result-modal`/`#result-box` inalterados. Nenhuma classe/atributo removido ou renomeado.
5. **e2e real** — `docker compose restart web` (200 em 6s) + `npx playwright test specs/battle-log.spec.ts --reporter=line` → **9 passed (40.6s)**, incluindo `consumption and reward copy` (`:101`) e `auto toggle chains to finish` (`:90`); sem flake, sem repetição necessária.
6. **Suíte escopada/lint** — gate dos 4 arquivos: **122 runs / 879 assertions / 0 failures / 0 errors / 0 skips** (e cada arquivo isolado: 7/20/73/22 runs, 0 falhas). `./scripts/lint`: **136 files inspected, no offenses detected**. Números da suíte completa **1182 runs / 6318 asserts / 0 falhas** — **aceitos como declarados** (não re-executada aqui, para não somar ao `db:setup` destrutivo nem concorrer com o Bloco A).

## Observações de ambiente (não achados)

- Sobras no `git status` não são minhas: `M TODO.md` (T2 do revisor do Bloco A) e os cassettes não rastreados em `test/cassettes/` (artefatos de execução da suíte, alguns gerados pelas rodadas concorrentes). Nenhuma modificação em código de produção/testes por minha conta.
