# Review — 0079 `open-design-history-1a1` (`e0e8514..2fb0755`) — 2026-09-15

**Escopo:** refinamento `e0e8514` + Passos 1–3 — `cad1f49` (Passo 1: teste C2 em `test/design_system_test.rb`), `6e41875` (Passo 2: `views/history.erb` + `test/history_curation_test.rb` + extensão de `test/history_view_test.rb`), `2fb0755` (Passo 3: status/§8/§9 da sessão). Spec autoritativa: `sessions/0079-open-design-history-1a1.md` (C1–C2, G1–G3, M1). Review **somente leitura**: nenhum arquivo de implementação/sessão/doc alterado; artefato único. A mutação do ponto 2 foi aplicada e **revertida byte-a-byte** (`git status --short` → vazio ao fim).

```
e0e8514..2fb0755 (0079)
  views/history.erb                |  4 +-   (2 linhas: card → card card--tight)
  test/history_curation_test.rb    | 48 +     (novo)
  test/history_view_test.rb        | 14 +
  test/design_system_test.rb       | 13 +
  sessions/0079-…md                | 17 +
  public/style.css                 | bloco 0079 NÃO está em nenhum commit da 0079 — entrou em 9b421c4 (ver A-1)
```

**Evidência verificada por mim (não herdada):**
- `./scripts/test` → **1180 runs / 6219 assertions / 0 failures / 0 errors / 0 skips** (117,3s) — 0079 incluída (rodei `test/history_curation_test.rb` + `test/history_view_test.rb` + `test/design_system_test.rb` isoladas: **31 runs / 581 assertions / 0 failures**). `./scripts/lint` → **136 files, no offenses**. `./scripts/check_docs` → `ok> docs consistentes`. `./scripts/checar-sessao 0079` → `ok> sessão 0079 estruturalmente consistente`.
- `git diff e0e8514 2fb0755 -- db/ Gemfile Gemfile.lock` → **vazio** (G2 ok); `git log cad1f49 -1 -- SESSIONS.md` / `6e41875` → último toque em `SESSIONS.md` é `67a2b6c` (refinamento da 0077), **nenhum** commit da 0079 tocou `SESSIONS.md`/`REQUIREMENTS.md` (G3/S4 ok).
- Contrato de design: `open-design/history.html:151,167` usam `class="card" style="padding: 8px 10px;"` → `.card--tight { padding: 8px 10px; }` (`public/style.css:1751`) é cópia fiel; `history.html:153-160` mantêm a largura da barra **inline** → `views/history.erb:42` idem (decisão "dado, não veste" confere com o protótipo). Bloco está antes da linha `fim` (`public/style.css:2055`), delimitador `/* === History 1:1 (0079)` em `:1748` (C2 posicional ok).

## Adjudicação dos pontos obrigatórios

**1. Fidelidade S1 — substância ok; a redação de C1 superdimensiona a evidência NOVA.** C2 → `test/design_system_test.rb:273` (`test_design_system_history_curation_classes`) asserta o delimitador `0079` + `.card--tight` dentro do bloco ODS: existe, passa, prova C2. C1 → `test/history_curation_test.rb:20` (`test_history_matches_prototype`) prova **o que a sessão de fato entregou** (o faltante): 2× `class="card card--tight"` (`:27`), ausência de `class="card" style=` (`:29`), os 3 contratos `data-od-id` (`:31-36`), shape da `.rank-bar` inline (`:37`) e o vazio `notice--info` (`:41`). Porém a linha de C1 (`sessions/0079-…md:48`) promete "`history.erb` + `history_page.erb` conferem com `history.html` **classe a classe** (pos-card/chips, rank-row/barra/`.you`, history-row/badge/data)" — **nenhum** teste lê `open-design/history.html`, e essa superfície nomeada é provada por testes **pré-existentes da 0075** (`test/history_view_test.rb:42,45-47,60,74,120-129`), não pela evidência nova. `views/history_page.erb` (3 linhas, wrapper) não foi tocada e o contrato `#history-view` é assegurado em `test/history_routes_test.rb:17` — apesar do comentário novo em `test/history_curation_test.rb:8` dizer que ele trava o `#history-view`. **Shape-only:** o que a 0079 adiciona de teste é um **contrato/trava** (a sessão anteviu isso em §8:103), e a extensão de `test/history_view_test.rb:98` **duplica verbatim** os dois asserts de `history_curation_test.rb:27,29` — zero cobertura nova. O que faltaria para "classe a classe" literal: um teste que faça o diff classes-do-render × classes-do-proto (ou, no mínimo, remover a extensão duplicada e apontar C1 explicitamente aos testes da 0075 que provam a superfície restante). Nada disso é gap funcional.

**2. Guards são red-capazes — UMA mutação reproduzida (behavioural).** Revertí a curadoria em `views/history.erb:35` (`class="card card--tight"` → `class="card" style="padding: 8px 10px;"`, o estado pré-0079):
- `./scripts/test test/history_curation_test.rb test/history_view_test.rb test/design_system_test.rb` → **`31 runs, 570 assertions, 2 failures, 0 errors, 0 skips`**, com `1) Failure: Expected: 2` e `2) Failure: Expected /class="card" style=/ to not match "…<div class=\"card\" style=\"padding: 8px 10px;\">…"` (o corpo completo do render no diff do failure).
- Revertido (`git checkout -- views/history.erb`): **`31 runs, 581 assertions, 0 failures, 0 errors, 0 skips`**; `git status --short` → vazio. A guarda não é vacuosa. Nota: o `refute_match` de `history_curation_test.rb:29` fica **mascarado** dentro do próprio teste pelo `assert_equal 2` anterior (quem disparou foi a cópia em `history_view_test.rb:98`) — a duplicação é o que o torna observável.

**3. Disciplina de escopo — nada fora do escopo declarado; a ressalva é de atribuição de commit (A-1).** O diff são as 5 linhas previstas (§3). G1 substantivo ok (árvore limpa, suíte 1180/0, lint 136/0). G2 ok (sem `db/`/gems). G3 ok (`check_docs` + `checar-sessao` verdes; `SESSIONS.md` intocado, linha 515 ainda `Refinada`). Design: fiel ao mockup (acima); único desvio de `DESIGN.md` é o componente novo `.card--tight` não listado (A-3).

**4. Processo — sem alegação de validação/`Concluída`.** `sessions/0079-…md:8` diz "TDD concluído … aguardando Revisor S7. PARAR — sem Done, sem §7, sem commit de conclusão"; §7 toda `pendente`. Correto. A ressalva é a A-1 (commits da 0079 não são auto-consistentes) e A-4 (o Status diz "Passos 1-2 verdes" num commit — `2fb0755` — que **é** o Passo 3).

**5. Risco de regressão — baixo.** `.card--tight` é classe nova, sem colisão (0 ocorrências antes; usada só em `views/history.erb:35,52`); `.card` base permanece `public/style.css:220` e a mudança só afeta os 2 cards de history (intencional). Nenhum parcial compartilhado, token `:root` ou rota tocados (`server.rb` intacto). O `refute_match(/class="card" style=/)` é page-wide mas `/history` só renderiza `history.erb` — sem falso-positivo hoje. Um teste existente pega a regressão? Sim: a própria guarda nova (mutação acima) + `history_view_test.rb`/`history_routes_test.rb`.

## Achados

| Severidade | file:line | problema | recomendação |
|---|---|---|---|
| **Média** | `cad1f49`, `6e41875` (diff) vs `9b421c4` | **Os commits da 0079 não são auto-consistentes: o artefato de C2 não está em nenhum deles.** `git show cad1f49:public/style.css \| grep -c card--tight` → **0**; idem em `6e41875`. O teste que `cad1f49` commita (`test/design_system_test.rb:273`, exige `.card--tight` no bloco) está **RED** nessa revisão e na seguinte; e em `6e41875` a view já referencia `.card card--tight` **sem definição** (cards perdem o padding 8px 10px até `9b421c4`). O bloco entrou em `9b421c4` — commit rotulado "Passo 1: classes battle-end-states/results-desktop no bloco ODS (delimitador 0078)". Bisect/CI nessas revisões quebra. A sessão **divulga** o fato (§8:106), mas o Status (`sessions/0079-…md:8`) afirma "Passos 1-2 verdes" — verdadeiro só na árvore de trabalho, não no commit. | Não dá para reescrever o histórico sem rebase; registrar no Status/§8 que o "verde" dos Passos 1-2 é **de árvore, não commit-atomic**, e nas próximas sessões com `style.css` dirty commitar o artefato com `git add -p public/style.css` (só o hunk do bloco) em vez de deixá-lo unstaged. |
| **Baixa** | `sessions/0079-…md:48` vs `test/history_curation_test.rb:20-39` | C1 promete prova "classe a classe" contra `history.html` (incl. `history_page.erb`); nenhum teste lê o protótipo e a superfície nomeada é provada por testes da 0075. A extensão `test/history_view_test.rb:98` duplica verbatim os asserts de `history_curation_test.rb:27,29`. | Repontar a coluna de evidência de C1: "novo = trava do faltante (`card--tight`/no-inline/`data-od-id`/vazio); restante = `test/history_view_test.rb:42-129` (0075)". Remover a extensão duplicada. |
| **Baixa** | `public/style.css:1751` vs `DESIGN.md:24-29` | Componente novo e reutilizável `.card--tight` não entra na lista canônica de componentes do `DESIGN.md`; o padding `8px 10px` hardcoda 8px apesar de `--gap-xs: 8px` (`DESIGN.md:18`). A literalidade é justificada pela fidelidade 1:1 (`open-design/history.html:151,167`), mas a doc canônica fica desatualizada. | Adicionar `.card--tight` à linha de componentes (`DESIGN.md:24-29`) na validação; opcional usar `padding: var(--gap-xs) 10px`. |
| **Baixa** | `sessions/0079-…md:22,55,102` | G1 prescreve "conferir/**stash** antes de editar (`não commitar alheio`)"; o `style.css` foi deixado dirty e varrido por commit alheio. Substância ok (árvore limpa no HEAD, bloco intacto em `:1748`), método divergente. | Registrar em §8 que o guard real passou a ser "deixar dirty + `grep -c` do delimitador após o commit alheio" — que foi o que de fato protegeu o artefato. |
| **Info** | `sessions/0079-…md:8` vs `2fb0755` | O Status diz "Passos 1-2 verdes … + docs" mas foi escrito pelo commit do **Passo 3** (suíte + lint + `check_docs` + `checar-sessao`), que não é mencionado em §8:105. Subnotifica o próprio trabalho. | Citar o Passo 3 no Status/§8 (já verifiquei os 4 gates verdes). |
| **Info** | `test/history_curation_test.rb:29` | `refute_match(/class="card" style=/)` não pega `class="card card--tight" style="…"` (o seletor mais provável de uma regressão) e é mascarado pelo `assert_equal 2` anterior no mesmo teste. | Trocar por `refute_match(/class="card[^"]*" style=/)` ou mover a checagem para depois do count. |

## VEREDITO: **Aprovado** — 0 blocker / 0 alta / 1 média / 3 baixa / 2 info

A 0079 entrega o que o refinamento fechou: a curadoria 1:1 do `history.html` reduziu-se a um único item real (os dois `style="padding: 8px 10px;"` inline dos cards de ranking/recentes → `.card--tight` no bloco ODS), e isso está feito, fiel ao mockup (verifiquei `open-design/history.html:151,167`), antes da linha `fim`, com delimitador próprio, aditivo e sem tocar `server.rb`, schema, gems ou parciais compartilhados. Os critérios C1/C2 e as garantias G1–G3 **se sustentam no HEAD**: suíte 1180/6219/0 (0079 incluída), lint 136/0, `check_docs` e `checar-sessao 0079` verdes, `db/`+`Gemfile*` intocados, `SESSIONS.md`/`REQUIREMENTS.md` não tocados pelos commits de implementação, e a sessão não alega validação nem `Concluída`. A mutação que apliquei (reverter a curadoria da view) derruba 2 asserts com o corpo do render provando a ausência de `card--tight`/presença do inline; revertida, volta a 581/0 — as guardas **não são vacuosas**. O único achado que eu registraria antes da fase 3 é a **A-1** (Média): os dois commits da 0079 são internamente vermelhos porque o CSS do próprio passo ficou fora do commit e entrou num commit rotulado 0078 — não afeta o que será validado, mas quebra bisect/CI naquelas revisões e merece a ressalva explícita no Status. Nenhum achado fere critério, comportamento ou contrato; nada bloqueia a validação do usuário.
