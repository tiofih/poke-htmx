# Review — 0088 Passos 1–6 (`c54b018..d5a06dd`) — 2026-09-15

**Escopo:** `c54b018..d5a06dd` — refinamento (`c54b018`) + Passos 1–6 (`6e1caa3` engine/índices, `612d68e` presenters, `a9f4f54` markup, `a657c5e` handler+keyframe, `4ede63b` origem+destino, `d5a06dd` verificação final). Spec autoritativa: `sessions/0088-projetil-slot-a-slot.md` (C1–C9, G1–G3, D1a–D8, RNF-01 exceção em `REQUIREMENTS.md:499`). Review **somente leitura**: nenhum arquivo de implementação/sessão/doc/e2e alterado por mim; artefato único. A mutação do ponto 4 foi aplicada e **revertida byte-a-byte** (`git status --porcelain` → 0 arquivos ao fim).

```
c54b018..d5a06dd (Passos 1-6)
 lib/battle_engine.rb          |   9 +-   lib/battle_log_presenter.rb |  9 +-
 lib/battle_juice_presenter.rb |  10 +   server.rb |  3 +-
 views/{layout,battle,_jx_shot,_fighter_panel}.erb | 44 +
 public/style.css              |  20 +-  e2e/specs/battle-log.spec.ts | 186 +++++---
 test/{battle_engine,move_engine,battle_log_presenter,battle_juice_presenter,
       battle_strike_routes,battle_routes,battle_view,layout,style_responsive}_test.rb
 sessions/0088-projetil-slot-a-slot.md | 17 +
```

**Evidência verificada por mim (não herdada):**
- `./scripts/test` → **1180 runs / 6219 assertions / 0 failures / 0 errors / 0 skips** (seed 56173, 111,0s). `./scripts/lint` → **136 files, no offenses**. `./scripts/check_docs` → `ok> docs consistentes`.
- `cd e2e && npx playwright test specs/battle-log.spec.ts` → **5 passed / 4 failed**; os 4 vermelhos falham em `buildTeamOfSix` (`#nav-badge` N/6) em `/`, antes de `/battle` — pré-existentes (`docs/draft-backlog.md:216`), não tocados. Verdes: `measured pair`, `container gate`, `reduced motion keeps`, `without JavaScript`, `arena keeps 3 in-flow columns`.
- Sonda Playwright própria em `/battle` vivo (arena 1200): ver adjudicação 1 e 3.
- Probes de no-op em `/`, `/team`, `/pokemon?name=…`, reduced motion e 900px (adjudicação 2 e 3).
- `git diff c54b018..d5a06dd -- SESSIONS.md REQUIREMENTS.md docs/draft-backlog.md` → **vazio** (só `c54b018` tocou docs, como S4 exige).

## Adjudicação dos pontos obrigatórios

**1. O offset medido é robusto — sim; o duplo disparo é real e não compõe.** Sonda viva: **2 `htmx:afterSettle` por golpe** (`{settles:2}`), confirmando o "dispara por nó OOB" do Passo 5. A base é medida por layout (`shot.offsetLeft/offsetTop/offsetWidth/offsetHeight`, `views/layout.erb:81-82`) e as três vars são **escritas** com `setProperty` (`:86,:89,:90`), nunca acumuladas. Reproduzi o teste de composição: chamei `placeShot()` 3× à mão depois do golpe e os valores **não cresceram** (strike 0: ox `246.0078125px`→`246.0078125px`, oy `-92.53125px`→`-92.53125px`, dy `-259.65625`→`-258.65625`; strike 1: ox/oy idênticos, dy `-259.65625`→`-259.6794`). A variação ≤1px vem de geometria real (não de composição). Todas as execuções leem o mesmo nó atual e a mesma base. **Falha que eu tentei construir**: reordenar os OOB ou repetir o settle não quebra — a última execução sempre roda com os dois swaps aplicados; a única dependência implícita é `.shot` estar `display:block` no momento da medida (ver B-1).

**2. O handler é no-op limpo fora de `/battle` — sim.** `views/layout.erb:64-92` guarda por reduced motion (`:65`), `min-width:981px` (`:66`) e `#jx-shot-track .shot` (`:68`, retorna `null` sem track) — a query existe mas é inofensiva. Probe: `/` com 5 settles → `{fxWrites:0, shot:false}`, sem `pageerror` (só o 404 pré-existente de `/favicon.ico`); `/team` e `/pokemon?name=pikachu` → sem shot, sem erro. Nenhum write de `--fx-*` em DOM sem arena.

**3. Origem/destino e estado inicial — corretos; fallbacks idênticos ao rail de 0087.** Medição independente no keyframe resolvido (`currentTime=0` e `endTime`): **origem x 0,03px / y 0,38px; destino y 0,88px** — mesma ordem de grandeza do reportado (≈0,25/0,45) e dentro da tolerância `<2px`; `offsetParentIsTrack:true`, `offsetWidth/Height:10`. Assets não deslocam: `img` carrega `width="96" height="96"` (`views/_fighter_panel.erb:16`). Reduced motion ao vivo: nó novo, `animationName:none`, `display:block`, vars todas `''`, sem erro. 900px: vars `''`, sem erro. Sem JS: o e2e (minha execução, verde) crava `y=0` e `|x| ≈ 0,35·arena + 42 + 10`. Valores de baseline red confirmados: a mutação do ponto 4 reproduziu **190,6171875px** de erro de origem — exatamente o 190,6 do Passo 4; `--fx-dy` medido = `-259,65625px`, o mesmo deslocamento que gerava os 259,6px de destino.

**4. Guards são red-capazes — UMA mutação reproduzida (behavioural, não vacuous).** Removi as duas linhas de origem (`views/layout.erb:89-90`):
- `./scripts/test test/layout_test.rb -n /projectile_handler/` → `1 runs, 14 assertions, 1 failures`: `o offset horizontal da origem vai para --fx-ox inline. Expected /setProperty\(\s*["']--fx-ox["']/ to match "…"` (`test/layout_test.rb:108`).
- `npx playwright test … -g "measured pair"` → `Error: origem x (from=0) / Expected: < 2 / Received: 190.6171875` em `e2e/specs/battle-log.spec.ts:256` → `1 failed`.
- Revertido: `git diff --exit-code -- views/layout.erb` limpo (0 arquivos); teste estático `1 runs, 22 assertions, 0 failures`; e2e `1 passed`.
As asserções novas de C6/C7/C9 são **behavioural**; a de `vars` em contexto `javaScriptEnabled:false` é redundante (garantida), mas a geometria do rail é sensível à perda do fallback (B-5).

**5. RNF-01 (G2) — substância OK, registro desatualizado.** Um único `<script>` inline (`views/layout.erb:37`) + CDN htmx (`:7`); `public/` só tem `style.css`; `EventSource|WebSocket|setInterval|sendBeacon|XMLHttpRequest|new Function|eval(` → **nenhum**; sem `onclick/onload/onsubmit/onchange`; sem `src=`/import novo. Mas a exceção registrada (`REQUIREMENTS.md:499`) diz que o handler "mede `getBoundingClientRect()` … e escreve **`--fx-dy`**", enquanto o implementado mede pela base de **layout** (`offsetLeft/offsetTop`) e escreve **três** vars (M-1).

**6. Fidelidade S1.** C1/C2/C3 são unit behaviourais (C1 ainda compara chaves **e** hash exato; o teste novo exige a transição b1→slot 1). C6 é o par ordenado vivo (não vacuous: exige as duas direções em `:279`). C8 tem estático **e** e2e behavioural (verifiquei ao vivo). C9 tem e2e behavioural do no-op em 900px. **Shape-only**: C4/C5 são regex de markup — mas ficam lastreados pelo e2e, que resolve os cards por `data-slot` (atributo ausente ⇒ `card()` nulo ⇒ falha). C7 é e2e sintético (B-5); G2/G3 são revisão manual de diff, como declarado.

**7. Leftovers/regressões.** Nenhum critério/teste ficou vacuous *novo*. Os achados não bloqueantes de `docs/draft-backlog.md:210-230` **não pioraram nem foram corrigidos em silêncio** (gate `@container` duplicado, comentário obsoleto em `test/style_responsive_test.rb:332-333`, `is-attacking` sem consumidor, e2e fora do CI, invariante de `offsetParent` sem assert). A classe do invariante **ganhou uma instância nova** (B-1). Sem código morto novo relevante (I-1 é payload especificado mas não consumido).

**8. Processo — OK.** `sessions/0088-…` sem `Concluída`/alegação de validação; Status mantém Implementação/Validação `Pendente` e a seção 7 vazia. `SESSIONS.md` intocado no range (só `c54b018`, refinamento, conforme S4). S3 2026-09-15 presente em `REQUIREMENTS.md:499`, `sessions/0087-…` e `docs/draft-backlog.md` (incl. a correção da afirmação falsa do "primeiro script").

## Achados

| Severidade | file:line | problema | recomendação |
|---|---|---|---|
| **Média** | `REQUIREMENTS.md:499` vs `views/layout.erb:64-92` | A exceção ao RNF-01 registra "mede `getBoundingClientRect()` … e escreve `--fx-dy`", mas o handler embarcado (introduzido por Passos 4 **e 5**) mede a base por layout (`offsetLeft/offsetTop`, `:81-82`) e escreve **três** vars — `--fx-dy` (`:86`), `--fx-ox` (`:89`), `--fx-oy` (`:90`). As vars de origem excedem D1a e o escopo §3 (que nomeiam só `--fx-dy`), sem reabertura formal de D1a. A substância (1 handler, htmx-bound, sem lib/polling/SSE) está intacta, mas o registro S3 que existe para vigiar a exceção **não descreve o que foi entregue** — enfraquece exatamente a disciplina G2. | Amendar a exceção (e a descrição de D1a no arquivo da sessão) para nomear as três vars e a base por layout; o usuário reaprova na validação (já declarado). Quem edita `REQUIREMENTS.md` é outro agente — esta sessão não pode. |
| **Baixa** | `views/layout.erb:81-82` | A base assume `shot.offsetParent === shot.parentElement` e `.shot` **renderizado** (`offsetWidth>0`) no momento do disparo. Nenhum dos dois é guardado/asserido. Se uma regra futura tornar `.battle-column` posicionada (mesma classe de fragilidade do achado de 0087), ou se o último settle cair com o gate off (`.shot display:none` ⇒ `offsetLeft/Width = 0`), origem/destino ganham offset silencioso. Hoje o gate já está `on` no último disparo (o settle da track vem depois do de `#jx-gates`) — robusto por ordem, não por construção. | Guarda de 1 linha: `if (shot.offsetParent !== shot.parentElement || !shot.offsetWidth) return;` (e/ou derivar a base do rect da track + `left/top` computados). |
| **Baixa** | `views/layout.erb:87-90` vs `:81-82` | Inconsistência de técnica: a **base** é imune a `transform`, mas os centros de atacante/alvo vêm de `getBoundingClientRect()` (sensível a `transform`). Hoje mascarado porque `juice-flash`/`juice-shake` têm `animation-delay: var(--t-impact)` = 0,55s (`public/style.css:941,2471`), então os cards estão em repouso no `afterSettle`. Re-medindo o `dy` 1s depois, vi drift de até 1px em 1 de 2 golpes (ainda <2px). Em frame lento o card poderia estar no meio do shake. | Medir os centros via layout (`offsetTop`-chain) como a base, ou deixar o e2e provar a estabilidade em N golpes (tolerância menor). |
| **Baixa** | `e2e/specs/battle-log.spec.ts:186-195` | Os dois `waitForFunction(...).catch(() => undefined)` engolem timeout; se o nó não for trocado, a medição pode ler um `.shot` **stale** e compará-lo aos cards do próprio `data-from-slot`/`data-to-slot` — as asserções de C6 passariam. Só `expect([...seen]).toEqual(['0','1'])` (`:279`) protege fracamente. Pré-existente (`docs/draft-backlog.md:216`), mantido no diff. | Remover o `.catch` para um nó ausente **falhar**, ou asserir dentro do evaluate que o nó não tem `data-probe-old`. |
| **Baixa** | `test/style_responsive_test.rb:332-333` | Comentário falso (pré-existente): diz que `.battle-column` "deixou de ser ancestral do nó", mas desde o Passo 7 da 0087 a track mora **dentro** dela. O assert segue correto; só o texto engana. Não corrigido pela 0088. | Reescrever: a coluna voltou a ser ancestral; a direção continua no `.shot` porque a track é fixa na coluna 0. |
| **Baixa** | `e2e/specs/battle-log.spec.ts:362-422` | C7 ("sem JS o rail de 0087 continua"): em `javaScriptEnabled:false` o handler comprovadamente não roda, então `vars == {ox:'',oy:'',dy:''}` é garantido; o gate é ligado por instrumentação do teste (o comentário admite). A asserção que **prova** algo é a geometria do rail (`y===0`, `x ≈ 35cqi+42+10`), que é red-capaz. Evidência = "fallback quando as vars inline estão ausentes", não um fluxo no-JS ponta-a-ponta (que é impossível: sem JS não há gate/golpe). | Registrar essa leitura de C7 na tabela da seção 7 (fase 3) — o critério é atendido, mas a evidência é sintética. |
| **Info** | `lib/battle_log_presenter.rb:65-67` | `slot_fields` (`from_slot`/`to_slot`) é emitido em toda entrada do log, mas **nenhuma view consome** (o markup do shot usa `BattleJuicePresenter`, `views/battle.erb:55`). É exigido por C3 e testado, porém hoje é payload morto. | Manter como contrato especificado, ou remover após a validação se ninguém consumir. |
| **Info** | `public/style.css:1030-1035` vs `:2480-2494` | Gate `@container (min-width: 981px)` **duplicado** (pré-existente, `docs/draft-backlog.md:215`); o primeiro bloco é subconjunto estrito do segundo e o teste estático lê o último. A 0088 não agravou (mexeu nos keyframes, não nos gates) e não corrigiu. | Unificar em um único bloco e `refute` um 2º gate de travel. |

## VEREDITO: **Aprovado** — 0 blocker / 0 alta / 1 média / 5 baixa / 2 info

A implementação fecha o objetivo da 0088 (saída no slot do atacante e chegada no slot do alvo em x e y) como *progressive enhancement* sobre o rail de 0087: os índices são serializados no engine (`attacker_index`/`target_index`, espelhando o item), chegam ao DOM por `data-slot`/`data-from-slot`/`data-to-slot`, e o handler único mede os centros reais e escreve as custom properties consumidas pelos keyframes com fallback `0`. O ponto de maior risco — o duplo disparo do `htmx:afterSettle` — é real (contei 2 por golpe) e a correção por base de layout é idempotente e **não compõe** (verifiquei com 3 chamadas manuais). A mutação que eu mesmo apliquei (remover as escritas de origem) derruba o teste estático **e** o e2e com o erro exacto de 190,6px; revertida, tudo verde — as guardas novas **não são vacuosas**. Suíte 1180/6219/0, lint 136/0, `check_docs` ok, 4 vermelhos e2e pré-existentes separados. Fallbacks verificados ao vivo (reduced motion e 900px zeram as vars; `/`, `/team` e fragmento são no-op sem erro). O único achado que eu pagaria antes da fase 3 é **M-1**: o registro da exceção ao RNF-01 precisa descrever as três vars e a base por layout, senão a própria política que autoriza o JS fica desatualizada em relação ao entregue. Os demais são dívida de robustez/evidência enumerável, nenhum reabre critério nem altera comportamento validado.
