# Review 0086 C1–C3 — S7 round 1 (narrow)

- Sessão: `sessions/0086-battle-log-juice.md`
- Commits: C1 `96a4c8b` · C3 `a50d8dd` · C2 `c118559` (HEAD)
- Escopo: só presenters/views/CSS/testes — engine/db intactos (G1 estrutural ok)
- Suítes tocadas (rodadas pelo revisor): `battle_log_presenter` + `battle_juice_presenter` + `style_responsive` + `battle_view` → **51 runs, 339 assertions, 0 failures**
- Full suite + lint (Passo 4): **não executados nesta revisão** (ver achado M2)

## VEREDITO: Requer ajuste

Motivo: metade da evidência S1 (e2e por critério) não existe; sem ela ou sem reabertura formal dos critérios (S3), C1–C3 não fecham.

## Achados (pior primeiro)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| High | `e2e/specs/` (ausente) ↔ `sessions/0086-battle-log-juice.md:46-49` | S1 quebrado: C1/C2/C3 apontam e2e (`battle-log.spec.ts`: round headers newest-first, reduced-motion disables juice, consumption and reward copy) que **não foram criados** — `e2e/specs/` só tem `a11y, modals, team-add, team-remove`. Só há cobertura Minitest | Criar os 3 e2e (ou reabrir os critérios via S3 com data + reaprovação do usuário, marcando `manual`/removendo a coluna e2e). Não aprovar sem um dos dois |
| Medium | `views/battle.erb:178` | Linha de derrota carrega `data-round="<%= @engine.rounds %>"` (nº da rodada **mais nova**) mas é renderizada no **fim** da lista (posição da mais antiga): polui qualquer seletor `data-round` newest-first (o próprio teste C1 `rounds.sort.reverse == rounds` quebra numa batalha terminada em derrota) e confunde âncoras Leitores de tela | Remover `data-round` da linha de derrota (ou `data-round="summary"`/sem id numérico) e re-escopar o teste C1 para `li.log__entry[data-round]` excluindo `--defeat` |
| Medium | `sessions/0086-battle-log-juice.md:76` (Passo 4) | G1 incompleto: Passo 4 (suíte completa + lint 0 + commit `Passo 4: regressao`) **não existe**; full suite/lint não rodados na sessão. Revisor rodou só os 4 arquivos tocados (verdes). Falha pré-existente conhecida (`test_battle_end_shows_gameloop_ctas`, RNG/button drift) segue sem evidência de baseline | Rodar `./scripts/test` + `./scripts/lint` cheios, registrar resultado (incl. a falha pré-existente como baseline) e commitar o Passo 4 antes da validação |
| Low | `public/style.css:2126` (bloco antigo, em `c118559`) | Bloco `@media (prefers-reduced-motion: reduce)` legado **sem `!important`**, anterior às regras de juice, mantido como peso-morto: não desliga nada sozinho (a cascata o sobrescreve — era o próprio bug da sessão) e confunde leitura futura | Remover o bloco antigo, mantendo só a rede final `0086` (`:2151`, com `!important` e por último) |
| Low | `sessions/0086-battle-log-juice.md:49` ↔ `test/battle_log_presenter_test.rb` | Deriva de nomes S1: critério C3 promete `test_consumption_reward_text`, mas os testes reais chamam-se `test_item_entries_with_stock_expose_remaining_and_last_unit` etc. Rastreabilidade S1 enfraquecida | Renomear ou atualizar a tabela §4 com os nomes reais |
| Low | working tree (não commitado) | `git status` sujo fora do escopo da sessão: `public/style.css` (+67/−7, resíduo `0077` — `.filter-grid`, `.roster-grid`, `.heal-list`, `.tabs`, `.mart`…) + `docs/draft-backlog.md` + `test/pokemon_routes_test.rb` + cassettes. Risco de vazar para commits da 0086 | Não incluir no Passo 4 nem na validação; commitar/stash separado antes de fechar a fase 2 |

## Observações (não bloqueantes, sem severidade)

- `remaining_map` (`lib/battle_log_presenter.rb`, C3): replay `stock_final + (totals − seen)` verificado correto para 1 uso, N usos e múltiplos itens; chaves string/símbolo tratadas; `stock: nil` preserva copy legada. `BattleEngine#winner → 0/1` confere com `@engine.winner == 1` da linha de derrota — sem falso-negativo.
- `BattleJuicePresenter#css_classes` + `_fighter_panel.erb:7`: conjunto de classes idêntico ao anterior (`is-hit`/`fainted`/`is-attacking` + `engaged` preservado); `juice` está nos `locals` dos dois callsites (`battle.erb:27,191`). Refactor seguro.
- XSS: `<%= entry[:text] %>` não escapa (ERB puro do Sinatra), mas é padrão pré-existente — nenhum sink novo introduzido; fora de escopo, só anotado.
- C2 cobre `.shot`/`.projectile`/legados `.fighter--*` na rede final; teste `test_juice_reduced_motion_disables_all` ancora ordem (`rindex reduce > last juice animation`) + `!important`. Metodologia `getComputedStyle` (não `getAnimations`) está registrada na sessão §8 para o e2e pendente.
