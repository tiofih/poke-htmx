# Review — 0087 Passo 7 (`49ffd74`) — 2026-09-15

**Escopo:** apenas `49ffd74` (Passo 7 da sessão `sessions/0087-strike-fx-chegada-real.md`), `4ae9df0..49ffd74`; Passos 1–6 já adjudicados em `reviews/review-2026-09-15T10-57-40-passos1-6.md` (Aprovado). Review **somente leitura**: nenhum arquivo de implementação/sessão/doc/e2e alterado por mim; artefato único.

```
49ffd74 Passo 7: track do projetil sai do fluxo do grid e ganha guarda de layout ao vivo
        e2e/specs/battle-log.spec.ts            | 48 +++++++++++++++++++++++++
        public/style.css                        |  6 ++++-
        sessions/0087-strike-fx-chegada-real.md | 22 +++++++++++++++
        views/battle.erb                        | 13 ++++++---
```

**Evidência verificada por mim (não herdada):**
- `./scripts/test` → **1173 runs / 6159 assertions / 0 failures / 0 errors / 0 skips** (1ª invocação abortou no flake bundler/DB já registrado; 2ª verde — igual ao round 1).
- `./scripts/test test/style_responsive_test.rb test/design_system_test.rb test/battle_view_test.rb test/battle_routes_test.rb test/battle_strike_routes_test.rb` → **169 runs / 1667 assertions / 0 failures**.
- `npx playwright test --config=e2e/playwright.config.ts e2e/specs/battle-log.spec.ts` → **3 passed / 4 failed**; os 3 verdes são geometria (`:226`), gate (`:272`) e a guarda nova (`:316`); os 4 vermelhos são os pré-existentes do helper.
- Sonda Playwright própria (`/tmp/probe.mjs`, fora do repo) medindo o DOM vivo em 1600/900/640px — números na adjudicação abaixo.
- Diff: 4 arquivos, **nenhum `.js`**, `package.json`/`SESSIONS.md` intocados. O diff não tem `.rb`, logo lint é inafetado por construção.

## Adjudicação dos pontos obrigatórios

**1. A contenção é sólida? Sim — verificada ao vivo, e o racional do `offsetParent` confere.**
Probe em página real (time barato, `/battle`): `#jx-shot-track` tem `offsetParent === DIV.arena`, `position:absolute`, e o **rect da track é idêntico ao rect do `.arena`** (941×974 @1600; 866×2039 @900) — a track continua cobrindo a arena inteira, não a coluna. `.battle-column[data-side="0"]` (pai a partir do Passo 7) não tem **nenhuma** regra CSS no repo (`grep battle-column public/ views/` → só o comentário `public/style.css:999`), medido ao vivo: `position:static`, `overflow:visible`, `transform:none`, `contain:none`, `display:block`; logo não recorta, não cria containing block e não cria stacking context — o `--fx-travel: calc(35cqi + 42px)` segue resolvendo contra o container `.arena` (`container-type: inline-size`) e a ordem de pintura (`z-index:2` do `.shot` vs `10` do `.podium`, ambos no contexto de `.arena`) fica inalterada. Nenhum OOB alcança a coluna: os alvos de swap são `#fighters-N` (innerHTML), `#jx-gates` (outerHTML, continua filho direto do `.arena`), `#result-box`, `#play-btn`, `#battle-view`, `#team-view` e modais — mover a track dentro da coluna não muda o `hx-swap-oob="innerHTML"` de `#jx-shot-track` (`server.rb:1140-1145`), que acha o nó por id; os testes de geometria dirigem o fluxo OOB real e seguem verdes (prova viva). LTR **e** RTL cobertos: o mesmo teste passa nas duas direções.

**2. A guarda é red-capaz e não vacuous — reproduzi a mutação.** Não há leitura de texto CSS/`gates.last`: lê `[...arena.children]` + `document.querySelector('#jx-shot-track')` (id único) e o assert `trackInArena` impede um nó fora da arena de satisfazer a checagem (track ausente → `trackPosition:null` → falha).
- (a) `public/style.css:1003` `position: absolute` → `position: static`, rodando só a guarda:
  `Error: em 1600px a track do projetil / Expected: "absolute" / Received: "static"` → **1 failed**; revertido → **1 passed**.
- (b) track de volta como filho direto do `.arena` (movidas as 3 linhas ERB p/ antes de `div.battle-column`, `views/battle.erb:55-56`):
  `Error: em 1600px a track e filho direto do .arena / Expected: false / Received: true` → **1 failed** (asserção `trackDirectChild`, `:349`); revertido → **1 passed**.
- O 900px **exercita** o regime single-column: probe ao vivo dá `grid-template-columns` = **1 track** em 900px (e 3 em 1600px), com as mesmas 3 fatos estruturais válidos. Ressalva em achado (a guarda não *afirma* a coluna única).

**3. Geometria preservada — sim.** `projectile reaches the target card border (cqi travel measured live)` verde: com a arena ancorada em 1200px, `tx` ficou dentro de ±2 px de `0.35·1200+42+10 = 472` (descarta `vw`) e a chegada fechou dentro da tolerância — `shotRight − cardRight ∈ (−40, 20]` quando o alvo é a coluna esquerda e `shotX − cardX ∈ (−20, 40)` quando é a direita, para `from`/`to` opostos nas duas direções (`['0','1']`). Ou seja: o ponto de pouso medido cai na borda próxima do card alvo (dentro de ~20 px depois / ~40 px antes dela). Esse teste é sensível ao containing block (`left: calc((100% − 2·gap)/3.06)` usa a largura dele), então ele é a prova viva de que a contenção não migrou para a coluna.

**4. Leftovers/escopo — limpo.** `SESSIONS.md` intocado; nenhum `.js` nem dependência; a sessão registra a reabertura **na validação** (`sessions/0087-…:160-181`) e repete `PARADA … nada marcado como Concluída e nenhuma alegação de validação`; o Passo 7 é documentado como reabertura S3, não como fase nova. Os **4 vermelhos pré-existentes** foram reproduzidos: todos falham em `#nav-badge` `Expected "6/6" / Received "5/6"` **durante a montagem do time em `/`**, antes de `/battle` — é o gate de orçamento do helper `buildTeamOfSix` (`:15-24`), que o diff do Passo 7 não pode alcançar (só toca view de batalha + comentário CSS + teste + doc).

**5. O que segue sem guarda na classe.** Ver achados: (i) o invariante que sustenta o fix (`offsetParent === .arena`) não é asserido; (ii) o gate `@container` duplicado do round 1 continua lá e o teste estático lê `gates.last`; (iii) sobra um comentário falso no teste estático. Nenhum overlay absoluto ficou como filho do grid: `.res-overlay`/`.res-dismiss-input`/`.log-skip-input` moram no `.podium` (`views/battle.erb:94/195`), e os filhos do `.arena` medidos são `#jx-gates` (display:none) + 2 `.battle-column` + `.podium` (sticky = in-flow).

## Achados

| Severidade | file:line | problema | recomendação |
|---|---|---|---|
| **Baixa** | `e2e/specs/battle-log.spec.ts:330-350` / `views/battle.erb:47-53` | A guarda prova a *posição* da track mas **não o invariante que o fix usa como justificativa** (`battle-column` é static ⇒ `offsetParent === .arena`). Se alguém puser `position:relative`/`contain`/`transform` na `.battle-column` (regra cosmética futura é plausível — a coluna hoje não tem CSS nenhum), a guarda continua verde (`position:absolute`, dentro da arena, não-filho-direto, 3 em fluxo) e a geometria quebra; quem pega é só o e2e de geometria, que **não roda no CI** (round 1, I1). | Somar 1 assert na sonda: `track.offsetParent === arena` (e/ou `|trackRect − arenaRect| < 1px` nos dois eixos). É a metade que falta para a guarda cobrir o racional escrito no próprio comentário/mensagem do commit. |
| **Baixa** | `public/style.css:1028-1033` vs `:2472-2484` | Gate `@container (min-width: 981px)` **duplicado** (round 1, B3): o bloco de `:1028` é subconjunto estrito do de `:2472`, que vence por vir depois; é regra morta que volta a valer se o segundo for removido/alterado, e o teste estático lê `gates.last` (`test/style_responsive_test.rb:359-360`) — editar o primeiro passa despercebido. O Passo 7 comentou o CSS (`:995-1001`) mas não fechou isso. | Manter um único gate (o com as direções) e adicionar `refute` de um 2º `@container` de travel. |
| **Baixa** | `test/style_responsive_test.rb:332-333` | Comentário agora **falso**: "o `.shot` saiu do card para a track do `.arena` … `data-side` do `.battle-column` — que deixou de ser ancestral do no". Depois do Passo 7 a `.battle-column` **é** ancestral de novo (o pai, inclusive). O assert em si segue correto e verde (a direção continua vindo de `data-from-side` do `.shot`, que é o certo, pois a track mora sempre na coluna 0). | Reescrever o comentário: a coluna voltou a ser ancestral no Passo 7, e a direção continua no `.shot` porque a track é fixa na coluna 0 independentemente de quem ataca. |
| **Baixa** | `e2e/specs/battle-log.spec.ts:322-350` | O laço `[1600, 900]` reafirma os **mesmos** 3 fatos; não afirma a diferença de regime que o comentário do teste invoca ("as duas larguras"). Verifiquei ao vivo que 900px é single-column (`grid-template-columns` = 1 track), mas se a media query `max-width:980px` (`public/style.css:614-616`) fosse perdida, a guarda passaria — a coluna única de 900px só é cravada por regex estática (`test/style_responsive_test.rb:99-105`). Impacto baixo (cobertura existe, texto vs. vivo). | Assert derivado da largura: `gridTemplateColumns.split(' ').length` = 3 em 1600 e 1 em 900. |
| **Info** | `public/style.css:620-622` vs `:789-790` | Pré-existente, fora do diff: o override `@media (max-width:700px){.podium{position:static}}` é **morto** — o `.podium{position:sticky}` de `:789` vem depois e vence na cascata. Probe ao vivo em 640px: `podiumPos = "sticky"`. `test/design_system_test.rb:269-270` só casa o **texto** do CSS e por isso passa. Efeito colateral positivo: confirma que descartar o `.podium` como container da track foi a decisão certa em **todas** as larguras, e não só acima de 700px. | Corrigir a ordem/especificidade (ou mover o override para depois de `:789`) e, se o "unstickies" importa, provar por teste vivo de `getComputedStyle`. |
| **Info** | `views/battle.erb:46-56` | A track passa a viver dentro da subárvore da coluna 0: um `display:none` na `.battle-column[data-side="0"]` mataria o projétil junto (filho absoluto não sobrevive a `display:none` do pai). Não existe tal regra hoje (medido: `display:block`, `overflow:visible`, sem OOB apontando para a coluna) e a alternativa (`.arena`) era justamente o defeito corrigido. | Nenhuma ação; apenas registrar que o custo da garantia estrutural é esse acoplamento — o assert sugerido no achado 1 também protege a metade "coluna continua medível". |

## VEREDITO: **Aprovado** — 0 blocker / 0 alta / 0 média / 4 baixa / 2 info

O Passo 7 fecha a classe de defeito que a validação do usuário expôs: a track deixou de poder virar item do grid (container static dentro de `.battle-column`, contendo `.arena` verificado ao vivo por `offsetParent` e rect idêntico) e a fragilidade ganhou guarda de regressão **red-capaz nas duas mutações que reproduzi com as saídas exatas** (`"absolute"`/`"static"` e `false`/`true`), não vacuous, em 1600px e 900px (single-column confirmado ao vivo). A geometria do Passo 5 segue provada ao vivo com o ponto de pouso na borda do card alvo nas duas direções; suíte 1173/6159/0 e os 169 testes estáticos de CSS/view verdes; escopo limpo (sem `.js`, sem dependência, `SESSIONS.md` intocado, reabertura registrada sem alegação de validação) e os 4 vermelhos de `buildTeamOfSix` são pré-existentes e alheios (falham em `/`). Os achados são dívida de evidência/higiene — nenhum altera comportamento validado nem reabre critério; o achado 1 é o único que eu recomendaria pagar junto do Passo 7 (1 linha).
