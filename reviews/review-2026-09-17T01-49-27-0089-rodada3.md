# Review 0089 — rodada 3 (teto S7): verificação escopada

**Veredito: Aprovado** — 0 high / 0 medium / 2 low / 1 info. Os 5 achados das duas revisões
anteriores fecham com prova; nada fora de escopo nos 2 commits.

## Escopo verificado

- `4e19b47` — presenter + 2 views + teste. 4 arquivos, +68/−18.
- `5b36a24` — `e2e/specs/battle-log.spec.ts` + `test/journey_service_test.rb`. 2 arquivos, +16/−6.
- Fora do escopo: nenhum. `db/`, `server.rb`, `sessions/`, `docs/` intocados.

## Evidência

| Achado anterior | Prova |
| --- | --- |
| **media** game over com empate imprimia "Derrota" | `reward_lead` (`lib/battle_end_state_presenter.rb:51-55`) deriva de `@engine.winner` (não do `state`): `0` → `nil` ("ganhou"), `nil` → "Empate", `1` → "Derrota". Semântica de `winner` conferida em `lib/battle_engine.rb:213-220` (nil só com KO duplo). Mutação aplicada (forcei `reward_lead` a devolver sempre o label de derrota) → **RED** em `battle_end_states_test.rb:90` (`test_game_over_draw_reward_copy_says_draw`) e `:101` (`test_reward_sentence_is_byte_identical`); 29 runs/2 failures; revertida (`git checkout`, `git status --short` limpo). |
| **baixa** frase duplicada nos 2 views | `reward_sentence(xp_gained:, money_gained:)` (`:59-66`) é a única montagem; `rg 'reward_sentence\|ganhou 10 XP\|REWARD_LEADS'` só acha o presenter e as duas interpolações (`views/battle.erb:125-127`, `views/_strike_result.erb:22-24`). Constante `REWARD_LEADS` não existe mais. |
| **baixa** guard vacioso do C9 no e2e | `e2e/specs/battle-log.spec.ts:126-130` — `toHaveCount(1)` + `expect(badge).toContain('Vencedor: Oponente')` + copy contém "Derrota" e não "ganhou", todos incondicionais. e2e: **9 passed (36.7s)** após `docker compose restart web`. |
| **baixa** fronteira de 5 membros | `test/journey_service_test.rb:40-44` usa `@team.add` + `@journey.started?` — sem `user_state:`/`mark_started` (API removida não reaberta). |
| **info** `REWARD_LEADS["loss"]` duplicava `STATES["loss"][:label]` | labels vêm de `STATES.fetch(...)`, ponto único. |

Verde reproduzido: escopado `battle_end_states + battle_strike_routes + journey_service` =
**46 runs / 316 assertions / 0 failures**; lint **136 arquivos / 0 offenses**. Suíte completa
1185/6332 **aceita como declarada** (não re-executada nesta rodada, por instrução).

## Achados remanescentes

| Severidade | Arquivo:linha | Problema | Recomendação |
| --- | --- | --- | --- |
| low | `views/battle.erb:126-128`, `views/_strike_result.erb:23-25` | Indentação inconsistente: `<p class="rewards">` ficou deslocado 4 espaços a mais que o `if` que o envolve (herança do diff de extração). | Reindentar (só legibilidade; zero efeito funcional). |
| low | `test/battle_routes_test.rb:793-794` | Pré-existente, não tocado na rodada 2: o assert aceita `assert.match?` com OR frouxo (`/ganhou \d+ XP por Pokémon e \+¥\d+/` **ou** `/Derrota — .../`), então uma troca de copy vitória↔derrota passaria. | Trocar por `assert_equal`/`assert_includes` da string exata — a cobertura por desfecho já existe em `battle_end_states_test.rb`. |
| info | `test/battle_end_states_test.rb:96-108` | O teste byte-idêntico cobre 3 desfechos (win com dinheiro, loss sem dinheiro, draw sem dinheiro) + nil para XP 0 — não cobre loss/draw **com** dinheiro nem `money_gained: 0`. | Suficiente para a extração; ampliar só se a tail de dinheiro mudar. |
| info | `lib/battle_end_state_presenter.rb:52-54` | `@engine.winner` é chamado 2× por `reward_lead` (idem `winner_name`). | Irrelevante para performance; memoizar só se virar hot path. |

## Observações

- Mutação revertida; nenhuma modificação minha permanece (`git status --short` mostra apenas
  ` M TODO.md` pré-existente e untracked alheios). Container `web` deixado no ar.
- Nada commitado (por instrução).
