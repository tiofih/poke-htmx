# Revisao escopada — sessao 0083 (`ui-polish`), rodada 1

**Escopo:** os 4 commits da 0083 (`294cb55`, `cbd3653`, `45e2528`, `71cac6a`) — C1 header gate,
C2 filtros labels+count, C3 pills, C4 `.pcard-meta`.
Leitura minima (`git show --stat`, `git diff`, `rg -n`, `sed -n`) + 3 mutacoes temporarias
(todas revertidas; `git status --short` identico ao baseline) + escopado verde.
Nenhum arquivo de producao editado; nenhum commit feito.

## VEREDITO: **Requer ajuste**

0 blocker / 0 alta / 2 media / 4 baixa / 4 info

C1–C4 tem teste real (as 3 mutacoes deram RED), G2 fecha literalmente e nao ha vazamento de
classe. O que segura a aprovacao sao 2 defeitos de "disabled + hint"/copy do CTA (media):
no mobile o gate fica sem explicacao alguma e no game over ele manda curar quem nao pode pagar.

## Achados (pior primeiro)

| Sev | Local | Problema | Recomendacao |
| --- | --- | --- | --- |
| media | `views/layout.erb:28` + `public/style.css:2056` | `@media (max-width:720px){ .cta-hint{display:none} }` esconde o hint em todo celular/tablet, e o fallback `title` do CTA (`layout.erb:27`) e inalcancavel porque `pointer-events:none` (`style.css:2055`) mata o hover — no ≤720px sobra um botao apagado sem nenhuma explicacao, exatamente a metade "+ hint" do C1; nenhum teste cobre isso (o teste novo vive em `LayoutViewportTest` mas nao asserta nada de viewport). | Manter um hint curto visivel ≤720px, ou amarrar via `aria-describedby` (o hint deixa de depender de hover) e nao esconder; se o motivo e o topnav estreito, quebrar o hint em duas linhas em vez de remove-lo. |
| media | `views/layout.erb:25` | O hint so distingue "vazio" vs "precisa de cura"; `@game_over` implica `@can_battle == false` (`lib/journey_service.rb:19-21`), entao no game over o header manda "Cure o time antes de batalhar." enquanto a propria tela diz que nao ha dinheiro para curar (`views/team.erb:9`) e a pill esta `pill--danger` "Game over" (`views/team.erb:20-23`) — instrucao impossivel no estado terminal. | Terceiro ramo no ternario (`@game_over` → copy de jornada encerrada/recomecar) + assert do texto novo no `test_battle_cta_gated_hint`. |
| baixa | `public/style.css:573,577` | `.pill.warn`/`.pill.danger` perderam o unico consumidor — era `views/team.erb:23` no base `9ec90c3` (`class="pill warn"`/`"pill danger"`) — e o bloco novo duplica as mesmas declaracoes em `.pill--stale`/`.pill--danger` (`style.css:2068,2070`): CSS morto + duas convencoes para o mesmo estado. | Reusar `.pill warn`/`.pill danger` (padrao do repo) ou remover as regras antigas no mesmo commit que introduz as novas. |
| baixa | `test/home_view_test.rb` (C4, `assert cost.nil? \|\| cost < add`) | Escape hatch: se o `.poke-cost` sumir do card, `cost` e nil e o assert passa — a metade "custo antes do +" vira no-op silencioso. | `refute_nil cost` + `refute_nil add` + `assert_operator cost, :<, add`. |
| baixa | `test/home_view_test.rb` (C2, `@media (max-width:360px)…1fr`) | "sem overflow em 360px" e provado por **presenca de texto CSS**, nao por medicao: qualquer outro overflow em 360px passa batido; e o breakpoint `max-width:360px` nao cobre 375px (iPhone SE/mini, mais comum que 360). | Assert no `design_system_test` sobre o conjunto de seletores/`min-width:0` do pane de filtros, ou um teste de layout que meça a largura do documento. |
| baixa | `views/layout.erb:27` (`aria-disabled` em `<a href>`) | `aria-disabled` em link nao desabilita de fato: AT ainda anuncia/aciona o link, o bloqueio e so de ponteiro; `opacity:.45` (`style.css:2055`) derruba o contraste do texto e nao ha `:focus-visible`, entao o estado "gated" nao e perceptivel por teclado. | Se o gate deve valer tambem para AT, usar `<button disabled>` ou `aria-disabled` + `role` coerente; preservar contraste minimo (token, nao opacidade) e um foco visivel. |
| info | `views/_filter_controls.erb:18` | "Resultados · N" duplica o "Arquivo · N" (`views/pokemon_list.erb:12`) e fica acima da grade de filtros, nao junto dos resultados. Nao desincroniza — chequei `filter_param_present?` (`server.rb:235`) = `params.key?`, e o `hx-include=".filter-state"` sempre envia as chaves, entao o OOB (`server.rb:126-137`) re-renderiza o count. | Se o numero repetido for intencional, tudo bem; senao, manter so "Arquivo · N". |
| info | `views/layout.erb:24-25` (`@team_size.to_i.zero?`) | Nil-safe confirmado por leitura e por teste sensivel a mutacao; ressalva latente: se algum dia existir rota com `@can_battle == false` e `@team_size` nil, o hint diria "Monte seu time" mesmo com time cheio. | Derivar o hint de `@team_size` real (`@team_size.nil?`) ou passar o motivo do gate junto do estado. |
| info | Vazamento de classe — **nada** | `.pcard-meta` so e renderizado por `views/pokemon_list_item.erb:5` (via `views/pokemon_list.erb:31,37`, chamado por `views/index.erb:40` e `server.rb:126,629` = catalogo/home); `.pill--*` so `views/team.erb:23`; `.btn--gated`/`.cta-hint` so `views/layout.erb`; `.filter-count`/`.filter-grid .field` so `_filter_controls`. `_strike_result`, `_center_modal`, `_mart_modal`, `_evolution_modal` e o `.tag-row` do roster (`team.erb:42`) limpos. | — |
| info | `public/style.css:2052-2076` (bloco 0083) | Bloco corretamente delimitado (`inicio`/`fim` proprios), aditivo, sem reformatacao/reordenacao, e as 12 regras tem consumidor no markup — nenhuma orfa no bloco novo (`.field` e classe pre-existente do ODS, `style.css:406`). | — |

## Evidencia

- **Mutacoes (todas RED, todas revertidas):** `layout.erb:24` `@can_battle == false` → `@can_battle`
  (`1 runs, 1 failures`, "time vazio: CTA gated no header"); `team.erb:20` pill `empty` → `stale`
  (`1 runs, 1 failures`); `style.css:2073` `display:flex` → `display:block` (`1 runs, 1 failures`).
  `git status --short` final identico ao baseline (so os cassettes untracked pre-existentes).
- **G2 (escopo):** `git diff --stat 9ec90c3..HEAD -- lib/ server.rb db/ config/ Gemfile Gemfile.lock`
  = vazio. `git show --stat` dos 4 commits: so `views/`, `public/style.css`, `test/`, `sessions/`.
- **Escopado:** `./scripts/test test/layout_test.rb test/home_view_test.rb test/design_system_test.rb`
  → 40 runs / 669 asserts / 0 failures / 0 errors / 0 skips. `./scripts/lint` → 136 arquivos, 0 offenses.
  `check_docs` ok; `checar-sessao 0083` ok. (Suite completa nao rodada — os numeros declarados de
  1184 runs nao foram reconferidos.)
