# Review 0082 round 3 (S7 narrow) — 2026-09-11

Scope: T67 `668fb12` (heal success closes Center modal, single notice) + uncommitted T68 working tree (overlay-fade). Verified against HEAD `668fb12` + `git diff` working tree.
User nok round 2: flash, duplicate notice, modal not closing.

## VEREDITO: Requer ajuste (1 item, estreito)

Committed T67 resolves duplicate notice + modal-not-closing. Gate/disabled intact. Targeted files green (108 runs / 843 asserts / 0 fail), `checar-sessao 0082` verde, G2 contido.
Blocker is uncommitted-only: T68 working tree deletes all `.evolution-*` styles and carries foreign hunks — landing it as-is unstyles the evolution modal and leaks out-of-scope changes. Flash-mask itself (overlay-fade) is sound but needs a reduced-motion guard.

## Verificação ponto a ponto

- Modal closes on heal: `server.rb:737` success → `render_heal_success_notice:772-776` returns team fragment + `oob_close_center_modal` (empty `#center-modal` slot, `server.rb:778-780`) + team-view + nav-badge; tests assert empty slot OOB and refute `Poke Center`/`Custo total`. OK (committed).
- Single balance message: close-slot OOB carries no notice; `@notice` set once in `render_heal_success_notice`, rendered once via `team` fragment (`render_team_fragment_with_notice:1023`); `oob_team_view`/`oob_nav_badge` carry no notice. OK (committed).
- Gate/disabled intact: `heal_team:734` gate still returns `journey_gate_notice + oob_center_modal`; `_center.erb:20-21` reason + `disabled` unchanged; prominent-reason test green. OK.
- Flash masked: ONLY in working tree (`overlay-fade` keyframes + `:target/.open` animation + `overflow-y:auto` on `.overlay`, style test `test_overlay_open_fades_instead_of_flashing` green). NOT committed → not delivered. Info, folded into the required item below.
- G1: touched files green (modal + team-heal + style); full suite was 1099/0 at round 2 (`e76cd28`); lint 26 offenses = pre-0082 baseline, no committed regression. G2: `lib/heal_cost_policy.rb lib/heal_service.rb lib/reward_rule.rb` untouched (empty diff + log). G3: `checar-sessao 0082` verde + this verdict.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| High | `public/style.css` (working tree, hunk `@@ -2027,61 +2105,6 @@`) | Uncommitted T68 deletes entire `.evolution-overlay` / `.evolution-modal-box` / `.evolution-row` block (sessão 0068), yet `views/_evolution_modal.erb` + `views/team_manage.erb` still use those classes — committing as-is leaves evolution modal unstyled; no test covers it | Land T68 as scoped commit with ONLY overlay-fade hunk + `overflow-y:auto` on `.overlay` + style test; restore/drop the evolution-block deletion (separate decision, own session if removal is intentional) |
| Medium | `public/style.css` (working tree) | T68 adds `animation: overlay-fade` with no `prefers-reduced-motion` opt-out (existing guards at `:1014,:2112` don't cover it); plus foreign hunks unrelated to 0082 (filter-grid, roster-grid, home 1:1 mart/tabs, `test/pokemon_routes_test.rb` plus-affordance rename, `docs/draft-backlog.md`, cassettes) | Add `@media (prefers-reduced-motion: reduce) { .overlay:target,.overlay.open { animation: none } }` in same commit; move foreign hunks out (own sessions/commits) |
| Low | `server.rb:743,750` | `buy_from_mart`/`sell_from_mart` gates return bare `journey_gate_notice` without `oob_mart_modal` — same stale-modal pattern 08f6b24 fixed for heal (carried from round 2, unchanged) | Follow-up outside 0082: mirror heal gate with `oob_mart_modal` + gate-via-modal test |
| Low | `test/pokemon_routes_test.rb` (working tree) | Uncommitted plus-affordance asserts introduce overlong regex line(s) — new `Layout/LineLength` risk on top of the 26-offense baseline | Keep out of 0082 commit or wrap to satisfy lint before landing wherever it belongs |

## G1–G3

- G1 sem regressão: touched files 108/0 OK; full suite 1099/0 (round 2 base); lint 26 = baseline, sem regressão commitada.
- G2 escopo contido: OK no commitado; working tree com hunks estranhos à 0082 (ver item Medium).
- G3 docs + revisão: `checar-sessao 0082` OK; este veredito (Requer ajuste, 1 item estreito) devolve ao Implementador — sem novo escopo, sem commit por este revisor.
