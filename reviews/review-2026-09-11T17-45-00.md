# Review 0086 round 4 (S7 narrow: 4f1538c effect-sync + 08a6d0e result-modal) — 2026-09-11

Scope: `c2f2c74..HEAD` (Passos 6-7); C1-C5 + S3 coherence, line→effect sequencing,
result modal gated `--log-total`, skip + reduced-motion order, G1. No edits, no commits.

## VEREDITO: Aprovado

- Effect-sync coherent (CSS-only, `4f1538c`): `views/battle.erb:23-42` accumulates
  `step_delay` oldest→newest with exact tier steps, `side_delay` = max per side;
  `_fighter_panel.erb:4,10` exposes it as `--step-delay` on every fighter `li`
  (plus `data-side`). All 5 juice effects gate on `var(--step-delay)`
  (`style.css` flash, damage number, KO, arena shake, projectile) — proven by
  `test_juice_effects_sync_to_step_delay`.
- Result modal gated (`08a6d0e`): `.arena` exposes `--log-total` = final `acc`
  (`battle.erb:43`); `.res-overlay` reuses Center `.overlay.open` + reveals via
  `res-reveal` delayed by `var(--log-total)`; dismiss is a sibling
  `#res-dismiss` checkbox (`:checked ~ .res-overlay { display:none }` — siblings
  confirmed at `battle.erb:79-80` inside `#result-box`); skip forces immediate
  reveal via `.arena:has(.log-skip-input:checked) .res-overlay`. Proven by
  `test_battle_result_gated_modal_after_log` + `test_result_modal_reduced_motion_last`.
- Skip + reduced-motion order correct: skip rules (Passo 6 `:has`, Passo 7
  res-overlay) sit inside the `0086` block, BEFORE the final
  `@media (prefers-reduced-motion: reduce)` net (last rule in file), which zeroes
  both `--step-delay` and `--log-total` with `!important` — proven by
  `test_skip_and_reduce_cover_step_delay` + `test_result_modal_reduced_motion_last`.
- C1-C4 intact: delta `c2f2c74..HEAD` touches only pacing/sync/modal + tests;
  headers/`data-round`/chips/consumption copy untouched.
- S3 coherent: session §7 records C5 addition dated 2026-09-11 (`--step-delay`
  T108 `4f1538c` + modal gated `--log-total` T109 `08a6d0e`, "C1–C4 intactos");
  §4 C5 row + §7 C5 validation row exist.
- G1: `git diff 0916f7a..HEAD -- lib/battle_engine.rb lib/battle_service.rb db/
  server.rb` EMPTY; targeted
  `battle_view + style_responsive + battle_juice_presenter + battle_end_states`:
  **50 runs, 546 assertions, 0 failures** (this review). Note: session defines
  only G1 — there are no G2/G3 to verify.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Medium | views/battle.erb:37-38 + views/_fighter_panel.erb:4,10 | `side_delay` is max-per-side with no group stagger, while each line's `--log-delay` adds `(size-1-group_index)*0.25` on top — so an effect fires at/below its line's delay (effect→line, not the specified line→effect), and a fighter hit only in an early round flashes at the side's last-touch delay | Accept as the CSS-only approximation (persistent cards can't replay per-line) or scope true per-line sync to a future session — non-blocking |
| Low | public/style.css (`.fighter.is-attacking .shot`) | `animation-delay: var(--step-delay, 0.15s)` overrides the shorthand's `0.15s` intra-step offset whenever the var is set — projectile fires simultaneous with the line instead of 150ms after | `animation-delay: calc(var(--step-delay, 0s) + 0.15s)` in a future pass — one line, visual nuance only |
| Low | sessions/0086-battle-log-juice.md:51 | C5 S1 names don't match real tests (`test_effect_sync_step_delay` vs actual `test_juice_effects_sync_to_step_delay` + `test_battle_fighter_cards_carry_side_and_step_delay` + `test_result_gated_on_log_total_as_modal`); claimed e2e `effect sync + result modal` has no spec (`e2e/specs/` grep empty, same gap as C4 in round 3) | Align C5 row to real tests at validation; add the e2e spec or drop the claim — doc-only |
| Low | views/battle.erb:84 | Modal close is a `<label>` with no `tabindex`/button semantics and focus is never moved into `role="dialog"` — mouse/touch fine, keyboard a11y incomplete | Future a11y pass (`tabindex` + key handling or button pattern); acceptable under the CSS-only constraint for now |
| Info | views/battle.erb:43 + public/style.css (`.res-overlay`) | `--log-total` is the unbounded cumulative sum, so long battles gate the modal by the full wait (same shape as the C4 stagger note) | Keep (tiers approved); Skip + reduced-motion are the escapes; consider a cap in a future session |
| Info | lib/battle_log_presenter.rb:78-85 | `from_side` fallback `entry[:attacker].to_i` maps attacker-less entries to side 0 — no evidence it triggers (entries carry attacker/from_side), `side_delay` keys verified integers | Noted only; no action |

## Residual

Nenhum bloqueador. Ready for user validation (phase 3, S2 table per criterion).
Follow-ups (all non-blocking, future sessions): per-line effect granularity +
projectile `calc()` offset; C4/C5 S1 test-name + e2e alignment at validation;
modal keyboard a11y.
