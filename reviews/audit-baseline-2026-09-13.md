# Baseline audit — poke-htmx — 2026-09-13 (read-only)

Scope: `server.rb` (1486 lines), `lib/**/*.rb`, `views/**/*.erb`, `Gemfile`. No files modified; this is the single new artifact.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| High | `server.rb:1456` | Session fixation / account takeover: `session[:user_id] = params["as"] if params["as"]` lets anyone set any user's id via query/body param and read/mutate their team, wallet, battles | Delete the line; assign `SecureRandom.uuid` only when absent. If impersonation is needed for dev, gate behind `RACK_ENV == "development"` + explicit env flag, never in prod path |
| High | `views/pokemon_detail.erb:1,29`, `views/_fighter_panel.erb:18,28-29`, `views/pokemon.erb:12`, `views/pokemon_list_item.erb:11,27` | Stored/reflected XSS: plain ERB `<%= %>` does not escape and app never sets `:escape_html`; attacker-influenced strings (pokemon/move/item names, `@q`, `@notice`) render into HTML body and attributes (`value="..."`, `alt="..."`, `class="ptag--<%= type %>"`) | Set `set :erb, escape_html: true` globally and audit `style`/attr contexts; or wrap with `ERB::Util.h`. Add a test posting `pokeName` with `"><script>` asserting escaped output |
| High | `server.rb:1419-1420` | Committed fallback `session_secret` (hex literal in repo) used whenever `ENV["SESSION_SECRET"]` is unset — all default installs share it, signed cookies forgeable | Fail boot when secret missing in production (`raise` unless `ENV["SESSION_SECRET"]`); keep dev-only fallback guarded by env |
| High | `lib/team_repository.rb:297-299,291-294` | Array-literal injection/corruption: `array_literal` interpolates move names into `"{a,b}"` passed as `$3`; a name with `,}","` breaks `parse_moves` (`[1..-2].split(",")`) and corrupts/truncates stored moves | Bind a real PG array (`PG::Connection#escape` per element or `array_append`/parameterized text[]) and parse with a quote-aware parser; add test with `move"name},evil` |
| Medium | `lib/gateways/poke_api_http.rb:21-33` + `server.rb:510-511` | Search/pagination N+1 on external API: every keystroke calls `fetch_all_names` → full `?limit=100000` list, then linear `find`; no cache/debounce | Cache name list server-side with TTL (reuse `PersistentJsonStore`/memory cache), paginate/filter at source, debounce `hx-trigger` on input |
| Medium | `server.rb:583-594` vs `lib/team_repository.rb:173-200` | Budget TOCTOU: `add_team_member` checks `team_total_cost` then `team.add` in separate steps; two parallel POSTs both pass the check and overspend | Re-check budget inside the insert transaction (or serialize per-user with advisory lock); return current `budget_blocked_response` on conflict |
| Medium | `lib/battle_engine.rb:157-164,266-269` | No round cap + unguarded target strategy: `battle` loops `until finished?`; `target_for` indexes with unchecked strategy result (`nil`/OOB → `NoMethodError`, hung request under Puma) | Add `MAX_ROUNDS` guard returning draw/timeout + validate `target_index` (`nil`/range → fallback first alive); test nil-strategy |
| Medium | `lib/gateways/poke_api_http.rb:57-63` | `Timeout.timeout` around `Faraday.get` can kill threads mid-ensure and leak connections | Use `Faraday.new(request: { timeout:, open_timeout: })` instead of `Timeout.timeout` |
| Medium | `lib/connection_registry.rb:22-31,65-81` | Hand-rolled pool keyed by `Thread.object_id` (ids recyclable) with LRU eviction that can close a connection still in use by a concurrent Puma thread | Replace with `PG::Connection` pool (`connection_pool` gem) or per-thread `Thread.current[]` store with refcount; never evict checked-out handles |
| Medium | `server.rb:1378-1383` | 500 handler returns HTTP 200 for htmx (`status(htmx_request? ? 200 : 500)`) — hides failures from monitoring/alerts and caches | Always return 5xx status; signal htmx swaps via `HX-Retarget`/`HX-Reswap` headers instead |
| Low | `server.rb:11,123,626,728,745` | Unvalidated numeric params: `params[:offset].to_i` (negative → odd pages), `params[:new_slot].to_i`, `quantity.to_i` (0/negative reach services) | Clamp/validate at boundary (`offset >= 0`, `quantity >= 1`, slot in `1..6`); reject with 422 notice otherwise |
| Low | `views/layout.erb:7` | htmx CDN `<script>` without SRI `integrity` — supply-chain tamper executes in-page | Pin `integrity="sha384-…"` + `crossorigin="anonymous"` or self-host the asset |
| Low | `server.rb:35-1486` (structure) | 1.4k-line route file via `Server*Actions` mixins + scattered `rubocop:disable`; `oob_*` helpers concat raw HTML strings (`oob_nav_badge`, `oob_pokemon_list`) bypassing escaping | Extract per-resource route files/presenters; render OOB via partials, not string interpolation; shrink disables to per-line with justification |

## What was NOT re-verified this pass

Prior `reviews/` findings (e.g. auto-play toggle loss `review-2026-09-12T21:30`, heal/battle OOB scoping) were not re-tested — read-only baseline, no suite run. XSS verdict assumes stock Sinatra ERB (no global `escape_html` found); confirm with one render test before fix.
