# Review 0082 round 4 (S7 narrow, final) — 2026-09-11

Scope: T73 re-review of `a560bad` (overlay-fade filtered) on chain 2942ffb, 2552da8, 6bf414f, 08f6b24, e76cd28, 668fb12, a560bad. Verified HEAD `a560bad` + worktree `git diff` + targeted tests. No edits, no commits by reviewer.

## VEREDITO: Aprovado

Committed `a560bad` lands ONLY overlay-fade hunk + `overflow-y:auto` + reduced-motion guard + overlay test (2 files, +34/-1). Evolution block intact (HEAD `public/style.css:2048-2098`, 0 `evolution` lines in `668fb12..HEAD` diff). Modal-close + single-notice (T67 `668fb12`, `server.rb:733-780`) untouched by `a560bad`. Foreign hunks correctly left dirty (uncommitted). Targeted suites green (30 + 97 runs, 0 fail), `checar-sessao 0082` verde, G2 contido.

## Verificação ponto a ponto

- Modal closes on heal: `server.rb:733-737` success → `render_heal_success_notice:772-775` returns team fragment + `oob_close_center_modal:778-780` (empty `#center-modal` slot) + team-view + nav-badge; unchanged by `a560bad` (CSS+test only). OK.
- Single message: close-slot OOB carries no notice; `@notice` set once, rendered once via team fragment. OK (carried from round 3, untouched).
- Flash masked: `public/style.css` committed fade (`@keyframes overlay-fade` + `.overlay:target,.overlay.open { animation: overlay-fade 0.15s ease-out }` + `.overlay { overflow-y:auto }`) + test `test_overlay_open_fades_instead_of_flashing` (`test/style_responsive_test.rb:226`). Targeted 30 runs green. OK.
- Evolution intact: `.evolution-overlay/.evolution-modal-box/.evolution-row` present HEAD:2048ff, identical set vs `668fb12:2031ff` (line shift only from fade hunk); committed diff contains 0 `evolution` lines. OK.
- Commit hygiene: `a560bad` = 2 files only (`public/style.css`, `test/style_responsive_test.rb`); foreign hunks (`docs/draft-backlog.md`, `test/pokemon_routes_test.rb`, cassettes, `public/style.css` worktree filter-grid/roster-grid/home-1:1) left dirty, uncommitted. OK.
- G1: `style+modal` 30/286/0 + `team+mart` 97/691/0; full-suite 1099/0 base from round 2/3 still valid (committed delta is CSS+test only). G2: `git diff 2942ffb..HEAD -- lib/heal_cost_policy.rb lib/heal_service.rb lib/reward_rule.rb` empty. G3: `checar-sessao 0082` verde + this verdict.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Low | `test/style_responsive_test.rb:171-191` | Existing `test_juice_reduced_motion_disables` selector list does not assert `.overlay:target` / `.overlay.open`, so the new guard (`public/style.css:2106-2122`) has no regression assert in that test (only the fade test covers animation presence) | Follow-up outside 0082: add `.overlay:target` / `.overlay.open` to the reduce selector list, or a dedicated reduce-assert in `test_overlay_open_fades_instead_of_flashing` |
| Low | `server.rb:743,750` | `buy_from_mart`/`sell_from_mart` gates return bare `journey_gate_notice` without `oob_mart_modal` — same stale-modal pattern 08f6b24 fixed for heal (carried, unchanged, out of 0082 scope) | Follow-up outside 0082: mirror heal gate with `oob_mart_modal` + gate-via-modal test |
| Info | worktree (uncommitted) | Foreign hunks remain dirty: `public/style.css` filter-grid/roster-grid/home-1:1, `test/pokemon_routes_test.rb` plus-affordance, `docs/draft-backlog.md`, 2 cassettes + untracked `ARCHITECTURE.md`/`PROJECT.md`/`TODO.md`/cassettes/`reviews/` | Keep out of 0082; land in own sessions/commits; do not block validation |

## G1–G3

- G1 sem regressão: targeted 127 runs / 0 fail; full-suite base 1099/0 unaffected (CSS+test delta); lint not re-run (no Ruby touched).
- G2 escopo contido: OK (lib economy diff empty; commit = modal flash-mask only).
- G3 docs + revisão: `checar-sessao 0082` OK; este veredito `Aprovado` libera para validação (fase 3, usuário).
