# Review — 0086 Passos 29-31 (cor do tipo no projétil, copy C3, #play-btn OOB — S7)

Escopo revisado (como instruído): `9254ba4` (Passo 29), `287743f` (Passo 30), `6fb9322` (Passo 31). Nenhum arquivo editado.

**Base e discrepância de escopo (Info):** `git log --oneline a93bdb3..HEAD` retorna **9 commits**, não 3: `8542a33` (Passo 21), `f77493d` (S3 doc), `0485249` (Passo 22), `5259ca8` (Passo 23), `03abff4` (Passos 24-25), `5cf6c4a` (Passos 26-27), `9254ba4`, `287743f`, `6fb9322`. O último review em `reviews/` é `review-2026-09-12T21-45-00.md` (Aprovado, escopo `a93bdb3` = Passo 20, 2026-09-12 21:33). **Passos 21-27 não têm arquivo de review** — esta revisão cobre apenas os três commits nomeados; a lacuna deve ser resolvida pelo orquestrador (reviews em rodadas não persistidas ou revisão pendente). Os achados abaixo sobre Passo 29 interagem justamente com CSS do Passo 26 (também não revisado).

**Evidência executada localmente (2026-09-14):**
- `./scripts/test` → **1171 runs, 6107 assertions, 0 failures, 0 errors, 0 skips** (confirma o reportado).
- `./scripts/test test/battle_strike_routes_test.rb test/style_responsive_test.rb test/battle_view_test.rb test/battle_strategy_routes_test.rb` → 77 runs, 0 failures.
- `./scripts/lint` → 136 files, **no offenses**.
- `git diff --stat a93bdb3..HEAD -- lib/battle_engine.rb lib/battle_service.rb db/` → **vazio** (G1).
- `git diff --name-only` dos três commits: só os arquivos declarados; sem cassettes, sem docs untracked, sem `docs/draft-backlog.md` (o churn de cassettes/`pokemon_routes_test.rb` continua dirty fora dos commits — preexistente).

## Checagem ponto a ponto

**1. D4 (`data-move-type` nos dois renders) — conforme.**
Diretivo (`sessions/0086-battle-log-juice.md:89`): «**D4:** … Expõe em `outcome_fields` e renderiza `data-move-type` nos DOIS caminhos (`views/battle.erb:195-199` inicial e `views/_strike_log_entry.erb` OOB).» Os dois caminhos citados são os **renders da entrada do log** (não o carrier): `lib/battle_log_presenter.rb:64` expõe `move_type`; `views/battle.erb:199-200` e `views/_strike_log_entry.erb:8-9` emitem `data-move-type`. A omissão em `views/battle.erb:45` é do **carrier `#jx-gates`** em repouso (guarda `defined?(move_type)` em `views/_jx_gates.erb:1`), coberta por teste novo (`test/battle_view_test.rb:347-356`) — não conflita com D4. Ressalva: ver achado 1 (o mapping CSS correspondente à entrada morreu).

**2. Colocação do CSS — conforme.** Regras novas em `public/style.css:1962-1979`, dentro do bloco `0086 C2` (início L1859, `fim` L2016), antes de `/* === Open Design System (0072): fim === */` (L2018). O bloco final `@media (prefers-reduced-motion: reduce)` começa em L2448 e é a **última regra** do arquivo (2498 linhas, fecha em `}` sem nada depois). Nada de juice após o reduce.

**3. Gotcha "var escrita e nunca lida" — sem novas vars mortas; porém uma sombra.** `--fx-color` é lida via `var()` em `.shot` (L1011) e em `.log__entry .fx` (L1990); `--fx-travel` em L1587/1598; `--fx-shake` em L1000/2432. Nenhum `--jx-*` novo introduzido pelos três commits (o gotcha preexistente de `--jx-*` permanece, fora do escopo). **Contudo**, a escrita `--fx-color` do Passo 29 em `.arena` é anulada dentro do log pela declaração remanescente `.log__entry { --fx-color: var(--t-normal) }` (L939) — ver achado 1.

**4. `server.rb:1129-1135` — dentro do escopo, sem violar G1.** É passagem de local para partial existente (`strike_gates_oob` → `erb :_jx_gates`), sem rota/verbo/CSRF novos, sem tocar engine/service/db (`git diff` vazio). O partial tem exatamente 2 chamadores: `server.rb:1133` (com `move_type`) e `views/battle.erb:45` (sem) — a guarda `defined?(move_type) && move_type` mantém o render full intacto, provado por `test/battle_view_test.rb:347-356`. Impacto em outros chamadores: nenhum.

**5. Copy C3 — conforme e não enfraquecida.** `BattleLogPresenter#format_item` (`lib/battle_log_presenter.rb:106-113`) monta `"#{attacker_name} usou 1 #{display}, +#{healed} HP — restam #{remaining}"` (+ `(última unidade)` no zero) só com dados já existentes: `entry[:item]/[:healed]/[:attacker_name]` + replay de estoque a partir de `engine.items` (`remaining_map`, L117-146; sem tocar valores). `287743f` altera **exatamente** as 2 linhas de asserção autorizadas (`test/battle_strategy_routes_test.rb:36-37`, de `"usou Pocao"` para `"usou 1 Pocao"` + `"restam 0"`) e registra o S3 datado (`sessions/0086-battle-log-juice.md:151`, 2026-09-14, opção (a)); o restante do teste (não-debitar, item limpo) permanece. Nada além do autorizado foi removido — a asserção ficou mais forte, não mais fraca. (A forma literal do critério `"usou 1 X (restam N)"` é parafraseada como vírgula + `— restam N`; o S3 autorizado nomeia `"usou 1 Pocao"` + `"restam 0"`, então alinhado.)

**6. Passo 31 — OOB `outerHTML`+`disabled` correto e mínimo; justificativa do comentário é falsa.** `views/_strike_result.erb:4` re-troca `#play-btn` desabilitado e sem `hx-post`; htmx casa OOB por `id`, e o `<button>` OOB não vai para o alvo principal (`#battle-log`, beforeend). Caminho em andamento intacto: `modal = result[:finished] ? ... : ""` (`server.rb:1094`) e `test_strike_keeps_play_button_while_running` (L95-103). O teste novo (L83-93) é realmente vermelho sem o fix: em andamento não há `#play-btn` algum no corpo (L101-102), e no fim o único emissor do botão é `_strike_result.erb:4` — sem ele `refute_nil play` falha. Sobre o `delete`: **o comentário está errado** — htmx documenta `delete` como valor de `hx-swap` ("deletes the target element regardless of the response") e `hx-swap-oob="delete"` remove o elemento por `id` (htmx.org/attributes/hx-swap e /hx-swap-oob; padrão de uso difundido). Não há bundle vendorizado para conferir: htmx vem por CDN (`views/layout.erb:7`). Achado 3.

**7. Higiene — limpa nos três commits; falta sync do doc de sessão.** Sem scope creep em engine/XP/gate/CSRF/JS: `5cf6c4a`-`6fb9322` tocam só view/CSS/partials/testes + o doc da sessão (o commit de doc é o S3 exigido). Sem cassettes ou docs untracked staged. Doc de sessão: §6 termina no Passo 28, §8/Status "Pendente", sem registro dos Passos 29-31 (achado 2/4).

## Achados (worst-first)

| Severidade | file:line | problema | recomendação |
|---|---|---|---|
| **Major** | `public/style.css:939` + `1962-1979` (consumo em `1990`) | O Passo 29 moveu o mapping de tipo para `.arena:has(> #jx-gates[data-move-type=…])` (L1962-1979), mas `.log__entry` continua declarando `--fx-color: var(--t-normal)` (L939). Como `.fx` (L1990) é filho de `.log__entry`, o valor do arena é **sombreado** e todo `.log__entry .fx` resolve para `--t-normal` → o flash por linha **perdeu a cor por tipo** que o Passo 26 (não revisado) entregava; `data-move-type` em `views/battle.erb:200` / `views/_strike_log_entry.erb:9` virou dado morto para estilo. O teste `test/style_responsive_test.rb:333` segue verde porque o regex `[data-move-type="…"]` também casa o seletor do carrier — nenhum teste pega a perda. | Reescrever a propagação para vencer a declaração do log (ex.: soltar/escopar o `--fx-color` de `.log__entry` L939 e cobrir `.log__entry .fx` pelo gate do carrier, ou mover o token para um wrapper que não redeclare), e assertar que uma entrada tipada resolve `--fx-color` para `--t-<tipo>` — não só que o carrier mapeia. Fix de 1-3 linhas. |
| **Minor** | `sessions/0086-battle-log-juice.md:151,153` | Mudança de comportamento do C11 (mapeamento deixa `.log__entry[data-move-type]`, L153) e os Passos 29-31 não foram registrados: §6 para no Passo 28, sem linhas/nota S3 para 29/30/31; só a troca de asserção do C3 tem nota datada. S3 exige registrar alteração de critério/comportamento com data + reaprovação. | Registrar Passos 29-31 e a migração do C11 (§6 + nota S3 datada) — ou restaurar o mapping por entrada (achado 1). |
| **Minor** | `views/_strike_result.erb:3` | Comentário afirma «htmx 2.0.3 nao tem swap "delete"» — falso: `delete` é valor válido de `hx-swap` e `hx-swap-oob="delete"` remove o elemento por `id` (docs htmx). Além disso não existe bundle vendorizado para sustentar a afirmação (htmx por CDN, `views/layout.erb:7`). | Corrigir o comentário com o motivo real (manter controle visível/desabilitado atrás do modal) ou usar `hx-swap-oob="delete"`; ajustar o gotcha correspondente. |
| **Minor** | `sessions/0086-battle-log-juice.md:8,116-135` | Status/plano do doc seguem "Pendente"/Passo 28 e a linha de evidência G1 (§7) está vazia — item já carregado do round 11 ("sync session doc before validation sign-off"). | Sincronizar antes do sign-off do usuário (S1/S2). |
| **Info** | — | Lacuna de escopo: `a93bdb3..HEAD` = 9 commits; Passos 21-27 (`8542a33`…`5cf6c4a`) sem review em `reviews/` desde 2026-09-12T21-45-00. | Orquestrador confirmar se essas rodadas foram revisadas em arquivo não persistido; se não, agendar review. |

## Checado e limpo (sem achado)

- G1: `lib/battle_engine.rb`, `lib/battle_service.rb`, `db/` intactos (`a93bdb3..HEAD` vazio); os toques em `lib/battle_log_presenter.rb` ficaram na rodada anterior (Passo 23), dentro do aprovado (D4).
- Passo 30: sem alteração de regra/economia; estoque/`remaining` derivado do log + `engine.items`.
- Passo 31: OOB por `id` correto; estado em andamento intocado; teste novo é red sem o fix.
- Carrier `#jx-gates`: 2 chamadores, guarda `defined?` cobre o render full; item omite `data-move-type` por contrato (testes `test_strike_gates_carrier_omits_move_type_on_item_entry`, `test_battle_arena_gates_carrier_omits_move_type_at_rest`).
- Reduced-motion continua última regra do arquivo (L2448+, fim L2498); bloco ODS `0086` delimitado e fechado antes do `fim` do ODS 0072.
- Sem scope creep/asserção enfraquecida/cassette churn nos três commits; suíte total e lint verdes localmente.

## VEREDITO: **Requer ajuste**

1 major + 3 minor. O Passo 29 introduz uma regressão visual silenciosa (cor por tipo do flash do log sombreada por `.log__entry { --fx-color: var(--t-normal) }`) que nenhum teste pega, no próprio commit cujo objetivo era a cor do tipo; e o C11 migrou de elemento sem registro S3. Correções exigidas do implementador: (a) fazer `--fx-color` do carrier chegar ao `.log__entry .fx` (ou registrar formalmente a remoção da cor da entrada) + assert do token por tipo na entrada; (b) registrar Passos 29-31 e a mudança do C11 no doc da sessão; (c) corrigir o comentário do `delete` no `_strike_result.erb`. O restante (D4, passagem de `move_type`, copy C3, OOB do `#play-btn`, colocação CSS) está conforme. Nenhum blocker.
