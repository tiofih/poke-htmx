# Review 0083 — verificação final (commit `91103f7` + estado de `test/cassettes/`)

Data: 2026-09-16 · Escopo: mini-verificação escopada (não é review S7 de código novo)
Método: `git show/--stat/--numstat`, `git diff`, `git status --short`, `git check-ignore`, `rg -n`. Nada foi editado, revertido ou commitado.

## VEREDITO: Aprovado (0 high / 2 medium / 2 low)

O commit `91103f7` faz **exatamente** o que foi pedido.

## 1. Commit `91103f7` — conferido

`git show --stat` = 3 arquivos, +15/-9: `public/style.css` (2/2), `sessions/0083-ui-polish.md` (8/5), `test/layout_test.rb` (5/2).

- **CSS**: o diff está inteiro dentro do comentário do bloco `UI polish (0083)`. Remove só `.btn--gated:focus-visible { outline: ... }`; o comentário passa a explicar a decisão (opção i, 2026-09-16). `.btn--gated { background: var(--fg-soft); color: var(--muted); pointer-events: none; }` e os tokens `--fg-soft`/`--muted` **permanecem**. Sem reformatação (2 linhas trocadas por 2).
- **Teste**: `assert_match(/\.btn--gated:focus-visible\s*\{[^}]*outline/)` → `refute_match(/\.btn--gated:focus-visible/)` + comentário explicando a opção (i) (`test/layout_test.rb:142-152`).
- **Sessão**: §8 converte o achado de "aberto/pendente de decisão do M1" em "RESOLVIDO" com data e justificativa — correto.
- **`tabindex` não foi tocado**: `git show 91103f7 -- …. | rg tabindex` = 0 matches; nenhum arquivo em `views/` no commit. Nada em `lib/`, `server.rb`, `db/`, `config/`, `Gemfile*` (G2 vazio confirmado).
- `rg -n 'focus-visible' public/style.css views/ test/` → **2 hits, ambos em `test/layout_test.rb:147-148`** (comentário + `refute_match`). CSS e views limpos.

## 2. `test/cassettes/` no worktree — estado real

Tracked modificados: **2** (não "2 dos 4 do `b578cc9`", ver achado L2), ambos **só inserções, 0 deleções**:
- `ServerHealJourneyGateTest/test_heal_released_after_journey_started.yml`: +222 linhas (origem: `d28fb28`).
- `ServerTeamRemoveHtmxTest/test_htmx_delete_team_removes_and_swaps_both_fragments.yml`: +21860 linhas — arquivo introduzido com 3069 linhas no `b578cc9`, agora ~24929 (**~7×**). Append puro (`numstat` 21860/0), logo nenhuma interação antiga foi sobrescrita.

Untracked (não ignorados — `git check-ignore` = NOT-IGNORED):
`BattleViewTest/` (23 KB + 234 KB), `ServerMartTest/test_mart_fragment_shows_affordable_quantity.yml` (623 KB, mtime **29/08** — resíduo antigo), `TeamHealRoutesTest/test_heal_blocked_when_insufficient_balance.yml` (**5,4 MB**), `ServerHealJourneyGateTest/test_heal_blocked_before_journey.yml`.
Total em disco: **358 MB**; tracked: 36 arquivos.

## Achados (pior primeiro)

| Severidade | Local | Problema | Recomendação |
|---|---|---|---|
| medium | `test/layout_test.rb:148` | `refute_match` roda sobre o **substring** extraído por `ui_polish_block` — reinserir `.btn--gated:focus-visible` em qualquer outro ponto de `public/style.css` (ex.: EOF) passa o teste. Limitação confirmada empiricamente pelo implementador. | Somar ao teste um `refute_match(/\.btn--gated:focus-visible/, File.read("public/style.css"))` (arquivo inteiro), mantendo o assert de bloco para o comentário. |
| medium | `test/cassettes/` (worktree) | 2 tracked modificados +22 082 linhas e 5 cassettes novos untracked (~6,3 MB, um de 5,4 MB, um de 29/08) deixam a árvore suja e fora do contrato offline versionado; a suíte já passa (1186 runs/0F) sem eles no índice. | **Não commitar**: `git checkout -- test/cassettes/` nos 2 tracked e ignorar os untracked (`.gitignore` em `test/cassettes/<nova>/` se a re-gravação é esperada). Só commitar se algum teste novo do 0083 **depender** do cassette para rodar offline — aí é 1 commit pequeno e isolado, não o `5,4 MB` de `TeamHealRoutesTest`. |
| low | handoff da 0083 | "re-gravou 2 dos 4 cassettes versionados em `b578cc9`" é impreciso: só `test_htmx_delete_team...` veio do `b578cc9`; `test_heal_released_after_journey_started.yml` veio do `d28fb28`. | Corrigir a redação ao registrar a verificação; sem impacto em código. |
| low | `test/cassettes/ServerTeamRemoveHtmxTest/...` | Crescimento de 7× (3069 → ~24929 linhas) por acúmulo de episódios do `record: :new_episodes`. Append puro, mas degrada a legibilidade e o tamanho do repo. | Se for manter algo, prefira re-gravar limpo (`:once`/apagar e regravar) em vez de acumular episódios; caso contrário reverter. |

## Info (sem ação)

- A nota do §8 (`sessions/0083-ui-polish.md:139-146`) corresponde ao commit: decisão registrada como opção (i) do usuário em 2026-09-16, com a explicação de que (ii) foi descartada e o `tabindex="-1"` permanece.
- A explicação de acessibilidade do CTA segue garantida por hint visível + `aria-describedby`, não pela regra de foco removida — remoção não cria lacuna de a11y.
