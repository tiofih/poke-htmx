# Review — Sessão 0086 round 2 (S7) — 2026-09-11

Scope: chain 96a4c8b, a50d8dd, c118559, 0916f7a. Narrow re-review after T93 Requer (T95 defeat data-round + dead reduce, T96 e2e, T97 S1 real names, T98 suite, T99 Passo 4 commit). No edits, no commits.

## VEREDITO: Aprovado

C1/C2/C3 + e2e present, defeat selectors fixed, dead reduce gone, S1 real names, engine/db untouched, targeted suite green, 0086 files lint-clean.

## Evidence

- C1: `views/battle.erb:159` header `log-round-head` + `data-round` + `id="round-N"`; entries carry per-entry `data-round`; `test/battle_view_test.rb:91-96` asserts header data-round + newest-first (regex narrowed to header-only in Passo 4, no longer inflated by entry attrs).
- C2: juice block delimited `0086`, reduced-motion `@media` is ULTIMA regra do arquivo with `animation: none !important` + `transition: none !important`, covering new (`.log__entry/.log-round-head/.log__entry--defeat`) + legacy selectors; `test/style_responsive_test.rb:206-212` checks block; e2e asserts `getComputedStyle().animationName === 'none'` + tail-of-file contains selectors.
- C3: defeat line `views/battle.erb:178` `log__entry--defeat` WITHOUT `data-round`; `test/battle_view_test.rb:165-166` refute guards numeric data-round on defeat; copy `Derrota — seu time foi derrotado. Cure no Poke Center.` distinguishes participation vs victory; stock chips (`chip--stock` / `chip--empty`) covered in presenter + view tests.
- Dead reduce: `grep reduce|inject lib/battle_log_presenter.rb lib/battle_juice_presenter.rb` → zero hits. Gone.
- S1: session file C3 row now points to real tests (`test_item_entries_with_stock_expose_remaining_and_last_unit`, `test_item_entry_without_stock_keeps_legacy_copy`) + e2e `consumption and reward copy`. No invented names.
- e2e: `e2e/specs/battle-log.spec.ts` (121 lines, committed in Passo 4) has 3 tests (C1 headers/chips, C2 reduced-motion, C3 consumption/reward); harness builds 6 via real catalog with `nth(i)` (avoids `.first()` re-add trap); defeat assertion conditional on `winner-badge` (robust to RNG outcome).
- Engine/db: `git diff 96a4c8b~1..HEAD -- lib/battle_engine.rb lib/battle_service.rb db/` → empty. Scope respected.
- Tests: targeted `battle_view + battle_log_presenter + battle_juice_presenter + style_responsive` → 51 runs, 341 assertions, 0 failures (this review). Full-suite 1115-run claim (T98) taken from implementer, not re-run here.
- Lint: `./scripts/lint` → 26 offenses, NONE in 0086 files (offenses live in `scripts/sweep-balance.rb`, `server.rb`, `home_view`, `team_routes`, and uncommitted `pokemon_routes_test.rb` plus-affordance). 0086 chain lint-clean.

## Residual findings (non-blocking)

Severity | file:line | problem | recommendation
Medium | `git status` worktree | Uncommitted unrelated changes dirty the tree (`public/style.css` 0077 home 1:1 block, `test/pokemon_routes_test.rb` plus-affordance, `docs/draft-backlog.md` T20/opponent-6v6 lines, 2 regenerated cassettes + 4 untracked cassette dirs). Risk: accidental co-commit with 0086 validation or confusion about what belongs to 0086. | Keep out of 0086 validation commit; commit/verify separately per owning session before 0086 closes.
Low | lint (global) | 26 offenses repo-wide (pre-existing `sweep-balance.rb`, `server.rb:76` AbcSize, `home_view`, `team_routes:445`, uncommitted `pokemon_routes_test` long lines). `lint 0` objective not met globally, though 0086 files are clean. | Track as tech-debt outside 0086; do not inflate 0086 scope to fix.
Low | e2e (not executed here) | e2e spec present and selectors sound on read, but this review ran only Minitest targeted files, not Playwright. Runtime proof (esp. C2 `getComputedStyle` + C3 RNG-conditional defeat branch) unverified in this pass. | User validation (phase 3) should run `e2e/specs/battle-log.spec.ts` once; record per-criterion S2 table then.
Info | `sessions/0086-battle-log-juice.md:47-55,81-86` | Criteria/G1 rows still `pendente`, phase 2 still `Pendente`. Correct per stop-rule (validation is user's), noting only so phase 3 fills the S2 table per criterion. | No action in phase 2; fill at validation.
