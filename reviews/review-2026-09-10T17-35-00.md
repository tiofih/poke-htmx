# Review — Sessão 0080 convergência visual (S7 round 1)

Veredito: **Aprovado**. Escopo 0080 (C1–C5 + C1b S3) confere contra os testes; nenhum achado bloqueante.

## Verificação executada

- `test/convergence_0080_test.rb` (5 métodos C1–C5) + `test/layout_test.rb` (6 métodos, incl. `test_full_page_starts_with_doctype` C1b): **11 runs, 83 assertions, 0 failures** — re-rodado nesta revisão via `./scripts/test`.
- Diffs dos commits 0080 (`3fb5532`, `a37fee0`, `68f94c2`, `26ada4f`, `587780f`, `39c76b5`) conferidos; passo 4 (`battle.erb`) balanceado (+1 open/+1 close).
- G2: nenhum commit 0080 toca `db/`, `Gemfile*` ou `test/open_design_contract_test.rb` (`git show 587780f --stat` = sessão + `battle_routes_test.rb` apenas).

## Achados

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Low | `views/team.erb:21` | classe `roster-grid` sem regra no CSS commitado (HEAD não define `.roster-grid`; só `0077` não-commitado a define) — hook morto no estado 0080 | 0081/0077 assumem o hook, ou remover a classe se 0077 não a usar; sem efeito funcional hoje (`.roster` aplica o grid) |
| Low | `views/battle.erb:86-143` | `.result-card` (aberta :86) sem `</div>` próprio — `:142` fecha `.result`, `:143` fecha `section`; desbalanceamento `div` +1 **pré-existe** ao 0080 (parent do passo 4: 22 open/21 close) | fora do escopo 0080 (RNF-04); registrar para a 0078 battle-1a1 fechar a tag |
| — | observação | working tree sujo com trabalho não-commitado da 0077 (`public/style.css` 63+/57-, `test/pokemon_routes_test.rb`, cassettes): o G1 1085/5291 foi medido em árvore limpa no passo 5 e não é reproduzível sem `stash` | não é defeito do 0080; validação M1/G1 final deve rodar em árvore limpa |

## TDD greens verificados

- C1 body global (`test_body_has_global_base_rule`) + C1b doctype (`test_full_page_starts_with_doctype`, fix S3 `layout.erb:1-2`) — verde.
- C2 topnav CTA `.btn` + Voltar ao time só em `/battle` — verde.
- C3 roster grid + nível + Gerenciar + `new_slot` preservados — verde.
- C4 pcard re-marcado (sprite-tile/pcard-name/pcard-meta/`·`/pcard-add, contrato POST intacto) — verde.
- C5 podium meio em `.card` — verde.
- M1 segue `manual`/pendente — fase 3 do usuário.
