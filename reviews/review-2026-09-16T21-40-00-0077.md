# Revisao escopada — sessao 0077 (`open-design-home-1a1`), 1a revisao

**Escopo:** os 5 commits da 0077 (`ea7192a`, `a00db99`, `2671204`, `d0832bf`, `d922add`) + o
bloco CSS `Home 1:1 (0077)` (carregado em `4f1538c`). Leitura minima (`git show --stat`,
`rg -n`, `sed -n`) + teste escopado. Nenhum arquivo de producao editado; nenhum commit feito.
Nao existia artefato de review da 0077 antes deste.

## VEREDITO: **Requer ajuste**

blocker 1 / alta 2 / media 3 / baixa 3 / info 2
(ajuste pedido e **somente documental**: proveniencia do C4 e do C1 nos SHAs da 0077;
o codigo no HEAD esta verde e a duplicacao ja foi removida por `41adcaf`)

## Evidencia por item

| # | Item | Resultado |
| --- | --- | --- |
| 1 | C1-C4 provados pelo teste citado? | **parcial** — `./scripts/test test/home_residue_test.rb test/modal_routes_test.rb test/evolution_routes_test.rb test/home_view_test.rb test/design_system_test.rb` → **55 runs / 871 asserts / 0 failures**. C1/C2/C3 com prova real no HEAD; **C4 nao e provado por nenhum commit da 0077** (ver blocker) |
| 2 | Trap dos asserts vacuosos | achados — nenhum assert ancorado no documento inteiro (todas as rotas de modal devolvem `layout: false`: `server.rb:550/567/572`); porem `refute_includes "onclick"` e sempre-verdadeiro e fica apos 10+ asserts (abortam o metodo), e o regex de `price` casa markup legado — 2 baixas |
| 3 | Trap das variantes/duplicacao do prototipo | **achado alto real** — no tip da 0077 a home renderiza `_center`/`_mart` inline (titulos `Poke Center`/`Poke Mart`) **e** nos overlays 0076 com os mesmos titulos → fragmento 2x, ids duplicados e grupo de radios `name="mart-tab"` compartilhado. Ja removido por `41adcaf` (2026-09-10, ancestral de `4f1538c`) |
| 4 | CSS: delimitadores, aditividade, orfas, vazamento | bloco **dentro** de `0072 inicio`…`0072 fim` (`public/style.css:1816-1868` vs `:6`/`:2053`), insercao pura (`@@ -1724,6 +1739,61 @@`, sem reformatacao); **1 regra orfa** (`.mart .muted-row`); nenhuma redefinicao de classe preexistente (fora do bloco, os nomes da 0077 so aparecem num comentario em `:1339`) → sem vazamento por redefinicao; **1 consumidor fora do escopo** (`.tag-row` → `views/team.erb:38`) |
| 5 | G2 (backend read-only) | ok no literal: nenhum commit toca `lib/`, `db/`, `Gemfile*`. `2671204` toca `server.rb` (+12: `member_levels_map`/`member_level_for` + `@member_levels` no `expose_manage_data`, `server.rb:52-64`) — cabia no Passo 3 ("thin nos renders"). `a00db99` toca `views/team.erb` (+2) — **nao** listado no §3 |
| 6 | Consistencia por commit | **RED em `ea7192a` e `d922add`**: o teste C4 criado pelo proprio `ea7192a` exige o bloco CSS no arquivo, e `git show ea7192a:public/style.css \| grep -c heal-list` = **0** (idem `d922add`) — a serie nao e bisect-clean |
| 7 | Docs | ok — `./scripts/check_docs` → `ok> docs consistentes` (exit 0); `./scripts/checar-sessao 0077` → `ok> sessao 0077 estruturalmente consistente` (exit 0) |

Nao re-executados por orcamento: suite completa e `./scripts/lint` (numeros do implementador).

Nota de fidelidade ao prototipo (item 3): `.tab.active` com JS (`home-team.html:172/606`) foi
adaptado corretamente para CSS-only (`#mart-tab-*:checked ~ .tab-*`, `style.css:1831-1834`);
`.mv-row .mv-pp` do prototipo (`:199`) nao foi portado e **nao** virou regra orfa; `.tabs`/`.tab`
nao existem em nenhuma outra tela.

## Achados

| Severidade | file:line | Problema | Recomendacao |
| --- | --- | --- | --- |
| **blocker** | test/design_system_test.rb:202-225 + public/style.css:1816 | C4 nao tem prova em nenhum commit da 0077: o teste criado em `ea7192a` exige `\.heal-list\b` dentro do bloco 0072, mas o bloco entrou no git so em `4f1538c` (2026-09-11, mensagem da 0086) — `grep -c heal-list` = 0 em `ea7192a` e em `d922add`. O "green" do Passo 1 existia apenas no working tree sujo, entao **todos os SHAs da 0077 estao RED** numa copia limpa (C4 e G1 nao fecham por eles) | registrar a proveniencia real no §4/§9 e na memoria: C4 verde pelo artefato de `4f1538c`, nao por commit da 0077; nao reivindicar C4/G1 como fechados pela serie |
| alta | views/team.erb:23-27 (estado de `d922add`) vs views/_center_modal.erb:4,11 e views/_mart_modal.erb:4,8 | duplicacao visivel: a 0077 passa a renderizar `_center`/`_mart` **inline** no card do time (`<h2>Poke Center</h2>`, `<h2>Poke Mart</h2>`) enquanto os overlays 0076 ja renderizam os mesmos fragmentos com os mesmos titulos → heal list e mart 2x na pagina, **ids duplicados** (`mart-tab-buy/sell`, `tab-buy/sell`) e o mesmo grupo `name="mart-tab"` compartilhado (clicar a aba de uma copia desmarca a outra). O C1 afirmava "overlay/rotas 0076 intactos" — nao estavam | ja removido por `41adcaf` (2026-09-10), HEAD limpo — nenhuma acao de codigo; corrigir o registro do criterio C1 |
| alta | views/team.erb (em `a00db99`) | `views/team.erb` foi alterado sem estar no escopo declarado (§3 lista so `_center`/`_mart`, `team_manage`/`_evolution_modal`, `pokemon_list_item`/`pokemon_detail`) — foi a origem exata do achado acima | absorvido por `41adcaf`; registrar a alteracao de escopo no livro da sessao |
| media | public/style.css:1839 | regra orfa: `.mart .muted-row` nao tem produtor — a 0077 nunca emite `muted-row` (o prototipo o usava no `<li>` de estoque vazio, `open-design/home-team.html:586`) | remover a regra, ou emitir `<li class="muted-row">` no estado vazio do `_mart.erb` |
| media | public/style.css:1867 + views/team.erb:38 | classe compartilhada fora do escopo: `.tag-row` do bloco 0077 estiliza o roster (`views/team.erb:38`, tipos por membro, vindo de `41adcaf`) — mexer no bloco 0077 muda o card do time, sem teste que cubra | escopar (`.member .tag-row`) ou declarar a classe como compartilhada no comentario do bloco |
| media | server.rb:52-64 | `@member_levels` foi posto no `expose_manage_data` compartilhado → toda render de center/mart/manage faz um `progression.get` por membro mesmo quando ninguem le `@member_levels`; o `rescue StandardError → 1` degrada silenciosamente para "Nivel 1" | calcular sob demanda (memo por request) ou restringir ao caminho do manage |
| baixa | test/home_residue_test.rb:47 | assert fraco: `class="[^"]*\bprice\b[^"]*"` casa o markup legado `class="num price"` (anterior a 0077) → nao prova o port | ancorar em `num price`/valor, ou trocar por assert de conteudo |
| baixa | test/home_residue_test.rb:34,53,82,100,124,160 | `refute_includes ..., "onclick"` e sempre-verdadeiro (esses fragmentos nunca tiveram onclick) e fica **apos** 10+ `assert_*`: no Minitest o primeiro assert que falha aborta o metodo, entao o refute nao roda justamente quando importa | mover o `refute` para o inicio do metodo (ou aceita-lo como guarda permanente, nao como evidencia da 0077) |
| baixa | sessions/0077-open-design-home-1a1.md (§4, linha do C3) | S1 incompleto: a linha do C3 cita so `test_catalog_detail`, mas a metade do list-item (`pcard-meta`/`pcard-name`) e provada por `test_catalog_cards` (test/home_residue_test.rb:103-125), nao citado | citar os dois testes na linha do C3 |
| info | views/_center.erb:1-12 / test/home_residue_test.rb:19-54 | o C1 so e provado na rota de modal (`/team/center`); o caminho inline (nao-modal) nao tem teste — foi o ponto cego que deixou passar o achado de duplicacao | nenhuma acao no HEAD (caminho inline ja nao existe); vale como licao no §9 |
| info | public/style.css:1816-1868 (`4f1538c`) | confirmado: o bloco 0077 foi carregado em `4f1538c` (2026-09-11, msg da 0086), 2 dias depois da 0077 — no mesmo commit houve 4 linhas deletadas alheias (`filter-grid` `grid-template-columns`/`gap` + 2 comentarios de juice da 0086), mas o bloco 0077 em si e insercao pura | nenhuma acao — fora do escopo da 0077 |
