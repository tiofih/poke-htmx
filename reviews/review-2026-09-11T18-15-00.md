# Review 0086 round 7 (S7 narrow: 6b00a48 per-aspect modules + b151c2f e2e chronological) — 2026-09-11

Scope: `9ffeb8b..HEAD` (Passos 10-11) + worktree `sessions/0086` S3 note; C1–C6 + S3 coherence, per-aspect independence, timed path, skip + reduced-motion, G1. No edits, no commits.

## VEREDITO: Aprovado

- C6 coherent (CSS-only, `6b00a48`): each aspect gated by its own `data-jx-*` (`views/battle.erb:44` ships `log/fx/chip/modal=on`, `hit/dmg/ko/shot/hp/shake=off`); one block per toggle in `style.css` (log 944, hp 951, hit 958, dmg 963, ko 975, shake 989, shot 1008, modal 1897, fx 1919/1929/1934, chip 1940). Aggregates default OFF (`li.fighter{--jx-hit/dmg/ko/shot/hp:0}`, `.arena{--jx-shake:0}`), timed per-line/modal ON. Proven by `test_battle_arena_carries_jx_toggles_per_aspect` (`test/battle_view_test.rb`) + `test_juice_aspects_modular_with_off_defaults` (`test/style_responsive_test.rb`).
- Timed path intact: `log→--log-delay`, `fx/fx--ko/fx--heal→--log-delay`, `chip--dmg→--log-delay`, `modal→--log-total` (capped `min(acc,3s)`, full kept in `--log-total-full`); updated `test_juice_effects_sync_per_line` asserts each gated delay + `test_result_gated_on_log_total_as_modal` asserts modal gate under `data-jx-modal`.
- Round-5 blocker resolved: `b151c2f` flips e2e to `round headers chronological...` + ascending assert (`e2e/specs/battle-log.spec.ts:35,54`) — matches shipped chronological DOM.
- Skip + reduced-motion order correct: skip kills (`style.css:1858` log, `1866-1874` aggregates, `1908` modal, `1945-1949` fx/chip) sit after their gated animations and before the final reduce net (`2336`, last rule); both kills are `!important` vs non-`!important` gated animations so they win regardless of attribute-selector specificity. Reduce zeroes `--step-delay/--log-total` + `animation:none` + `--jx-*:0`. Proven by `test_skip_and_reduce_cover_step_delay` + `test_result_modal_reduced_motion_last` (green this review).
- C1–C5 intact: delta `9ffeb8b..HEAD` touches only arena toggles + gating prefixes + tests + e2e rename; headers/`data-round`/chips/consumption/pacing math/modal cap untouched.
- S3 coherent: session §7 records C6 addition dated 2026-09-11 (T121 map + T122 `6b00a48`, aggregates OFF, log/fx/chip/modal timed, "C1–C5 intactos"); §4 C6 row + §7 C6 validation row exist (worktree, uncommitted per T123 order — expected).
- G1: `git diff 9ffeb8b..HEAD -- lib/battle_engine.rb lib/battle_service.rb lib/battle_log_presenter.rb lib/battle_juice_presenter.rb db/ server.rb` EMPTY; targeted `battle_view + style_responsive + battle_log/juice_presenter`: **65 runs, 610 assertions, 0 failures** (this review). Session defines only G1 — no G2/G3 exist (task's "G1-G3" = G1 only).

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Low | sessions/0086-battle-log-juice.md:46-52 | S1 names still don't match real tests (carried + extended): C1 names presenter `test_entries_grouped_by_round_chronological` (doesn't exist — chronological proof is `battle_view_test.rb:86` + e2e); C4 names `test_log_tiered_pacing_delays`/`data-log-index` (actual `test_battle_log_pacing_uses_tiered_step` + `..._cumulative_per_entry`, no such attr); C5 names `test_effect_sync_step_delay` (actual `test_juice_effects_sync_per_line` + arena per-line test); C6 names `test_per_aspect_modules_with_toggles` / `test_aggregates_off_per_aspect_timed` / e2e `per-aspect toggles` (actual `test_battle_arena_carries_jx_toggles_per_aspect` + `test_juice_aspects_modular_with_off_defaults`, no such e2e spec) | Align the four rows to real test names at validation (S2) — doc-only, no code change |
| Low | public/style.css:927-942, 2370-2378 | `--jx-*` custom properties are written (defaults + reduce zeroing) but never read via `var(--jx-*)` — gating is purely attribute-selector (`[data-jx-...="on"]`); the vars are dead, and reduce's `--jx-*:0` is a no-op (the `animation:none !important` in the same block is what actually disables) | Either consume the vars (e.g. `animation-play-state`/opacity via `var(--jx-*)`) or drop the var writes and keep attr-gating + `animation:none` — cosmetic, future pass |
| Info | views/battle.erb:44 | With any toggle `off`, the aspect has no base style (e.g. `data-jx-modal="off"` leaves `.res-overlay` unstyled = immediately visible, no gating) — correct toggle semantics (off = no juice) but means toggling off also removes the reveal delay, not just the animation | Document as intended toggle semantics; no action |
| Info | public/style.css:989-991 | `.arena[data-jx-shake="on"]` shake still uses uncapped full `acc` via `--step-delay` while modal `--log-total` is capped at 3s — on long logs shake fires behind the revealed modal (carried round-5 note; now muted by default since `shake=off` ships) | Keep; cap arena delay at same ceiling only if shake is ever defaulted ON |
| Info | views/battle.erb:84 (carried) | Modal close `<label>` without keyboard semantics, focus never moved into `role="dialog"` — unchanged, out of narrow scope | Future a11y pass; acceptable under CSS-only constraint |

## Residual

Nenhum bloqueador. Round-5 Medium (stale e2e) fixed by `b151c2f`; round-5 Lows about li flash-at-t0 / unsynced projectile are moot (aggregates now default OFF — muted until per-line stepping). Remaining: S1 name alignment + dead `--jx-*` vars, both doc/cosmetic. Ready for user validation (phase 3, S2 table per criterion). No engine/routes/db touched.
