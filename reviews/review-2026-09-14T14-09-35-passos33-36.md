# Review — 0086 Passos 33–36 (`1c6722e..aea27c1`) — 2026-09-14

> **Nota de persistência:** este arquivo foi **re-persistido a partir do relatório do revisor**. A escrita original (mesmo conteúdo, timestamp do relatório) foi perdida: as chamadas de ferramenta do revisor morreram antes do `write` e o arquivo não chegou ao working tree. O conteúdo abaixo é a transcrição fiel do relatório; nenhum achado foi alterado nesta re-persistência.

**Escopo:** range `1c6722e..aea27c1` — **Passos 33–36** (trabalho de fila pós-validação da sessão 0086). 7 commits (`git log --oneline 1c6722e..aea27c1`):

```
aea27c1 Contexto: fecha o T4 (arvore limpa, triagem dos cassettes/config/docs commitada nos passos anteriores)
4773665 Contexto: alinha o §6 da sessao 0086 com o Status e registra o Passo 28 sem commit
4d8c20d Passo 36: dedup do --log-delay e remove strike_side_delay inocuo
c41a0f5 Passo 35: remove hooks e regra legada inertes do juice; C15 prova contencao no @media
754d0d0 Passo 34: linha do tempo do turno em instantes nomeados (--t-approach/--t-travel/--t-impact/--t-log/--t-hp)
1cda0ad Contexto: corrige comentario falso sobre swap delete do htmx no teste do strike
1c6722e Passo 33: shake dispara no instante do impacto (--fx-shake alinhado ao projetil)
```

Este review é **somente leitura** — nenhum arquivo do repo foi alterado por ele. O range toca **9 arquivos**: `public/style.css`, `server.rb`, `views/battle.erb`, `views/_strike_log_entry.erb`, 3 testes (`test/style_responsive_test.rb`, `test/battle_view_test.rb`, `test/battle_strike_routes_test.rb`), `TODO.md` e `sessions/0086-battle-log-juice.md`. HEAD de verificação: `aea27c1`, working tree limpo.

**Evidência (verificada por comando real, não herdada):**
- `./scripts/test` → **1171 runs / 6126 assertions / 0 failures**.
- `./scripts/lint` → **136 files / 0 offenses**.
- `./scripts/check_docs` → **ok**.
- `git diff --stat 1c6722e~1..HEAD -- lib/battle_engine.rb lib/battle_service.rb db/` → **vazio** (nada de engine/XP/economia/schema no range).
- `git status --short` → **vazio** (árvore limpa).

## Confirmações por item da fila

1. **Linha do tempo (T6a, `754d0d0`).** Rodando os helpers reais (`css_time_vars`/`resolve_css_time`) contra o CSS corrente: `approach=0.15`, `travel=0.4`, `impact=0.55`, `hp=0.75`, `t-log=0.2`. Relações: `impact == approach+travel` **OK**; `shake == impact` **OK**; `hp >= impact` **OK**; `shot delay+travel == impact` **OK**. Não restou literal `0.15s/0.4s/0.55s/0.2s` nas regras da linha do tempo — os que restam são `transition`, durações de juice e as próprias declarações `--t-*`.
2. **C4.** `step_of` (`1.0`/`0.5`/`0.2`) intacto (`views/battle.erb:25`); o grupo do botão **Pular** zera `.fighters .bar-fill` (`public/style.css:1911`), `.fx`/`.chip` (`:2040-2043`), `.shot` e `.res-overlay`. O bloco `@media (prefers-reduced-motion: reduce)` segue **ÚLTIMO** (`:2483-2533`) com `animation:none !important` em `.bar-fill`.
3. **C12/C11.** `data-strategy="strike"` presente nos dois renders + 2 asserts (`test/battle_view_test.rb:158`, `test/battle_strike_routes_test.rb:168`); 18 tipos em `#jx-gates` (`public/style.css:1967-1984`) e no seletor genérico (`:1991-2008`). A regra inerte `.log__entry[data-strategy="strike"]` removida em `c41a0f5` não tinha consumidor nem teste que a exigisse.
4. **C15.** Balanceamento de chaves correto; simulando a regra ltr movida para **fora** do bloco `@media`, o teste **FALHA** (a asserção antiga teria passado).
5. **`4d8c20d`.** `--log-delay` só no `<li>`; `.fx`/`.chip` herdam; `strike_side_delay` sempre devolvia `{0=>0.0,1=>0.0}` → HTML emitido `--step-delay: 0.0s` **byte-idêntico**.
6. **C13.** Simulando `--fx-shake: 0.5s` (delta 0,05), o teste **FALHA** — por **aritmética**, não por presença de token.
7. **Transversal.** Nenhuma asserção enfraquecida/removida; nada de engine/XP/economia/rotas/CSRF/JS; §7 intocado (hunks apenas no §6 e nas notas S3 datadas).

## Achados

| Severidade | file:line | problema | recomendação |
|---|---|---|---|
| **Baixa** | `sessions/0086-battle-log-juice.md` (nota S3 do T6a, `:189`) | A nota diz "muda só a expressão, que vira nomeada" — **impreciso**: o `animation-delay` de log/fx/chip saiu de `var(--log-delay)` para `calc(var(--t-log) + var(--log-delay))` com `--t-log: 0.2s`, deslocando-os **+0,2s**, e a barra de HP saiu de `0` para **0.75s**. É intencional no novo contrato, mas a nota deveria dizer que a base é deslocada por `--beat`. | Reescrever a nota para declarar o deslocamento de base por `--beat` (log/fx/chip +0,2s; HP em 0.75s). |
| **Baixa** | `test/style_responsive_test.rb:20-36` (`resolve_css_time`) | O helper divide apenas por `+` e o `term[/\Avar\((--[\w-]+)/]` ignora aritmética no lado direito: simulando `--t-hp: calc(var(--t-impact) - 0.1s)` ele devolve **0.55** (real 0.45) e `hp >= impact` **ainda passa**. | Rejeitar `-` / exigir a forma suportada exata (`var(--x)` puro), falhando em vez de devolver valor parcial. |
| **Info** | `public/style.css:968,975,980,992` | As regras legadas `.arena[data-jx-hp\|hit\|dmg\|ko="on"]` estão tão mortas quanto a de shake removida em `c41a0f5` (os atributos são estáticos no `.arena`; o gate vivo é `:has(> #jx-gates[...])`), mas **um teste ainda as exige**. | Follow-up opcional: remover as regras e atualizar o teste que as exige, ou religá-las ao carrier vivo `#jx-gates`. |
| **Info** | `sessions/0086-battle-log-juice.md` §7 | O §7 cita testes inexistentes (`test_log_tiered_pacing_delays`, `test_effect_sync_step_delay`, `test_aggregates_off_aspect_timed`) — divergência **pré-existente**, fora deste range. | Alinhar nomes critério→teste ou marcar como divergência só-doc. |
| **Info** | `git ls-files` | 33 cassettes VCR seguem rastreados (4 com 18k–27k linhas) — **pré-existente**. O T4 em si está satisfeito: cassette de 20,99 MB no disco e ignorado (`.gitignore:46`), `docs/spawn-ledger.csv` ignorado (`.gitignore:79`). | Nenhuma para este range; triagem de cassettes é dívida pré-existente. |

## VEREDITO: **Aprovado** — 0 blocker / 0 alta / 0 média / 2 baixa / 3 info

Nenhum blocker, nenhum achado alto/médio. O range é higiene de timing/evidência/documentação pós-validação: os dois achados **baixa** são (1) uma imprecisão de redação numa nota S3 e (2) um endurecimento defensivo do helper de teste — nenhum dos dois altera o comportamento validado. **A validação do usuário de 2026-09-14 NÃO é reaberta**: as células `ok` do §7 permanecem intactas e nenhum critério ficou `nok`. Os 3 itens **info** são dívidas pré-existentes ou follow-ups opcionais, fora da força deste range.
