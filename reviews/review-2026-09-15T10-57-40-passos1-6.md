# Review — 0087 Passos 1–6 (`0637cb6..4ae9df0`) — 2026-09-15

**Escopo:** `0637cb6..4ae9df0` (refinamento + Passos 1–6 da sessão `sessions/0087-strike-fx-chegada-real.md`); `55fc846` (Draft) fora do escopo. Review **somente leitura**: nenhum arquivo de implementação/sessão/doc alterado por mim.

```
44e6582 Passo 1: .shot sai do card do lutador para a track do .arena (OOB com origem/alvo/tipo do golpe)
c5cb140 Passo 2: gate do travel do projetil por @container na arena (fecha a faixa 900-980px single-column)
f6f25cb Passo 3: travel do projetil mede a arena (35cqi + 42px) em vez do viewport (40vw)
420e687 Passo 4: gate do projetil acende o .shot da track do .arena (cor de tipo e reduced-motion de volta ao no)
222d918 Passo 5: chegada do projetil medida ao vivo (geometria da track ancorada no alvo)
4ae9df0 Passo 6: suite 1173/0, lint 0 e check_docs ok na limpeza do projetil por cqi
```

**Evidência verificada por mim (não herdada):**
- `./scripts/test test/style_responsive_test.rb test/design_system_test.rb test/battle_strike_routes_test.rb test/battle_routes_test.rb` → **146 runs / 1454 assertions / 0 failures**.
- `./scripts/test` → **1173 runs / 6159 assertions / 0 failures / 0 errors / 0 skips** (a 1ª invocação abortou antes de rodar, com trace de bundler/DB — compatível com o flake pré-existente do `ConnectionRegistryTest` já registrado no draft; a 2ª foi verde).
- `./scripts/lint` → **136 files / 0 offenses**; `./scripts/check_docs` → **ok**.
- `git diff --name-only 0637cb6..4ae9df0` → 11 arquivos, **nenhum `.js`**, `SESSIONS.md` **intocado**.
- Red-capability checada por mutação **em memória** (regex reais dos testes contra CSS mutado), não por inspeção visual.

## Adjudicação dos pontos obrigatórios

**1. Fidelidade S1 (C1–C9).** Nenhum critério depende só de presença/forma *sem* cobertura real, mas há assimetrias:
- **C3/C4/C5 (os pontos sensíveis declarados).** Os asserts estáticos são de forma (`container-type: inline-size` presente; `@container` contém `animation: juice-projectile`; `--fx-travel: calc(35cqi + 42px)`; keyframe sem distância hardcoded) e **não** resolvem `cqi` — isso é correto e está declarado em D5. O que fecha o critério é o e2e: verifiquei a aritmética — `.arena` ancorada em 1200 num viewport 1600 discrimina `cqi` (472 = 0.35·1200+42+10) de `vw` (650); se o gate/token voltarem a `40vw`, o assert de `tx` (±2px) falha. Red-capaz por mutação: reintroduzir `var(--fx-travel, 40vw)` faz o `refute_match` de distância hardcoded disparar; trocar o threshold para 900 faz `assert gate[1].to_i > 980` falhar; remover `container-type` faz o assert de C3 falhar. **Suficiente.**
- **C2** é o critério mais fraco em S1: o teste citado (`test/battle_strike_routes_test.rb#test_strike_oob_moves_shot_into_arena_track`) só prova a **forma** dos `data-*` (regex `[01]`/`[a-z]+`); o pareamento `from_side ≠ to_side` só é provado no e2e de C6 (`expect(obs.from).toBe('1')` quando `to === '0'`). Evidência necessária se um dia o e2e sair: assert de pareamento no minitest (o `entry` do strike é determinístico no stub).
- **C1** prova o defeito real (o `refute_match(/<li class="fighter[^>]*>\s*<span class="shot"/` casa o markup antigo do card — verificado com o markup pré-Passo-1), mas o "filha direta do `.arena`" só é provado indiretamente (geometria do e2e); nenhum teste falha se a track for movida para dentro de uma coluna desde que a âncora acompanhe.
- **C7/C8** são aritmética/seletor vivos (bons). **C9** é o único critério cuja única evidência é *presença* de seletor no bloco reduce — e a guarda é incompleta (achado **B1**).

**2. Histórico da geometria (Passos 1–4 → Passo 5).** Correto e completo. O Passo 1 (`44e6582`) colocou `#jx-shot-track` no `.arena` **sem CSS** (o commit não toca `public/style.css`) → 4º item do grid → deslocamento das 3 colunas e `.shot` fora da tela em RTL. O Passo 5 é a **correção da omissão do Passo 1** (`.shot-track{position:absolute;inset:0}` + `.arena{position:relative}` + âncoras por lado), não um remendo sobre um desenho alternativo: o `.shot` continua **descendente do `.arena`** (track como filha direta), então o contrato do carrier C7 (`--fx-color` no `.arena`, herdado por `.shot`) segue válido; `pointer-events:none` é a escolha certa para uma camada `inset:0` (o `.shot` herda `none`, sem captura de clique) e `aria-hidden` está no track (o nó decorativo). Não há `.shot` fora do `.arena`, então remover o fallback de `--fx-travel` é seguro.

**3. Integridade do e2e do gate.** **Red-capaz, não vacuous.** `expect.poll` só "tolera a janela de swap"; com o gate quebrado (threshold 900, ou `display` resolvido) o valor estável é `display:block` e o `toEqual({display:'none',animation:'none'})` falha no timeout; se a locator nunca casar um nó anexado, o `evaluate` do poll estoura → falha (não passa vazio). A causa declarada (`getComputedStyle` de nó destacado devolve `""` no Chromium) é real e a correção é adequada. Restrição: a única prova do **gate estreito** é o e2e por viewport 900 (o estático só olha `gates.last`, ver B3) e o e2e **não roda no CI** (I1).

**4. Contrato truncado (`--fx-travel`).** Verificado: os únicos consumidores de `--fx-travel` são `juice-shot-ltr/rtl` (`public/style.css:1587/1598`) — o `grep` por `fx-travel` no repo não acha outro. A derivação bate com D4 e com o e2e: `1.06/3.06 = 34,64% ≈ 35cqi`; 2·gap(32px) + resíduo do fr ≈ 41,8 ≈ 42px; arena 1200 → `tx = 472`. ✓

**5. Leftovers.** `views/_strike_fighters.erb` e `views/_fighter_panel.erb` **não** carregam mais nada do projetil ✓. Mas: o contrato `--jx-*` inteiro está morto (0 consumidores `var(--jx-*)`), o `is-attacking` ficou órfão, e o gate `@container` está duplicado (B2/B3/B4).

**6. Disciplina de escopo.** Limpo: só `server.rb` (fragmento OOB de view), `views/` (3 ERBs + 1 novo), `public/style.css` e testes; `_strike_log_entry.erb` intocado; sem `.js` (G2 ✓); sem engine/regras/economia/migração; sem segunda estratégia (`data-strategy` intocado, D6a); CSS novo sem hex cru (G3 ✓, `DESIGN.md` intocado).

**7. Processo.** A sessão **não** alega validação nem `Concluída` ✓; `SESSIONS.md` intocado ✓; as reaberturas S3 existem em `sessions/0086-battle-log-juice.md:199` (C15) e `:201` (C9), datadas 2026-09-15, com C13 explicitamente sem mudança de semântica ✓. Dois desvios menores de doc: o Status da 0087 ainda diz "Implementação Pendente" com os 6 passos commitados (B5) e a nota de G2 cita um comando/composição que não corresponde ao diff real (B6).

## Achados

| Severidade | file:line | problema | recomendação |
|---|---|---|---|
| **Baixa** | `public/style.css:2494` (+ gate `:1025/:2469`) | A guarda de reduced motion só mata `animation`; o gate continua aplicando `display:block` → com `prefers-reduced-motion: reduce` o `.shot` fica visível como **ponto estático** na borda da coluna do atacante (opacidade 1, sem fill) e persiste até o próximo OOB. C9 só prova a *presença* do seletor no bloco reduce, não o efeito "flash-only". Padrão herdado da 0086, mas o Passo 4 re-apontou exatamente esse seletor. | Resetar `display:none` para `#jx-shot-track .shot` nos dois blocos reduce (ou tirar `display:block` de trás do gate) e fazer o teste de C9 falhar se o `display` não for resetado. |
| **Baixa** | `public/style.css:965` e `:2517-2525` | `--jx-shot: 0` (e a família `--jx-*` inteira) é **inerte**: 0 ocorrências de `var(--jx-*)` no repo. Mesma classe das regras que a 0086/Passos 35 e 38 já removeram; a linha 2523 é ainda mais órfã agora que o reducer aponta para `#jx-shot-track .shot`. | Provar o 0 por grep e remover a família (ou migrar os toggles para o padrão `:has(> #jx-gates[...])`), atualizando os testes que a citam. |
| **Baixa** | `public/style.css:1024-1029` vs `:2468-2482` | Gate `@container (min-width: 981px)` **duplicado**: o bloco de `:1024` é subconjunto estrito do de `:2468` (mesmo seletor, mesmas declarações) — regra redundante. Pior: o teste estático pega `gates.last` (`test/style_responsive_test.rb:359-360`), então mudar o threshold do **primeiro** bloco passa no teste (verificado por mutação). | Manter um único gate (o que tem as direções) e adicionar `refute` de um segundo `@container` de travel. |
| **Baixa** | `public/style.css:1016-1022` | A âncora hardcoda a razão do grid (`/3.06`) em 2 lugares, enquanto a razão vive em `grid-template-columns: 1fr 1.06fr 1fr` (`:605`) e o travel em `35cqi` (`:952`): 3 cópias de um mesmo fato geométrico, **sem teste que as amarre**. Mudar a razão do grid quebra a chegada silenciosamente. | Teste que derive o divisor dos `fr` reais de `grid-template-columns` e o percentual do `cqi` da fatia da coluna do meio. |
| **Baixa** | `e2e/specs/battle-log.spec.ts:191-206` | Os dois `waitForFunction` de `strikeAndMeasure` engolem timeout com `.catch(() => undefined)`; se a animação não rodar, a medição pode cair no **nó anterior ainda no DOM** (mesma `#jx-shot-track .shot`) e ainda satisfazer o loop. O `expect([...seen].sort()).toEqual(['0','1'])` limita o dano, mas a espera não é prova. Além disso a tolerância de chegada é assimétrica (`-20..40` num lado, `-40..20` no outro) e ~3–4× o erro real (~5–15px). | Falhar a medição quando o wait estoura (flag `ok`) ou exigir `getAnimations().length > 0`/marcador antes de ler o transform; derivar a tolerância da geometria (`≤10px`) em vez de calibrá-la. |
| **Baixa** | `lib/battle_juice_presenter.rb:37` | `is-attacking` continua sendo emitido em todo lutador atacante, mas **nenhuma** regra CSS o consome desde o Passo 4 (ele foi removido do bloco ODS, da rede reduce e do `design_system_test`). O comentário do teste diz que "segue contrato do presenter", mas nada prova mais esse contrato. | Ou re-apontar um teste para o presenter (asserção 1:1 classe↔hook), ou remover a classe junto com seu `shooting?`. |
| **Baixa** | `sessions/0087-strike-fx-chegada-real.md:8` e `:53-61` | O Status ainda diz `Implementação (fase 2, TDD) | **Pendente**` e as colunas `Estado` de C1–C9/G1–G3 seguem `pendente`, embora os 6 passos estejam commitados e verdes (§Progresso). Não é alegação de validação (ok), mas é factualmente falso. | Ajustar o Status para "concluída (fase 2) — aguardando Revisor/validação" e datar a nota do §4, sem tocar no §7. |
| **Baixa** | `sessions/0087-strike-fx-chegada-real.md:155-156` | A nota de G2 afirma que `git diff 0637cb6..HEAD` "toca só `public/style.css` + ERBs" — falso para esse range (`server.rb`, 4 testes e a própria sessão também mudam). A substância (nenhum `.js`) está certa; o comando citado nos critérios é `git diff --stat -- public/ views/`. | Citar o comando do critério (escopado a `public/ views/`) em vez do range inteiro. |
| **Info** | `.github/workflows/ci.yml` | O CI roda só `./scripts/test`, `./scripts/lint` e `./scripts/check_docs`; o e2e (única prova de D1b/D4 — geometria `cqi` — e da metade e2e de C4) não é gate. Não há como uma regressão de geometria ser pega automaticamente. | Adicionar job de Playwright (ou pelo menos rodar o spec do strike no pipeline). |
| **Info** | `views/battle.erb:46-47` | O render completo re-deriva o payload do `.shot` no view (`@engine.log.reverse.find { … != :item }` + `juice.from_side/to_side`) enquanto o OOB o deriva em `server.rb:1137-1145` (`strike_shot_oob`, com o gate `strike_gates(entry)["shot"]`). Duplicação de regra que pode divergir. | Mover a seleção "última entrada de golpe" para o presenter/um helper usado pelos dois caminhos. |
| **Info** | `public/style.css:1016-1022` / `_jx_shot.erb:1` | `data-to-side` e `data-move-type` no `.shot` não têm consumidor CSS (só `data-from-side` escolhe o keyframe); são hooks de contrato/e2e (C2). Coerente com D6a, mas vale saber que a evidência vive no e2e. | Nenhuma ação; manter a asserção de pareamento no e2e (ver ponto 1/C2). |
| **Info** | `./scripts/test` (1ª execução) | A primeira invocação da suíte abortou antes de rodar (trace de bundler/DB); a segunda foi 1173/6159/0. Consistente com o flake pré-existente registrado em `docs/draft-backlog.md` — fora desta sessão. | Nenhuma para este range. |

## VEREDITO: **Aprovado** — 0 blocker / 0 alta / 0 média / 8 baixa / 4 info

Os critérios C1–C9 e as garantias G1–G3 estão atendidos no worktree (suíte 1173/6159/0, lint 136/0, `check_docs` ok, sem `.js` novo, `SESSIONS.md` intocado, S3 reaberto datado na 0086). As três fraquezas declaradas foram adjudicadas: a trilha `cqi` **é** provada de verdade pelo e2e (com discriminação `cqi`×`vw` e sub-mutação), o fix de geometria do Passo 5 é uma correção completa (o `.shot` segue descendente do `.arena`) e o `expect.poll` do gate **é** red-capaz. Nenhum achado altera comportamento validado nem reabre critério; os 8 baixos são dívida de higiene/evidência (guarda reduce incompleta, gate duplicado, `--jx-*` morto, acoplamento geométrico sem teste, espera do e2e que engole timeout, `is-attacking` órfão) e 2 imprecisões de doc na própria sessão — corrigíveis em follow-up, sem bloquear a fase 3.
