# Review 0082 round 4 (S7, narrow: T77 074adcc + T78 78c3ea3) — 2026-09-11

Scope: no scroll-jump, modal closes, confrontation enabled post-heal, G1–G3. No source edits, no commits.

## Veredito: Aprovado

- No scroll-jump: `views/battle.erb:125,137` + `views/index.erb:25` trocaram `<a href="#center-modal">` por `<button type="button">`, mantendo `hx-get="/team/center" hx-target="#center-modal" hx-swap="outerHTML" hx-push-url="false"`. Sem hash, sem push — sem jump. Prova: `test/modal_routes_test.rb` (`refute_includes href="#center-modal"`, 14 runs 0 failures).
- Modal fecha: `server.rb:776` (`render_heal_success_notice`) mantém `oob_close_center_modal` (`server.rb:792-794`, `_center_slot` OOB outerHTML) na cadeia. Prova: `test_heal_via_modal_closes_and_rerenders` verde.
- Confronto habilitado pós-heal: `server.rb:775` invalida (`settings.battle.invalidate`) + `server.rb:779-790` OOB `#battle-view` (innerHTML, `prepare_battle_fragment`) gated em `battle_view_requested?` (HX_CURRENT_URL ou referer com `/battle`). Prova: `test_heal_from_battle_refreshes_battle_view_with_new_confront_enabled` (OOB presente, `hx-post="/battle/play"`, sem `disabled title="Recupere..."`) + `test_heal_outside_battle_omits_battle_view_oob` — 3 runs `/heal/` 0 failures.
- G1: modal 14/0; heal 3/0; combinado modal+battle só falha em `test_battle_end_shows_gameloop_ctas` (RNG pré-existente, falha com stash segundo tarefa). Lint: 26 offenses no repo, mas `server.rb` só tem `member_types_from_api AbcSize 17.97/17` em `server.rb:76` (pré-existente, fora do diff); `test/battle_routes_test.rb` + `test/modal_routes_test.rb` limpos.
- G2: `git diff --stat -- lib/heal_cost_policy.rb lib/heal_service.rb lib/reward_rule.rb` vazio; `a560bad..HEAD` só toca `server.rb`, `views/battle.erb`, `views/index.erb`, `test/battle_routes_test.rb`, `test/modal_routes_test.rb`, `test/style_responsive_test.rb`, `public/style.css`.
- G3: escopo contido aos dois passos; este review fecha S7 round 4.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Low | server.rb:785-790 | `battle_view_requested?` depende de `HX_CURRENT_URL`/referer; cliente sem header (heal direto fora do htmx) omite OOB e a view só atualiza no reload — comportamento correto mas implícito | documentar no arquivo da sessão ou aceitar como contrato htmx; sem mudança de código |
| Low | views/battle.erb:138, views/index.erb:26 | links do Mart ainda usam `href="#mart-modal"` (com `hx-target="body"` no battle) — mesmo padrão de scroll-jump que T77 removeu do Center permanece no Mart | backlog/fora deste slice; unificar Mart para `<button>` em sessão própria se julgado necessário |
| Low | working tree (uncommitted) | `public/style.css` (65 linhas), `test/pokemon_routes_test.rb`, cassettes e `docs/draft-backlog.md` modificados no working tree, fora de `a560bad..HEAD` — não pertencem ao slice | não commitar junto da 0082; separar em sessão própria |
| Info | test/battle_routes_test.rb:384 | `test_battle_end_shows_gameloop_ctas` falha (RNG, pré-existente) — impede G1 "suíte 100% verde" literal | manter registrado como falha pré-existente; sem ação neste slice |

## Residual

Nenhum bloqueador. Toast feedback já movido para backlog T75 por ordem da tarefa — fora deste veredito.
