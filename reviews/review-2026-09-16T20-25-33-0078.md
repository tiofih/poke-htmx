# Review — 0078 `open-design-battle-1a1` (`9b421c4..39f3de6`) — 2026-09-16

**Escopo:** Passos 1–4 da fase 2 — `9b421c4` (Passo 1: bloco ODS `0078` aditivo + `test/design_system_test.rb`), `924e5b6` (Passo 2: fim de `views/battle.erb` + `test/battle_end_states_test.rb` + 3 cassettes), `4604ee9` (Passo 3: `test/battle_view_test.rb`), `39f3de6` (Passo 4: Status/§4/§8 da sessão). Spec autoritativa: `sessions/0078-open-design-battle-1a1.md` (C1–C3, G1–G3, M1). Review **somente leitura**: nenhum arquivo de implementação/sessão/doc alterado; artefato único (não commitado).

```
9b421c4  public/style.css (1197+/1003-, -w: 211+/37-) | test/design_system_test.rb (+20)
924e5b6  views/battle.erb (40 +/-) | test/battle_end_states_test.rb (+131)
         | test/cassettes/BattleEndStatesTest/*.yml (3× ~1.78k linhas)
4604ee9  test/battle_view_test.rb (+15)
39f3de6  sessions/0078-open-design-battle-1a1.md (+20/-7)
```

**Evidência verificada por mim (não herdada):**
- `./scripts/test test/battle_end_states_test.rb test/design_system_test.rb test/battle_view_test.rb test/battle_routes_test.rb` → **123 runs / 1155 assertions / 0 failures / 0 errors / 0 skips** (90s, `poke-htmx-db-1`). `./scripts/test test/team_routes_test.rb test/mart_routes_test.rb` → 97 runs / 0 failures (a "8 falhas alheias" do G1 **não reproduz** no HEAD — ver A-6). `./scripts/lint` **escopado** (`test/battle_end_states_test.rb test/design_system_test.rb test/battle_view_test.rb`) → 3 files, no offenses. Suíte total **não** rodada (instrução de orçamento); `views/*.erb` não é lintável (passar `.erb` ao RuboCop devolve `Lint/Syntax: unterminated string`, ruído — não é achado).
- `./scripts/check_docs` → `ok> docs consistentes`; `./scripts/checar-sessao 0078` → `ok> sessão 0078 estruturalmente consistente`.
- **G2 (leitura apenas) ok:** `git show --stat` dos 4 commits não toca `lib/`, `server.rb`, `db/`, `Gemfile*` (verificado commit a commit → `none`). `battle.erb` consome `@game_over` de `server.rb:1043` sem alterá-lo.
- **CSS aditivo/bloco:** delimitador `/* === Battle 1:1 (0078)` em `public/style.css:1649`, fecho `/* === Open Design System (0072): fim ===` em `:1690` → bloco dentro do ODS, antes do `fim`. Diff normalizado (regras por `}`, sem whitespace) do `style.css` antes/depois de `9b421c4`: 293→330 regras, **39 regras novas** (todas do bloco 0078) e **2 regras pré-existentes textualmente substituídas**, ambas equivalentes (`:root` só reflui linhas; `#global-loading` `width .2s`→`0.2s`). Nenhum seletor do bloco redefine regra fora dele; `.mini-arena` aparece 2× = base + `@media (max-width: 600px)` (mesmo bloco, fiel a `battle-end-states.html:91-92`).
- **Fidelidade aos protótipos:** classes e estrutura conferem com `open-design/battle-end-states.html:155-181` (`state-card`/`state-head`/`state-pill{win,loss,over}`/`mini-arena`/`frow lost`/`result-card`) e `open-design/battle-results-desktop.html:154-158` (`res-screen`/`res-top`/`res-top-left`/`state-pill`/`state-title`). Os chips de tipo com `style="background:var(--t-…)"` **são inline também no protótipo** (`battle-end-states.html:161-166`) → fiel, não é achado. `hp_fill` traduz tier (`high→ok, medium→warn, low→danger`) como o protótipo.
- **Empate real tratado:** `winner.nil?` → `draw` (o markup antigo `winner.zero?` estouraria); `test_battle_end_states_draw` cobre. Balanceamento de `<div>` inalterado pelo Passo 2 (22/21 → 22/21; o único `<div>` sem par é o pré-existente em `views/battle.erb:23`, fora da região 0078).
- **Sem resíduo de debug** nos 4 commits (`pry`/`binding.irb`/`console.log`/`TODO` → 0 linhas; os 2 hits de grep eram ids aleatórios de cassettes VCR com a substring "pry").
- Nota de escopo temporal: `39f3de6` é ancestral de `main` (HEAD `6077f2c`, 2026-09-15); `views/battle.erb` recebeu trabalho de sessões posteriores (0082/0086/0088) — os testes acima rodaram na árvore de trabalho e o markup 0078 persiste nela.

## Adjudicação dos pontos obrigatórios

**1. C1–C3 existem e são substantivos (nenhum critério vazio; nenhum sem teste — S1 satisfeito no essencial).** C3 tem teste próprio com nome exato (`test_design_system_battle_end_states_classes`, `test/design_system_test.rb:61`). C1 ganhou arquivo novo com 5 métodos cobrindo os 4 estados (win/defeat/draw/game-over) e C2 (`test_results_desktop`). As 3 linhas de estado em §4 foram repontadas para `verde (fase 2)` no Passo 4, o que é correto para a fase. S1 tem duas lacunas de **preenchimento** da coluna de evidência (C1 nomeia 1 dos 4 métodos; "delimitador `0078`" não tem assert) — A-5.

**2. G2 substantivo ok.** Nenhuma mudança em motor/economia/XP, nenhum schema/gem/service; `resolve` (`lib/battle_service.rb:407-420`), `finish_effects` (`:449-457`) e `expose_battle_result` (`server.rb:1015-1022`) intocados. Backend thin de fato **não precisou** mudar (o resultado exposto já bastava: `@engine.winner`, `@game_over`, `@xp_gained`, `@money_gained`).

**3. G1 substantivo com evidência desatualizada.** Escopo 0078 100% verde (acima). Mas a linha de G1 e o Status registram "1070/5088 com 8 falhas alheias (0077, `_center`/`_mart` dirty)": `views/_center.erb`/`views/_mart.erb` estão **limpos** no HEAD e `test/team_routes_test.rb test/mart_routes_test.rb` passa (97/0). A evidência ficou *stale* — A-6. Não é regressão da 0078.

**4. G3:** parte de docs conferida verde (`check_docs` + `checar-sessao`); o veredito S7 é este artefato → G3 passa a `ok` com o `Aprovado` abaixo.

**5. Fidelidade híbrida (decisão §5) — executada com uma composição não prevista.** Onde havia legado (juice 0063/0074, `data-side`, contratos htmx, `result`/`rewards`/`ctas`), o legado foi preservado e as asserções de contrato antigas continuam verdes; onde não havia, a cópia é literal (classes + inline de tipo idênticos ao protótipo). O desvio é que **os dois protótipos foram compostos, não alternados** — A-1.

## Achados

| Severidade | file:line | problema | recomendação |
|---|---|---|---|
| **Média** | `views/battle.erb:61` e `:66` (@`924e5b6`) | **O badge de estado e o título renderizam duas vezes.** `state-pill` aparece em `res-top-left:61` **e** em `state-head:66` (mesmo `end_label`), e há **dois `<h2>`** para o mesmo estado (`Vencedor: Seu Time` em `:62` e `end_title`/`Seu Time venceu` em `:66`). Nenhum dos dois protótipos mostra os dois blocos juntos: `battle-results-desktop.html:154-158` é *uma* variante (res-top com pill + state-title + ctas + listas com `side-title`) e `battle-end-states.html:155-181` é *outra* (state-card com state-head + mini-arena + result-card). O resultado é uma tela com pill e título redundantes — visível, e o único ponto que o M1 do usuário vai julgar como desvio. | Escolher uma composição: ou o `res-top` (desktop) é o cabeçalho e o `state-head` fica só com o `h2` (pill removida de `:66`), ou o `state-head` é o cabeçalho e o `res-top` fica só com o `state-title`. Registrar a escolha em §8 (decisão de composição, não de código). |
| **Média** | `test/battle_end_states_test.rb:78` vs `views/battle.erb:134-140` e `public/style.css:1659-1660` | **C2 promete "`rewards` (XP/dinheiro) no padrão `battle-results-desktop.html`" e o teste não prova isso.** `assert_match(/class="rewards"/)` já era satisfeito **antes** do commit: o `<p class="rewards">` do legado (`#result-box > .result-card > .result`) é linha **de contexto** no diff de `924e5b6` — a asserção é vacuosa. No protótipo os rewards ficam **dentro de `.state-title`** (`battle-results-desktop.html:158`: `<div class="state-title"><h2>…</h2><p class="rewards">…`), e no entregue o `state-title` (`views/battle.erb:62`) tem **só o `<h2>`**; as regras portadas `.state-title .rewards`/`.state-title .rewards strong` (`public/style.css:1659-1660`) ficaram **sem consumidor**. Ou seja: a colocação desktop de rewards não foi portada e o teste mascara. | Mover o `<p class="rewards">` (e o `evil`/`bad` quando houver) para dentro de `.state-title` como no protótipo, **ou** reescrever a linha de C2/test para o que de fato se entrega (rewards no `result-card` legado) — em qualquer caso, trocar a asserção vacuosa por uma que ancore a posição (ex.: `assert_match(/state-title.*rewards/m, body)`). |
| **Baixa** | `public/style.css:1657` e `:1672` | **Duas regras portadas ficaram mortas.** `.res-top .ctas` e `.side-title` vieram de `battle-results-desktop.html:91,96` mas não têm consumidor nas views: os CTAs continuam no `.ctas` legado dentro de `.result-card` (`views/battle.erb:157-168`) e a lista de lutadores usa `.mini-side-title` (`:70`) + `.fighter-rows`/`.frow`. CSS morto com teto conhecido: falsa sensação de "desktop completo". | Remover as duas (o adaptado é o `.mini-side-title`), ou mover os CTAs/listas para o layout desktop — decidir junto com A-1. |
| **Baixa** | `views/battle.erb:75` vs `:70` (@`924e5b6`) | **"derrotado" tem duas definições que podem divergir.** A linha marca `lost` por `fighter.hp_percent.zero?`, que é **arredondado** (`lib/fighter_presenter.rb:30`: `((hp_current.to_f / hp_max) * 100).round`) — um lutador com 1/200 HP rende `0%` e ganha `li.frow.lost` + `· derrotado` **sem** estar caído; o título do lado usa `fainted?` (`:70`). Dois critérios para o mesmo rótulo. | Usar o mesmo predicado nas duas: `" lost" if poke.fainted?` (o `poke` está no escopo do `each`; `BattlePokemon#fainted?` em `lib/battle_pokemon.rb:88`). |
| **Baixa** | `test/battle_end_states_test.rb:20-49` e `test/design_system_test.rb:61-79` | **Duas lacunas de S1 no preenchimento.** C1 nomeia só `test_battle_end_states` na coluna de evidência, mas derrota/empate/game-over são provados por `test_battle_end_states_defeat`/`_draw`/`_game_over` (não citados em §4). E C3 exige "com delimitador próprio `0078`", mas o teste só verifica classes entre `inicio`…`fim` — o delimitador em `public/style.css:1649` **não** tem assert. | Completar a coluna de C1 com os 4 métodos e acrescentar 1 assert do delimitador (`assert_match(%r{/\* === Battle 1:1 \(0078\)}, block)`). |
| **Baixa** | `sessions/0078-open-design-battle-1a1.md:8` e `:56` | **Status subnotifica o próprio commit e G1 carrega evidência stale.** `:8` diz "Passos 1-3 commitados (9b421c4, 924e5b6, 4604ee9)" — mas essa linha foi escrita pelo commit do **Passo 4** (`39f3de6`), que não aparece. E `:56` cita "1070/5088 com 8 falhas alheias (0077, `_center`/`_mart` dirty)": no HEAD os dois arquivos estão limpos e `team_routes`/`mart_routes` dão 97 runs / 0 failures — a evidência registrada não corresponde mais à árvore. | Citar o Passo 4 em `:8` e atualizar a linha de G1 com o número vigente (ou marcar explicitamente a data da medição e o fato de o dirty da 0077 ter sido resolvido depois). |
| **Baixa** | `4604ee9` e `test/battle_view_test.rb:100-113` | **Fronteira de passo não corresponde ao plano, e o teste novo duplica o anterior.** §6 Passo 3 promete "mini-arena + `rewards` no padrão desktop" (view/CSS), mas `4604ee9` altera **apenas o teste** (+15): o trabalho de produção de C2 foi entregue no Passo 2 (`924e5b6`). E as 6 asserções de `test_battle_end_uses_results_desktop_markup` (`res-screen`/`res-top`/`state-title`/`state-card`/`mini-arena`/`frow`/`result-card`) são **verbatim** as de `test_results_desktop` (`test/battle_end_states_test.rb:62-76`) — mesmo padrão de duplicação já apontado na review da 0079. | Manter o plano honesto (registrar em §8 que C2 saiu no Passo 2) e remover a duplicata, deixando a extensão só para o que for específico de `battle_view_test` (o `#battle-view` do render cheio). |
| **Info** | `9b421c4` (diff) | **Passo 1 varre o arquivo inteiro, não só o bloco.** `public/style.css` entra com 1197+/1003- (com `-w`: 211+/37-): reformatação de espaços→tabs em todo o arquivo + o bloco da **0079** (`.card--tight`, já adjudicado na review da 0079) junto do bloco 0078. Semanticamente vazio (só 2 regras diferem textualmente, ambas equivalentes) e **divulgado** em §8:106, mas contraria o "aditivo, só dentro do bloco" declarado em §2/§3. Ruído de bisect/review, não de comportamento. | Já registrado; para as próximas sessões com `style.css` dirty: `git add -p public/style.css` (só o hunk do bloco) — a review da 0079 chegou à mesma recomendação (A-1). |

## VEREDITO: **Aprovado** — 0 blocker / 0 alta / 2 média / 5 baixa / 1 info

C1–C3 têm prova automatizada verde nas revisões sob review, G2 (motor/economia leitura-apenas) está intacto, G3 fecha com este artefato, o CSS 0078 é aditivo e vive dentro do bloco delimitado, e não há resíduo de debug nem regressão de contrato htmx/juice. Os dois achados **média** são de composição/evidência, não de correção: A-1 (badge/título duplicados na tela entregue) é decisão visual a resolver **na validação (M1)** com o usuário — corrigir ou registrar como composição deliberada — e A-2 (rewards fora do `state-title` + asserção vacuosa) reabre **C2** se o texto do critério for mantido literal (S3: reabrir, datar, reaprovar). As 5 baixas são de higiene (CSS morto, predicado divergente, preenchimento S1, Status/G1), endereçáveis no mesmo passo ou no draft de backlog.
