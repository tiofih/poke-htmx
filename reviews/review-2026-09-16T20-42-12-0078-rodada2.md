# Revisão S7 — Sessão 0078 (rodada 2, escopada)

**Escopo:** SOMENTE a rodada de correção da revisão `reviews/review-2026-09-16T20-25-33-0078.md`
(commits `814b722` + `67aec64`): os achados foram corrigidos? a correção introduziu defeito novo?
Não re-audita a sessão 0078 inteira.

## Evidência executada

| Checagem | Comando | Resultado |
|---|---|---|
| Árvore/stat dos commits | `git show --stat 814b722 67aec64` | 5 arquivos (+53/-45) + 1 arquivo de sessão (+8/-5) |
| G2 (engine/economia leitura-apenas) | `git diff --stat 6077f2c..HEAD -- lib server.rb db Gemfile Gemfile.lock` | **vazio** (ok) |
| CSS aditivo/dentro do bloco | `git show 814b722 -- public/style.css` | só remoções + reescrita do comentário; bloco `0078`…`fim` (`public/style.css:1755`…`:2049`) inalterado em ordem/indentação |
| Órfãos de CSS com consumidor | `rg -n 'res-top\|state-title\|side-title'` (fora de `public/style.css`) | nenhum consumidor (só comentários de teste/doc) |
| **A-2 mutation test** | mutar `<p class="rewards">` → `rewardsMUT` em `views/battle.erb` + `./scripts/test test/battle_end_states_test.rb` | **RED**: `6 runs, 63 assertions, 1 failures` — falha exatamente no assert ancorado (`Expected /<div class="result-card">.*?<p class="rewards">.*?<div class="ctas">/m`). Mutação revertida (`git diff -- views/battle.erb` vazio) |
| Escopado | `./scripts/test test/battle_end_states_test.rb test/battle_view_test.rb test/design_system_test.rb` | **50 runs / 750 assertions / 0 failures / 0 errors** |
| Lint | `./scripts/lint` | **136 arquivos, 0 offenses** |
| Árvore limpa | `git status --short` | só untracked pré-existentes (`reviews/*`, `test/cassettes/*`); nenhuma modificação minha |

Snapshots de produto conferidos no markup renderizado (via falha da mutação, documento completo):
um único `class="state-pill win"` (`views/battle.erb:101`), `.state-card` → `.mini-arena` →
`.frow`, `.result-card` com `winner-badge` / `rewards` / `.ctas` (Novo confronto, Poke Center,
Poke Mart) e restart de game-over intactos.

## Achados anteriores

| Achado | Status | Prova |
|---|---|---|
| A-1 (Média) badge/título duplicados (`res-top-left` + `state-head`) | **Corrigido** | `battle.erb:100-101` é a única variante (`state-head`); `res-top`/`res-top-left`/`state-title` removidos do markup e das regras; `test_results_card_rewards` conta `state-pill` = 1 e faz `refute_match(/class="res-top/)` — asserção de delta real (antes eram 2) |
| A-2 (Média) assert vacuoso + regras `.state-title .rewards` sem consumidor | **Corrigido** | assert agora ancorado na posição e **falha de verdade** (mutation RED acima); `.state-title*` removidas; C2 reescrito para o que se entrega, registrado com S3 em `sessions/0078-open-design-battle-1a1.md:68` |
| A-3 (Baixa) CSS morto `.res-top .ctas` / `.side-title` | **Corrigido** | 7 regras órfãs removidas; `rg` sem consumidor |
| A-4 (Baixa) `hp_percent.zero?` vs `fainted?` | **Corrigido** | `battle.erb:110` usa `poke.fainted?` (mesmo predicado de `:105`); teste novo `test_lost_follows_fainted_not_rounded_hp` (hp 1/300 vivo ≠ lost, KO = lost) |
| A-5 (Baixa) lacunas S1 | **Corrigido** | C1 cita os 4 métodos de estado + o novo (`:48`); C3 com `assert_match(%r{/\* === Battle 1:1 \(0078\)})` e `refute_match(/\.res-top\b/)` |
| A-6 (Baixa) Status/G1 stale | **Corrigido** | `:8` cita Passo 4 + `814b722`; `:57` com suíte completa 1180/6208/0 (HEAD `814b722`) e a nota de que as 8 falhas da fase 2 eram do dirty da 0077 |
| A-7 (Baixa) duplicata do Passo 3 | **Corrigido, sem perda de cobertura** | `test_battle_end_uses_results_desktop_markup` removido; os 6 asserts eram verbatim de `test_battle_end_states*` (que cobrem `res-screen`/`state-card`/`mini-arena`/`frow`/`result-card`) no **mesmo** caminho de render completo (`post /battle/play`) — nada exclusivo de `battle_view_test` ficou sem prova |

## Achados desta rodada

| Severidade | file:line | problema | recomendação |
|---|---|---|---|
| **Baixa** | `views/battle.erb:98` e `:101` | **Sobrou uma duplicata de título visível:** `modal-head` renderiza `<h2>Seu Time venceu</h2>` e `state-head` renderiza `<h2>Seu Time venceu</h2>` — mesmo texto, duas vezes na mesma tela. Não foi introduzido por `814b722` (o `modal-head` é do modal legado, já existia sob review na rodada 1 e não estava entre as linhas citadas de A-1), mas A-1 pedia explicitamente "um título" e este é o resíduo que M1 vai ver. | Decidir na validação (M1): manter o `modal-head` só com o botão fechar (`aria-label` já carrega o título) ou esconder `.res-screen .modal-head h2`. Não bloqueia: badge e título do card já são únicos. |
| Info | `test/battle_end_states_test.rb:70-84` | O assert reescrito é **guard de regressão**, não prova de delta: o `<p class="rewards">` legado já ficava dentro de `.result-card` antes de `924e5b6`. É honesto (o critério C2 foi reaberto e reescrito para essa colocação, S3 em `:68`), mas a reescrita **ainda pende de reaprovação do usuário** na fase 3. | Reaprovar C2 na validação (S3) — já registrado no arquivo da sessão. |
| Info | `test/battle_end_states_test.rb:71` | `assert_equal 1, body.scan('class="state-pill ')` conta no documento inteiro: qualquer `state-pill` não relacionado (ex.: painel do lutador) quebra o teste por motivo alheio ao A-1. | Se incomodar depois, escopar ao `#result-box`/`.state-card`. |
| Info | `docs/draft-backlog.md:392` | A lista de "seletores sem uso" ainda cita `.side-title:1778`, removido nesta rodada (`public/style.css` não tem mais a regra). | Ao revisar o draft, remover o item `.side-title` da lista. |

## VEREDITO: **Aprovado** — 0 blocker / 0 alta / 0 média / 1 baixa / 3 info

Os 2 achados **média** e as 5 **baixas** da rodada 1 estão corrigidos, com prova executada (não por
leitura): a asserção de A-2 falha de verdade sob mutação do markup, a contagem de `state-pill` de A-1
é delta real, o CSS órfão saiu sem deixar consumidor pendente, a dedup do Passo 3 não removeu
cobertura (só asserts verbatim, no mesmo endpoint de render completo) e G2 segue com diff vazio em
`lib/`/`server.rb`/`db/`/`Gemfile*`. O escopado (50/750/0) e o lint (136/0) fecham. Nenhum defeito
novo introduzido pela correção; a baixa restante (`modal-head` × `state-head` com o mesmo `h2`) já
existia antes da rodada e é decisão visual de M1, não de código.

_Rodada 2 de no máximo 3 do loop S7. Artefato não commitado, conforme instrução._
