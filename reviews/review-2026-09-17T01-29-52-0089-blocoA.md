# Review 0089 — Bloco A (Passos 1–5): remoção do estado morto `user_state`

Data: 2026-09-17 · Escopo fechado: `d57947c` (Passo 1), `d6f1ff3` (Passo 2), `e7bcaf3` (Passo 3), `34b07b0` (Passo 4), `a487875` (Passo 5). **Bloco B fora do escopo** (outro revisor).

Método: `git show --stat/--diff`, `rg -n` com fronteira de palavra, `./scripts/rake db:setup` **3×** + consultas PG em `pokedex`/`pokedex_test`, sonda de linha (INSERT + setup), testes escopados + os 4 do gate citados no C4, `./scripts/lint`, `./scripts/check_docs`. Nada editado/commitado; mutação revertida (a linha-sonda foi destruída pelo próprio `db:setup`, 0 resíduos).

## VEREDITO: Aprovado (0 blocker / 0 alta / 1 média / 2 baixa / 3 info)

O Bloco A entrega C1–C6 e C8; o desvio Passo 2↔3 é legítimo; nenhum achado bloqueia a fase 3 (a média é pré-existente e de redação, fora dos 5 commits).

## 1. Idempotência (o ponto mais crítico) — provada, com uma ressalva de redação

- `./scripts/rake db:setup` **3×**: `exit 0`, zero erro nas três; `to_regclass('user_state')` = **NULL** em `pokedex` **e** `pokedex_test` nas três leituras.
- As 5 tabelas (`team_pokemons`, `team_pokemon_progress`, `battles`, `wallet`, `inventory`) **existem** nos dois bancos depois das 3 execuções.
- A `0037` é de fato não destrutiva: `db/migrations/0037_drop_user_state.sql:6` = `DROP TABLE IF EXISTS user_state;` (sem `TRUNCATE`) e é a única ação destrutiva do Bloco A.
- **Sonda empírica** (achado M1): `INSERT` de 1 linha em `pokedex.team_pokemons` → `rake db:setup` → **0 linhas**. As migrações pré-existentes `0003_add_user_id.sql:4` e `0007_add_slot.sql:4` (`TRUNCATE team_pokemons CASCADE`) rodam em **toda** execução (setup "sem ledger", D3), apagando `team_pokemons` e `team_pokemon_progress`. Também observei 108 → 0 linhas no `pokedex` no meio desta revisão.

## 2. Órfãos (C2/C3/C6) — zero real

`rg -n '\bUserStateRepository\b|\buser_state\b|\bmark_started\b|\bmark_started_when_full\b|\bclear_user_state!\b|\bstart_journey\b' lib/ server.rb views/ test/ db/ e2e/ Rakefile scripts/` → só:

- `db/migrations/0037_drop_user_state.sql:1,6` e `test/user_state_removal_test.rb` (o guard novo, 7 linhas) — esperados;
- falsos positivos de substring: `restart_journey` (`server.rb:941,1304`; `test/journey_routes_test.rb:16-70`).

Arquivos removidos confirmados ausentes (`lib/user_state_repository.rb`, `test/user_state_repository_test.rb`, `db/migrations/0036_add_user_state.sql`). Menções remanescentes em `GDD.md`/`REQUIREMENTS.md`/`SESSIONS.md`/`docs/`/`sessions/` são históricas ou declaram a remoção — legítimas.

## 3. C4 / C5

- **C4** — os 4 testes do gate verdes individualmente: `test_not_started_with_empty_team_and_no_flag`, `test_team_of_six_derives_started_without_flag`, `test_team_size_liberates_even_without_flag` (1 run/1 assert cada) e `JourneyGameOverTest#test_not_game_over_below_team_of_six` (1/1). `lib/journey_service.rb:10` segue `@team.all(user_id).size >= TeamRepository::MAX_TEAM_SIZE`.
- **C5** — `rg 'flag_alone|below_six|JourneyMarkWhenFull|ServerTeamJourneyMarkTest|UserStateStartedTest' test/` → só nomes legítimos que contêm `below_six` (`test/team_routes_test.rb:755`, `test/battle_routes_test.rb:1044`). Os 4 vacuosos, o `JourneyMarkWhenFullTest`, o `ServerTeamJourneyMarkTest` e o repo de teste não existem mais. O desvio Passo 2↔3 atende o critério: era exigido pelo green-por-commit da G1 (`mark_started*` saíam no 2 e os testes que os chamavam não podiam sobreviver); as 4 remoções + o repo aconteceram, só mudou o SHA — registrado no §8.

## 4. Testes escopados, lint e suíte

- `./scripts/test test/user_state_removal_test.rb` = 4 runs / 10 asserts / 0F; `test/journey_service_test.rb` = 16/17/0F; `test/team_routes_test.rb` = 76/550/0F.
- Rodada **multi-arquivo** abortou com `PG::TRDeadlockDetected` e `TeamRepository::TeamFullError` — causa: **outro agente rodando `test/battle_end_states_test.rb` no mesmo container/`pokedex_test`** (evidência: `pg_stat_activity`/`ps` mostrando o `rake_test_loader` concorrente). Rodados isoladamente, os 3 arquivos ficam verdes. Não é regressão do Bloco A (info I1).
- `./scripts/lint` = **136 arquivos, 0 offenses**; `./scripts/check_docs` = ok. Suíte completa **1181/6309** declarada: **aceita** (não re-executada, por causa do agente concorrente — I1).

## 5. Docs (D7/C8) e escopo (G2)

- `git show d6f1ff3` nos docs: `GDD.md:27` diz que a tabela foi removida na 0089 e aponta novo lar a definir (`docs/draft-backlog.md:344`); `REQUIREMENTS.md:643-645` registra a limpeza e que o gate segue derivado; `docs/draft-backlog.md:109` marca **OBSOLETO — removido na 0089**; `docs/5F-decisoes-pendentes.md:39-43` + "Para fechar" itens 1–2 registram a decisão e a execução. `draft-backlog.md:133` (J4) e `:344` (onboarding) **intactos** (conferidos por leitura).
- `git show --stat` dos 5 commits: só os arquivos declarados (+24; +42/−175; −9; −7; +31 de sessão). Sem gems, sem schema além da 0037, sem tocar motor/economia/rotas/CSS/budget 450.
- Remoção de `test/user_state_repository_test.rb` justificada (o arquivo testado não existe mais); os 2 `ServerTeamJourneyMarkTest` provavam a flag morta e o `refute` de `team_routes_test.rb:1188` (atual `:1151`) era órfão do persistido — sem perda de cobertura de produto.

## Achados (pior primeiro)

| Severidade | Local | Problema | Recomendação |
|---|---|---|---|
| média | `db/migrations/0003_add_user_id.sql:4` e `0007_add_slot.sql:4` | `TRUNCATE team_pokemons CASCADE` roda em **toda** execução de `rake db:setup`/`TestDatabase.setup!` (setup sem ledger): apaga `team_pokemons` + `team_pokemon_progress` do `pokedex` (sonda 1→0; 108→0 observado). Pré-existente, **não** dos 5 commits — mas o §8 do Passo 4 ("idempotente … 5 tabelas intactas") só é verdadeiro para *ausência de erro/existência*, não para os dados. | Reescrever o §8/Passo 4 como "sem erro, `user_state` ausente; `team_pokemons` é truncado pelas migrações 0003/0007 (pré-existente)" e anotar no draft (T2) — o usuário pode perder o time de dev rodando o setup na validação. |
| baixa | `test/journey_service_test.rb` (ex-`:48`) | Com a saída do teste vacuoso, a asserção **unitária** do boundary `team = 5 → started? == false` ficou só no nível de rota (`test/battle_routes_test.rb:1044`, `test/team_routes_test.rb:755`). | Somar 1 teste de 1 linha em `JourneyStartedTest` com 5 membros → `assert_equal false` (protege o `>=` de `lib/journey_service.rb:10`). |
| baixa | `docs/5F-decisoes-pendentes.md:9` | A linha-fonte do digest cita `db/migrations/0036_add_user_state.sql:4-7`, arquivo apagado no Passo 4; o bloco FECHADO (`:39-43`) esclarece, mas a referência aponta para arquivo inexistente. | Sufixar "(arquivo removido na 0089)". |
| info | container/`pokedex_test` compartilhados | Rodadas multi-arquivo do revisor deram deadlock/contaminação porque outro agente rodava a suíte simultaneamente no mesmo banco. | Serializar: 1 agente por container (ou parar `web` antes de rodar contra outro agente). |

## Info (sem ação)

- **I2** — `sessions/0089-limpeza-user-state-e2e.md` §4 mantém `Estado = pendente` em C1–C9: correto, a fase 3 é do usuário; o §8 é a trilha de evidência.
- **I3** — `git status --short`: nenhum arquivo *tracked* modificado pela revisão; só `test/cassettes/**` untracked (de rodadas de teste minhas e do agente concorrente — VCR `:new_episodes`), fora dos 5 commits.
- **I4** — C7/C9 pertencem ao Bloco B; não avaliados aqui.
