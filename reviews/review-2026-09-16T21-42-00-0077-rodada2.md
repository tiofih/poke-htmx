# Revisao escopada — sessao 0077 (`open-design-home-1a1`), rodada 2

**Escopo:** os 2 commits de correcao da rodada 1 (`e3b991a`, `023554f`) + resolucao da
contradicao material sobre **G2** + verificacao por mutacao dos 3 consertos de teste/CSS.
Leitura minima (`git show --stat`, `git log -S`, `git grep`, `rg -n`, `sed -n`) + 3 mutacoes
temporarias (todas revertidas; `git status --short` identico ao baseline de 6 entradas).
Nenhum arquivo de producao editado; nenhum commit feito; `reviews/` nao commitado.

## VEREDITO: **Aprovado**

0 blocker / 0 alta / 0 media / 1 baixa / 2 info

As correcoes da rodada 1 funcionam: a regra orfa sumiu sem quebrar nada, os `refute_includes
"onclick"` agora **executam** (provado por mutacao RED) e o assert de `price` e nao-vacuoso
contra o estado legado (provado por mutacao RED). O unico achado remanescente e factual e
documental (nome de metodo errado em 2 arquivos), sem impacto em criterio.

## Resolucao da contradicao G2 (prioridade 1)

**G2 como esta escrita na sessao e VERDADEIRA; a frase "read-only: nenhum commit da 0077 toca
`server.rb`" e um misquote — essa versao mais forte seria FALSA, mas nao e o que a sessao
garante.**

- §4 (linha 61) define G2 como: *"backend só no listado (§3) — sem migração/schema/gems/services"*,
  com evidencia `git diff --stat -- db/ Gemfile*` vazio. §3 permite explicitamente
  *"backend thin listado ... ajustes so em `render_team_*`/... para expor o dado que o markup novo exige"*.
- `git show --stat` dos 5 commits: `ea7192a` (so `test/`), `a00db99` (`views/` + testes),
  `d0832bf` (`views/` + testes), `d922add` (so `sessions/`), `2671204` (`server.rb` +12,
  `views/`, testes). **Nenhum toca `lib/`, `db/` ou `Gemfile*`** → o literal de G2 fecha.
- Quem introduziu `@member_levels`: `git show 2671204 -- server.rb` (hunk `@@ -52,6 +52,12 @@`,
  `+ @member_levels`/`+ def member_levels_map`/`+ def member_level_for` em `ServerCommon`) —
  confirmado; `git log -S '@member_levels' -- server.rb` lista exatamente `a37fee0` (sessao
  **0080**, 2026-09-10) e `2671204`. Nao houve remocao intermediaria.
- A rodada 1 **nao** confirmou G2 erradamente: o item 5 dela diz textualmente que `2671204`
  toca `server.rb` (+12) e julga que *"cabia no Passo 3 ('thin nos renders')"*. O §9 (linhas 172-176)
  tambem ja registra a origem. Nada falta registrar sobre G2.

Ressalva (vira achado baixa/info abaixo): o toque caiu no helper `expose_manage_data`
(chamado por `render_team_manage`) e em 2 helpers novos, nao literalmente num dos 5 `render_*`
nomeados em §3 — "no listado" por espirito, adjacente pela letra.

## Evidencia por item

| # | Item | Resultado |
| --- | --- | --- |
| 1 | Mutacao `onclick` executou? | **SIM** — injetei `<ul class="heal-list" onclick="x">` em `views/_center.erb:5`; `./scripts/test test/home_residue_test.rb -n /test_center_mart_inner/` → **RED** com `Failure: Expected "<div id=\"center-modal\"...` *to not include "onclick"* (3a assertion, metodo abortado). Revertido |
| 2 | Assert de `price` nao-vacuoso? | **SIM** — reconstrui o estado legado exato (¥ fora dos 2 rows, como em `d0832bf`) → **RED**: `Expected /class="num price">¥\d/ to match ...` (32 assertions, 1 failure). Revertido. Com o ¥ presente: verde |
| 3 | `.mart .muted-row` removida sem consumidor? | **SIM** — `e3b991a` deleta exatamente `.mart .muted-row { color: var(--muted); }`; `rg muted-row public/ views/ test/` = **0** hits; escopado verde |
| 4 | Escopado (HEAD) | `./scripts/test test/home_residue_test.rb test/design_system_test.rb test/home_view_test.rb test/modal_routes_test.rb test/evolution_routes_test.rb` → **55 runs / 871 asserts / 0 failures / 0 errors** (bate com o declarado) |
| 5 | Proveniencia do C4 (§4/§9) | **ok** — `git show <sha>:public/style.css \| grep -c heal-list` → `ea7192a` = **0**, `d922add` = **0**, `4f1538c` = **1**; `4f1538c` = 2026-09-11, `Passo 6 0086: juice sincronizado ao pacing do log...` → sessao 0086 e data corretos; bloco `Home 1:1 (0077)` comeca em `public/style.css:1816` |
| 6 | Docs | `./scripts/check_docs` → `ok> docs consistentes`; `./scripts/checar-sessao 0077` → `ok> sessao 0077 estruturalmente consistente` |
| 7 | Higiene de git | Baseline e pos-mutacoes identicos (`M TODO.md` + review untracked + 4 cassettes untracked); nenhum mutante sobrevivente |

## Achados

| Severidade | file:line | Problema | Recomendacao |
| --- | --- | --- | --- |
| baixa | `sessions/0077-open-design-home-1a1.md:173-174` e `docs/draft-backlog.md:249` | afirmacao de proveniencia **factualmente errada**: dizem que `a37fee0` *"re-adicionou a mesma linha em `expose_team`"*. O hunk real de `a37fee0 -- server.rb` e `@@ -973,6 +973,7 @@ module ServerBattleActions` dentro de **`prepare_team_fragment_data`** (HEAD `server.rb:1034`), nao `expose_team`; e nao houve "re-adicao" — `git log -S '@member_levels' -- server.rb` so tem `2671204` (add) e `a37fee0` (add em outro metodo), sem remocao intermediaria | trocar por: `a37fee0` (sessao 0080, 2026-09-10) **adicionou** a mesma linha em `prepare_team_fragment_data`; corrigir nos 2 arquivos (1 linha cada) |
| info | `sessions/0077-open-design-home-1a1.md:176` | a frase *"`server.rb` é read-only aqui"* (i.e. nesta rodada de correcao) foi lida a montante como *"nenhum commit da 0077 toca `server.rb`"* e virou a contradicao com G2 desta rodada. §3 tambem nomeia so os 5 `render_*`, mas o toque de `2671204` caiu em `expose_manage_data` + 2 helpers de `ServerCommon` | trocar por *"`server.rb` nao foi alterado nesta rodada de correcao (o toque da 0077 esta no `expose_manage_data`, dentro do 'thin listado' de §3)"* — elimina a reincidencia |
| info | `test/home_residue_test.rb:48` | o ancora do assert novo e o `¥`, que **nao** e marcador da 0077: ele veio de `5795866` (`Passo 18: simbolo ¥ nos valores em dinheiro`, 2026-09-11, sessao posterior). No estado do fim da 0077 (`d922add`) o markup era `class="num price"><%= item.price %>` sem `¥`. Nao ha falso-verde (a mutacao prova RED contra o legado), mas ha acoplamento: reverter a copy do `¥` de `5795866` quebra o C1 com o port da 0077 intacto | ancora nativa da 0077 seria `class="num price">` (existe desde `a00db99`), com o valor assertado a parte; opcional |
| info | `docs/draft-backlog.md:249-250` | referencias de linha ja defasadas pelo proprio `e3b991a`: cita `public/style.css:1867` para `.tag-row` (esta em **`:1866`**) e `server.rb:52-64` para o bloco de niveis (vai ate **`:67`**: `member_levels_map` 59-61, `member_level_for` 63-67) | ajustar os 2 numeros, ou citar por simbolo (`expose_manage_data`, `.tag-row`) em vez de linha |
