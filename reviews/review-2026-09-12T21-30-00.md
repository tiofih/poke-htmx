# Review — 0086 round 10 (strike-flow C8, S7)

Scope: bb22c53 (stepper) + 59f9f4e (strike route OOB) + f83d67d (button) + a759ff3 (e2e) + 041d9ed (empty #battle-log) + daf94a2 (lint). C1–C8 + S3, stone order, HP/modal/auto, /battle/play fallback, G1 engine untouched. No edits, no commits.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Medium | views/battle.erb:70-72 | Post-finish Batalhar button stays live: strike uses `hx-swap="none"`, so `#battle-view` controls never re-render; after `finished` the button remains clickable and `strike_battle` returns `""` (nil entry no-op). Flow still completes via modal CTAs, but dead primary button. | Next pass: OOB-disable/hide `#play-btn` (or swap controls fragment) when `result[:finished]`; assert in `battle_strike_routes_test.rb` (button disabled or absent after finish). |
| Low | views/battle.erb:71 | Button listens to stale `next-round from:body` although it now posts `/battle/strike` (C7 legacy emitter gone; auto now chains `next-strike`). Harmless (no emitter) but misleading. | Remove `next-round from:body` from strike button trigger, or comment why kept for compat. |
| Low | views/_strike_result.erb:1-50 vs views/battle.erb (result-box) | Result-modal markup now duplicated in two sources (`battle.erb` full render + `_strike_result.erb` OOB); end-state copy already drifted once (defeat log line `.log__entry--defeat` only renders full, never via strike OOB — e2e asserts it conditionally). | Extract single partial for result-box used by both paths; add strike-OOB defeat-line or record explicit scope decision in 0086 §8. |
| Low | views/_strike_log_entry.erb:1-15 | Fresh strike entries carry no `data-log-index`/`--log-delay`, so C4 tiered pacing + Pular are dead paths for strikes (S3-noted as intentional), yet Pular UI still renders beside them. | Keep (stone: line appears immediately), but note in 0086 C4 row that pacing/Pular apply to initial render only. |
| Low | lib/battle_log_presenter.rb (format_single) | `rindex { equal? } \|\| rindex(entry)` falls back to `==`; duplicate equal hashes (same attacker/damage) resolve to last index, possibly misattributing stock `remaining` copy. | Pass index explicitly from route (`engine.log.size - 1` is the appended entry) instead of searching. |
| Low | server.rb (strike_hp_oob) | Both `#fighters-0/1` re-render `innerHTML` per strike (full 6v6 lists) — resets per-fighter CSS animation state, heavier than targeted HP patch. Acceptable at this size. | Keep; revisit only if juice-per-fighter regresses (targeted HP span OOB). |
| Note | dirty tree | Uncommitted `test/pokemon_routes_test.rb:436,449` breaks lint-0 (3 offenses: RegexpLiteral + LineLength); `server.rb` adds unrelated `Metrics/AbcSize` disable; cassette churn. None from 0086 strike commits (strike files lint-clean). | Clean tree before G1 sign-off; G1 holds at HEAD, not on dirty tree. |

## Checked and clean (no finding)

- Stone order Batalhar>golpe>linha>efeito>HP>modal: `advance_strike` → 1 `entry` → OOB `beforeend:#battle-log` + HP OOB + modal only at finish; `test_strike_appends_exactly_one_log_line` (1 strike = 1 `log__entry`) + e2e `+1 per Batalhar` no-replay guard.
- No replay: `hx-swap="none"`, `refute_includes battle-view` (no full re-render), empty-body no-op without battle.
- HP/modal/auto: HP OOB per side with `HP \d+/\d+`; modal gated `finished`; `HX-Trigger: next-strike` only when auto=1 and not finished, nil at finish and when auto off.
- Fallback intact: `register_play` + `advance_battle` untouched (`server.rb:1065,1311`); comment at `:1080` records coexistence.
- G1 engine formulas untouched: `act`/`damage_for`/`damage_multiplier` unchanged; stepper only queues (`refill_strikes`/`play_next_strike`) with `test_play_next_strike_damage_matches_bulk_formula` proving identical damage; speed order, faint-skip, noop-when-finished covered.
- C1–C8+S3 coherent in `sessions/0086-battle-log-juice.md:122`: C8 S3 entry cites bb22c53/59f9f4e/f83d67d/a759ff3/041d9ed, C1–C7 intact; `#battle-log` always rendered (041d9ed) so OOB lands from strike 1.

## VEREDITO: Aprovado

C8 strike-flow meets stone order append-only with no replay; HP/modal/auto correct; `/battle/play` fallback intact; engine formulas provably untouched. Residuals above are all low except one medium UX dead-button after finish, which does not corrupt state and is fully worded as a next pass. No S7 re-loop required.
