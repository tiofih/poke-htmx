# Review — 0081 tuning final S7 (narrow)

**VEREDITO: Aprovado** — todos os achados do round 3 (`review-2026-09-10T22-15-00.md`: 1 high + 2 medium + 2 low) conferidos como resolvidos nos commits. Sem edit, sem commit.

Scope: `bedd78b..1efcd2d` (passos 14–17) + working tree. Commits verificados: `6136d7a` (String keys), `259daec` (@avg_level), `7dfe969` (EARLY_XP), `1efcd2d` (consolo 10 + copy derrota).

## Achados residuais (pior primeiro)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| info | working tree (`public/style.css`, `test/pokemon_routes_test.rb`, cassettes) | Arquivos dirty fora do tuning — `style.css` (grid/roster/tabs) pertence a outra sessão (0082 etc.), não aos passos 0081. G2 desta revisão é sobre o range commitado, que está limpo. | Implementador: não incluir esses arquivos no commit de validação da 0081; rodar `./scripts/test` + `./scripts/lint` + `check_docs` + `checar-sessao 0081` em tree limpa e anexar evidência na fase 3. |
| info | `sessions/0081-tuning-economia-progressao.md:44-59` | Critérios ainda marcados `pendente` com fase 2 feita — correto pelo protocolo (só a validação do usuário preenche §7), não é gap. | Nada a fazer nesta fase; validar por critério (S2) na fase 3. |

## Conferência dos achados anteriores (todos cleared)

- **High keys** (`team.erb:30`, `team_manage.erb:5`, `_center.erb` vs `server.rb:59`): `member_levels_map` → `[member.id.to_s, ...]` (`6136d7a`) + teste `GET /team` com `Nível` real; `team.erb:30`/`team_manage.erb:5` usam `poke.id.to_s` — consistente. Cleared.
- **Medium avg duplicado** (`_center.erb:2-5`): `server.rb:545` `@avg_level = settings.heal.average_level(...)` via `center_data` (chamado com `@team` já carregado, `server.rb:989-996`); view só interpola (`259daec`). `preview_cost` e `average_level` operam sobre o mesmo conjunto. Cleared.
- **Medium S3 C1b/G2**: C1b (`xp_for(:lose)=10`) registrado em §4 + teste `test_xp_for_lose_is_ten`; G2 alargado p/ copy sem layout (§3/Fora de escopo). Retune 5→10 (`1efcd2d`) com testes atualizados em par (`reward_rule_test`, `battle_service_test` 1005→1010, `battle_routes_test` copy + XP `[1010,1025,1050]`). Cleared.
- **Low EARLY_XP** (`experience_curve.rb`): constante `EARLY_XP = 60` + rationale (`7dfe969`). Cleared.
- **Low fila SESSIONS.md**: "Próxima sessão" agora `0081-validação → 0082 → 0083 → 0086` (uncommitted, correto). Cleared.
- **Copy derrota** (`battle.erb:93-99`): `end_state` definido na linha 56, em escopo no uso da 94; ramo `loss` sem verbo de vitória; guard `.positive?` mantido. Cleared.

## Contenção do diff (G2)

Range commitado: `db/`, `lib/opponent_generator.rb`, `lib/team_budget.rb` intocados; `views/` só `_center.erb` (interpolação), `battle.erb` (copy derrota), `team_manage.erb` (key fix) — dentro do G2 alargado. `lib/` + `test/` em par. Contido.
