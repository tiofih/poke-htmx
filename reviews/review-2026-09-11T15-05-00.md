# Review 0082 round 2 (S7 narrow) — 2026-09-11

Scope: commits 6bf414f (slot+push false), 08f6b24 (heal swap none + gate OOB), e76cd28 (prominent reason).
User nok round 1: modal flash + dead heal. Verified against HEAD e76cd28.

## VEREDITO: Aprovado

Flash gone, heal works via modal on index + battle, gate shows notice, reason prominent.
Suite 1099 runs / 5445 asserts / 0 failures. `checar-sessao 0082` verde. G2 escopo contido.
Residuals below are follow-up / pre-existing, none block 0082.

## Verificação ponto a ponto

- Flash gone: `grep flash` só comenta em `lib/battle_juice_presenter.rb:19`; nenhum `flash` em `server.rb`/`views/`. OK.
- Heal via modal index + battle: form `views/_center.erb:19` `hx-post /team/heal hx-swap="none"`;
  resposta `server.rb:764-767` retorna team fragment + `oob_center_modal` + `oob_team_view` + `oob_nav_badge`
  (OOB processado mesmo com swap none). Slot presente em `views/index.erb:29` e
  `views/battle_page.erb:4`. CTAs miram `#center-modal` com `hx-push-url="false"`
  (`views/index.erb:25`, `views/battle.erb:125,137`). Teste `test_heal_via_modal_closes_and_rerenders` + `test_battle_center_cta_targets_slot_without_hash_push` verdes. OK.
- Gate mostra notice: `server.rb:734` gated retorna `journey_gate_notice + oob_center_modal`;
  `@notice` vaza para `_center_modal.erb` (`if @notice`), logo modal re-renderiza com aviso. OK.
- Reason prominent: `views/_center.erb:20` `<p class="notice notice--error heal-disabled-reason" role="status"><strong>…</strong></p>` antes do button; testes `test_center_insufficient_reason_is_prominent_before_button` (ordem, strong, role, sem `meta`) + `test_center_healed_reason_is_prominent` verdes. OK.
- G1: suite verde; lint 26 offenses idêntico ao baseline 1d176fb (diff vazio) — dívida pré-existente, sem regressão da sessão. `checar-sessao` verde.
- G2: `git diff --stat -- lib/heal_cost_policy.rb lib/heal_service.rb lib/reward_rule.rb` vazio. OK.

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| Low | `server.rb:741,748` | Gate de `buy_from_mart`/`sell_from_mart` retorna só `journey_gate_notice`, sem `oob_mart_modal` — mesmo padrão stale-modal que 08f6b24 corrigiu no heal; mart aberto pré-jornada fica obsoleto | Follow-up fora de 0082: anexar `oob_mart_modal` no early-return do gate do mart (espelhar `heal_team:734`), com teste de gate via modal |
| Low | `views/battle.erb:117,138` | CTA do mart no battle usa `hx-target="body" hx-swap="beforeend"`, enquanto index usa slot `#mart-modal` e o center já foi migrado para slot+outerHTML em 6bf414f — inconsistência; cada abertura no battle empilha overlay no body | Follow-up: migrar CTA do mart no battle para `#mart-modal` + slot (espelhar center), ou registrar fora de escopo em 0083+ |
| Low | repo lint baseline | `lint` = 26 offenses, idêntico ao baseline pré-0082 (sem regressão), mas critério G1 pede "lint 0" — nunca verde enquanto a dívida (`scripts/sweep-balance.rb`, `server.rb:76`, views/tests) existir | Não cobrar de 0082; abater dívida num passe próprio ou relaxar G1 para "sem novas offenses" |

## G1–G3

- G1 sem regressão: suite 1099/0 fail OK; lint sem regressão (26 = baseline), "0" pendente por dívida pré-existente.
- G2 escopo contido: OK (só modal/slot/CTAs + `heal_team`; sem regra de custo).
- G3 docs + revisão: `checar-sessao 0082` OK; este veredito fecha S7 round 2.
