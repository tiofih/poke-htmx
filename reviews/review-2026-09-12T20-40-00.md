# Review 0086 round 8 (S7 narrow: e695cd6 server-stepped + ea9de31 auto toggle + T133 S3 note) — 2026-09-12

Scope: `6b00a48..HEAD` (Passos 12-13) + worktree `sessions/0086` S3/C7 note; C1–C7 + S3 coherence, stepping + auto chain + halt, skip + reduced-motion + modal, G1. No edits, no commits.

## VEREDITO: Requer ajuste (1 high — C7 auto-chain dies in the browser)

- Stepping coherent: `POST /battle/play` → `advance_battle` → `BattleService#advance` → `engine.play_round` (single round, `server.rb:1065-66`, `lib/battle_service.rb:397-405`); `finish_effects` only on the open→finished transition, rewards/XP granted once (`battle_payload` gates on `finished?`). Proven by `test_battle_play_advances_single_round_per_request` + `test_battle_log_grows_round_by_round_until_finish` (`test/battle_routes_test.rb:268,279`) — green this review.
- Halt coherent: `HX-Trigger: next-round` sent only when `auto_play_requested? && !finished?` (`server.rb:1070`); `test_battle_auto_toggle_chains_advance_until_finished` asserts header present mid-battle and absent at `Fim de batalha` (`test/battle_routes_test.rb:183`); `test_battle_auto_off_stops_chain` asserts no header without the toggle (`:203`). Button wiring (`hx-trigger="click, next-round from:body"`, `hx-include="#auto-play"`, `views/battle.erb:69-71`) matches the `HX-Trigger`-on-body pattern. Green this review.
- Skip + reduced-motion + modal intact: skip kills (`style.css:1858,1866-1874,1908,1945-1949`) sit after gated animations, final reduce net (`style.css:2336-2384`) is still the last rule with `!important`; modal still gated on `--log-total` (capped `min(acc,3s)`). C1–C6 untouched by the Passo 12–13 delta (only `server.rb` advance call + header/helper, `battle.erb` button/label, route tests).
- S3 coherent: worktree §7 records C7 (T129 `e695cd6` + T132 `ea9de31`, stagger CSS now dead path for fresh rounds, "C1–C6 intactos") + C7 rows in §4/§7 validation tables; uncommitted single-file state per T133 order — expected.
- G1: `git diff e695cd6~1..HEAD -- lib/battle_engine.rb lib/battle_service.rb db/ lib/battle_log_presenter.rb lib/battle_juice_presenter.rb` EMPTY (`advance` pre-existed, engine primitive reused); targeted `battle_routes + battle_view + style_responsive`: **114 runs, 801 assertions, 0 failures** (this review). Session defines only G1 — no G2/G3 exist (task's "G1-G3" = G1 only).

## Findings (worst-first)

| Severity | file:line | problem | recommendation |
|---|---|---|---|
| High | views/battle.erb:69-71 | C7 auto-chain cannot reach `finished` in a real browser: `#auto-play` lives inside `#battle-view` (swapped via `hx-target="#battle-view" hx-swap="innerHTML"`), re-renders unchecked every round, and `hx-include="#auto-play"` sends `auto=1` only when checked — so the first chained `next-round` request already loses the toggle and the server stops emitting the header after ~2 rounds. The unit test masks it by hand-posting `"auto" => "1"` 300× instead of going through the DOM | Persist the toggle across swaps: re-emit `checked` when `params["auto"] == "1"` (or move the checkbox to `battle_page.erb` outside `#battle-view`, or pin via `hx-vals`); add the missing e2e auto test (check box → click once → banner reaches `Fim de batalha` with no further clicks) — that spec would have caught this |
| Medium | e2e/specs/battle-log.spec.ts:36-93 | S1 C7 promises `e2e stepped rounds + auto toggle`, but no such spec exists — only 3 specs (chronological, reduced-motion, consumption); the browser-only High above escaped exactly through this gap | Add one e2e: stepped clicks advance `Rodada N` one at a time + auto-chain spec above; or drop the e2e half of the C7 S1 row at validation (S2) |
| Low | sessions/0086-battle-log-juice.md:53 | S1 C7 names `test/battle_advance_test.rb` + `test_advance_steps_single_round_until_finished` / `test_auto_toggle_chains_advance_via_hx_trigger` — file doesn't exist; real proofs are `test/battle_routes_test.rb:268,279,183,203,214` (same carried class as round-7 Low for C1/C4–C6 rows) | Align C7 (and carried C1/C4–C6) rows to real test names at validation (S2) — doc-only |
| Low | views/battle.erb:24-41,193 | Each `advance` re-renders the full history with regrowing cumulative `acc` (`--log-delay`, `--step-delay`, `--log-total`), so every "Próxima rodada" replays the whole log animation instead of revealing only the new round; modal cap (`min(acc,3s)`) bounds the result delay but not the replay | Accept for now (consistent with "stagger dead path" note) or render only the fresh round group on `advance`; future pass |
| Info | lib/battle_service.rb:407 | `resolve` (full-run loop) now unwired from any route — only specs call it; dead production path kept alongside `advance` | Keep as spec helper or remove in a cleanup pass; no behavior impact |
| Info | views/battle.erb:69 | `.auto-play` checkbox/label has no dedicated styling or reduced-motion interplay — visual only, out of narrow scope | Future UX pass if needed |

## Residual

One blocker: persist `#auto-play` across `#battle-view` swaps + add e2e chain coverage; everything else (stepping, halt, skip/reduce/modal order, C1–C6, S3, G1) is coherent and green. After the fix, re-review narrowly (auto toggle DOM state + new e2e) and go to user validation (phase 3, S2 table per criterion).
