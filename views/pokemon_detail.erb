<img src="<%= @pokemon.sprite %>" alt="<%= @pokemon.name %>">
<%= @pokemon.name %><br>
<% @pokemon.types.each do |type| %>
  <span class="type"><%= type %></span>
<% end %><br>
<ul class="stats">
  <% @pokemon.stats.each do |stat| %>
    <li><%= stat[:name] %>: <%= stat[:value] %></li>
  <% end %>
</ul>
<% unless @pokemon.evolutions.empty? %>
  <ul class="evolutions">
    <% @pokemon.evolutions.each do |evolution| %>
      <li class="evolution">
        <a href="#" hx-get="/pokemon/<%= evolution.number %>" hx-target="#pokemon-detail" hx-swap="innerHTML">
          <img src="<%= evolution.sprite %>" alt="<%= evolution.name %>" loading="lazy"><br>
          <%= evolution.name %>
        </a>
      </li>
    <% end %>
  </ul>
<% end %>
<form
    hx-post="/team"
    hx-target="#add-status"
    hx-swap="innerHTML">
  <input type="hidden" name="pokeName" value="<%= @pokemon.name %>">
  <button class="btn">Adicionar ao time</button>
</form>
<button
    class="btn"
    hx-get="/pokemon/close"
    hx-target="#pokemon-detail"
    hx-swap="innerHTML">Fechar</button>
