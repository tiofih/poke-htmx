# Sessão 0073 — open-design-home (index + team adaptativos, `.pcard` do catálogo, Gerenciar em `budget-summary`)

## Status

| Fase | Status |
| --- | --- |
| Refinamento | **Concluída** — decisões do usuário em 2026-09-09 (A1, B2, C1, ITEM 3, D 1–10) |
| Implementação | **Pendente** |
| Validação | **Pendente** (executada pelo usuário — fase 3; **não** marcar Done) |

---

## 1. Objetivo

Portar as telas **home** (`index` — 2 colunas time+catálogo) e **`team`** (roster) para o design system do `open-design/` (`migration-guide.md` §5.2/§5.3), **vestindo a view existente** com as classes novas (`grid-2-1`/`catalog-pane`/`app-head`/`budget-summary`/`services`/`roster`/`member`/`bar`/`.pcard`), **preservando** o comportamento e as features já testadas (não é clone literal do guia — A1). Inclui as classes visuais de catálogo **`.pcard`/`.pcard-add`** (re-marcar `pokemon_list_item.erb` — B2) e move o botão **"Gerenciar time"** do `team.erb` para o **`budget-summary`** do `index.erb` (**ITEM 3**). 2ª de 5 sessões da Onda open-design (base → home → battle → history → modals-filters).

## 2. Contexto (estado atual — diagnóstico)

- **0072 entregou** (validada 2026-09-09, suíte **1009/4058**, lint 0): no `public/style.css` o **bloco delimitado** `/* === Open Design System (0072): inicio/fim === */` com `:root` (tokens oklch) + classes-núcleo (`.topnav`, `.btn*`, `.container`/`.section`/`.stack`/`.row`/`.row-between`/`.card`, `.eyebrow`/`.lead`/`.meta`/`.num`/`.muted`); e o **shell `views/layout.erb`** (topnav + footer, `<link rel="stylesheet" href="/style.css">`, `body page-battle/page-history/page-list`, JS de `#global-loading`/scroll-restore preservado, `@team_size`/`/6` no badge). Design system **autônomo** (C4, não depende do sakura); **sakura mantido como fallback até a 0076** (mix visual topnav dark + conteúdo sakura-light é intencional e temporário — 0072 D1/B2).
- **`public/style.css` = 952 linhas.** Sistema de views antigo (`.list-team-grid`/`.list-column`/`.team-column`/`.filter-controls`/`.team-budget`/`.gameloop-cta`/`.notice`/`.slot`/`.evolution-trigger`/`.poke-cost`/`.pokemon-grid`/`.pokemon-list`/`.pagination`) **ainda presente** — remoção é responsabilidade da **0076**, não da 0073. **`style_responsive_test.rb:37`** afirma a **regra CSS** `.list-item img, .starter-item img { max-width: 100% }` — ao anexar as classes de tela do design system, **não remover** essas regras (senão quebra regressão).
- **Views atuais** (nomenclatura antiga): `index.erb` (19 linhas — `.list-team-grid` > `.list-column` [`#filter-controls` + `#pokemon-list` + `#add-status` + `#pokemon-detail`] + `.team-column` [`#team-view`]); `team.erb` (64 linhas — `.notice`/`.journey-progress`/`.team-budget`/`.team-tools`/`.gameloop-cta`/`.slot #N`/`.evolution-trigger`/forms move ▲▼/form remove hardened); `pokemon_list_item.erb` (24 linhas — `<li class="starter-item">`/`<li class="list-item">` + `.poke-cost` + form `POST /team`); `_filter_controls.erb` (52 linhas — `name="q"` + selects `type/generation/tier/cost_max/sort` + "Limpar filtros" **sem `team=`**); `pokemon_list.erb` (47 linhas — `.list-state` + `.pokemon-grid` [`ul`>li] + `.pagination`).
- **Testes que amarram a nomenclatura antiga** (impacto da re-marcação B2 — ver Observações §8): `test/pokemon_routes_test.rb` (`test_index_renders_clickable_list` 27×`starter-item`/9×`list-item`; `test_index_page_uses_full_width_list_body_class` `list-team-grid`/`team-column`; contagens `list-item`/`starter-item` nas linhas ~419/474/481-482/490/502/519-520/554/566/589), `test/pokemon_list_filters_test.rb:185`, `test/pokemon_list_cost_test.rb:218`, `test/team_routes_test.rb:872-924`, `test/team_fainted_routes_test.rb:48/61`. `style_responsive_test.rb:37` é **regra CSS** (mantida → não quebra).
- **Baseline:** suíte **1009/4058**, lint 0 (0072). Fila (REQUIREMENTS.md:698-706 / SESSIONS.md): **0073 open-design-home** → 0074 open-design-battle → 0075 open-design-history → 0076 open-design-modals-filters → 0077 escritas atômicas → 0078 CSRF → 0079 respiro.
- **Preservar:** `server.rb`/`lib/**` intocados (só CSS + views + testes); `style_responsive_test.rb`/`layout_test.rb`/`design_system_test.rb` verdes; delimitadores `/* === Open Design System (0072) === */` estáveis (o C4 de autonomia extrai o bloco por regex).

## 3. Escopo

### Produção

- **`public/style.css`** — anexar, **dentro** do bloco delimitado do design system (sem tocar nos marcadores `inicio`/`fim`), as classes de tela da home + catálogo + roster:
  - Home: `.grid-2-1` (colapsa ≤920px), `.app-head`, `.row-between`, `.budget-summary`/`.budget-meta`, `.meter`/`.meter-fill`, `.services`/`.services-row`, `.catalog-pane`, `.catalog` (grid), `.field`/`.input`/`.filter-grid`.
  - Catálogo visual: `.pcard`, `.pcard-name`, `.pcard-meta`, `.pcard-add` (botão "+"/adicionar), `.pcard .sprite-tile`.
  - Roster (`team.erb`): `.roster`, `.member`, `.member-top`, `.sprite-tile`, `.member-name`, `.member-lvl`, `.hp`/`.hp-label`/`.bar`/`.bar-fill`/`.hp-val`, `.member-actions`.
  - **Não remover**/reordenar as regras atuais; **não** deixar `.list-item img, .starter-item img max-width:100%` fora do arquivo (regra continuando presa — regressão G1).
- **`views/index.erb`** — shell novo (C1): `section.section > container > app-head.row-between [ título (`eyebrow`+`h1`) + **budget-summary** (`.budget-meta` custo `@team_cost/@team_budget` + `.meter` + botão **"Gerenciar time"** `hx-get="/team/manage" hx-target="#team-view" hx-swap="innerHTML"` — ITEM 3) ] + grid-2-1 [ `stack` (team-pane: `#team-view` `<%= erb :team, layout: false %>` + `card` services com `Saldo <%= @balance %>` e `.services-row`) | `aside.catalog-pane` (`.card` com `#filter-controls` (`_: _filter_controls`) + `.catalog` com `#pokemon-list`) ]`.
  - **Obrigatório:** `#pokemon-list` como **`<div class="catalog">`** (NÃO `<ul>` — preserva o `hx-swap="innerHTML"` que injeta o `.pokemon-grid` de `pokemon_list.erb`); preservar `#pokemon-detail`, `#add-status`, `#filter-controls`, `#team-view`; **não duplicar** campo de busca (`_filter_controls` já tem `name="q"` — descartar o `fsearch` do guia); **não** incluir `team=` no "Limpar filtros" (0076); `#pokemon-list` continua a receber `hx-get="/pokemons"`.
- **`views/team.erb`** — vestir `.card`/`.roster`/`.member`/`.member-top`/`.sprite-tile`/`.member-name`/`.member-lvl`/`.hp`/`.bar`/`.bar-fill`/`.hp-val`/`.member-actions` **mantendo**: movimentação ▲▼ (`name="new_slot"`, `aria-label` "Mover para cima/baixo"), badge de slot `#N` (`@team` ordem), **Evoluir por membro** (`hx-get="/team/<%= poke.id %>/evolution"`), `team-tools`, **`gameloop-cta Batalhar`** (`/battle`), restart de game-over (`POST /journey/restart`), notice "jornada", **hardening do form Remover** (`hx-delete="/team"` + `hx-disabled-elt`/`hx-sync`/`hx-indicator`/`hx-include`), `disabled` por fainted. **Remover do `team.erb`** o `<p class="team-tools"><a href="#" hx-get="/team/manage">Gerenciar time</a></p>` (ITEM 3 — sem duplicar; `team-tools` pode permanecer como container, mas sem o link Gerenciar).
- **`views/pokemon_list_item.erb`** — re-marcar (B2): `<li class="starter-item">`/`<li class="list-item">` → **`<li class="pcard">`**; o **botão** do form `POST /team` ("Adicionar ao time"/"No time ✓") ganha **`.pcard-add`** (mantém htmx/`disabled`). O partial **continua `pokemon_list_item`** (não criar partial novo).
- **`views/_filter_controls.erb`** — **sem rota nova**: manter `name="q"` + selects + "Limpar filtros" (sem `team=`). Opcional: envolver em `.filter-grid` para o visual do `catalog-pane` (C1 exige `#filter-controls` preservado).
- **`views/pokemon_list.erb`** — **mantém** o container `.list-state`/`.pokemon-grid` (o `#pokemon-list`/`.catalog` é `innerHTML` daqui; NÃO trocar o shell do index para `<ul>`, pois o `.pokemon-grid` continua `<ul>`>li`.pcard`).

### Testes

- **`test/home_view_test.rb`** — **novo teste estrutural** (prova C2, parte de C1/C9): `get "/"` com stub, assert `.app-head`, `#team-view`, `.services-row` + `Saldo` (`@balance`).
- **`test/design_system_test.rb`** — **estender** (C9): novo método `test_design_system_home_screen_classes` (assert das classes de tela novas presentes em `public/style.css`: `.grid-2-1`, `.catalog-pane`, `.catalog`, `.pcard`, `.pcard-add`, `.roster`, `.member`, `.bar-fill`, `.budget-summary`, `.services`).
- **Editar (marcação antiga substituída — os 3 citados):**
  - `test/pokemon_routes_test.rb` **`test_index_page_uses_full_width_list_body_class`** (C1): `.list-team-grid`/`.team-column` → `.grid-2-1`/`.catalog-pane`; **manter** `class=" page-list"`.
  - `test/pokemon_routes_test.rb` **`test_index_renders_clickable_list`** (B2): 27×`starter-item`/9×`list-item` → **`.pcard`** (36×`<li class="pcard">`); manter `name="q"`, `Página 1`, `name="type"`, `<select`.
  - `test/team_routes_test.rb` **`test_team_fragment_manage_link_is_on_its_own_line`** (ITEM 3, ~L120): relocar o assert do Gerenciar para o novo local — `budget-summary` em `GET /` (assert `budget-summary` + `hx-get="/team/manage"`).
- **Preservados (comportamento/features):** `test_index_is_list_only_screen`, `test_index_uses_single_layout_with_external_css`, `test_index_has_battle_entry_fragment`, `test_index_has_header_navigation_links`, `test_index_renders_team_panel_with_counter_when_not_started`, `test_index_team_panel_shows_members`, `test_team_*` de slot controls/remove form/battle CTA/fainted (C4–C6), `test_pokemon_list_filters_test.rb` (exceto reconciliação da contagem — ver Observações), `team_budget_test.rb`, `style_responsive_test.rb` (CSS), `layout_test.rb`, `design_system_test.rb` (C1–C6 verdes, delimitadores estáveis).

### Fora de escopo (não abrir — RNF-04; anotar para 0076)

- **battle/history** → 0074 / 0075.
- **Modais center/mart/membro + `team=`/filtro disponibilidade + `GET /team/center`/`/team/mart`/`/team/:id/manage`** → 0076.
- **Remoção do sakura** do `layout.erb` → 0076.
- **Remoção do CSS antigo** (`.list-item`/`.starter-item`/`.list-team-grid`/`.team-column`…) → 0076 (responsabilidade de cada sessão de view, mas a **limpeza** final é 0076).
- **Rota nova** — nenhuma.
- **`server.rb`/`lib/**`/`db/*`** — intocados.

## 4. Critérios de aceite

### Resultado (S1 — cada critério aponta o teste que o prova)

- [ ] **C1 `GET /` responde 200, `body` `page-list`, `#team-view` + catálogo com htmx** — rota `get "/"`; `body` mantém `class=" page-list"`; `#team-view` presente; catálogo com `hx-get="/pokemons"`. — prova: `test/pokemon_routes_test.rb` `test_index_page_uses_full_width_list_body_class` (**editado**: `.grid-2-1`/`.catalog-pane`, mantém `page-list`).
- [ ] **C2 `index` renderiza `.app-head` + `#team-view` + `.services-row` com Saldo** — `.app-head.row-between`, `#team-view`, `.services-row` e `Saldo` (`@balance`). — prova: `test/home_view_test.rb` `test_index_app_head_team_view_services_saldo` (novo, estrutural).
- [ ] **C3 `index` preserva alvos de fragmento** — `#pokemon-detail`, `#add-status`, `#filter-controls`, `#team-view`. — prova: `test/pokemon_routes_test.rb` `test_index_is_list_only_screen` + `test_index_uses_single_layout_with_external_css` (mantêm; `#pokemon-detail`/`#add-status`/`#pokemon-list`/`href="/style.css"`).
- [ ] **C4 `team.erb` roster `.member` + badge `#N` + sprite/alt + ordem** — `.roster`/`.member`/`.member-top`/`.sprite-tile`/`.member-name`, badge de slot `#N` (`.slot`→novo badge), `img alt="<nome>"`, ordem do time preservada. — prova: `test/team_routes_test.rb` `test_team_panel_shows_members` / `test_index_team_panel_shows_members` (mantêm).
- [ ] **C5 `team.erb` preserva ▲▼ (`name="new_slot"`, aria-labels)** — forms `hx-post="/team/<id>/move"` com `name="new_slot"` e `aria-label="Mover para cima"/"Mover para baixo"`. — prova: `test/team_routes_test.rb` `test_team_slot_controls_*` (mantêm).
- [ ] **C6 `team.erb` preserva hardening do Remover** — `hx-delete="/team"` + `hx-disabled-elt`/`hx-sync`/`hx-indicator`/`hx-include`; `disabled` por fainted. — prova: `test/team_remove_s3_test.rb` `test_remove_form_*` + `test/team_fainted_routes_test.rb` (mantêm).
- [ ] **C7 Gerenciar migra para `budget-summary`/index + Batalhar CTA mantém em `team.erb`** — "Gerenciar time" no `budget-summary` do `index.erb` (`hx-get="/team/manage"`, `hx-target="#team-view"`), **sem** duplicar em `team.erb`; `gameloop-cta battle` continua no `team.erb`. — prova: `test/team_routes_test.rb` `test_team_fragment_manage_link_is_on_its_own_line` (**editado** para `budget-summary` em index) + `test_team_panel_shows_battle_cta_after_journey` (mantém).
- [ ] **C8 Busca/limpar filtros re-renderizam `#pokemon-list`** — `_filter_controls` (`name="q"`/selects) e "Limpar filtros" (sem `team=`) mantêm `hx-get="/pokemons"`/`hx-target="#pokemon-list"`/`hx-swap="innerHTML"`. — prova: `test/pokemon_list_filters_test.rb` (mantém; C1 exige `#pokemon-list` não-`<ul>`).
- [ ] **C9 `style.css` ganha as classes novas da home + `.pcard` aditivas** (sem remover sakura/antigas; dentro do bloco delimitado) — `.grid-2-1`, `.catalog-pane`, `.catalog`, `.pcard`, `.pcard-add`, `.roster`, `.member`, `.bar-fill`, `.budget-summary`, `.services`. — prova: `test/design_system_test.rb` `test_design_system_home_screen_classes` (estendido) ou `test/home_view_test.rb` (estrutural).
- [ ] **C10 Suíte completa verde + lint 0 + sem regressão** — `./scripts/test` + `./scripts/lint`; `style_responsive_test.rb`/`layout_test.rb`/`design_system_test.rb` verdes. — prova: `./scripts/test` + `./scripts/lint` (nota G1 — não é critério de código unitário, mas verificação do ciclo).

### Garantias (RNF)

- [ ] **G1 sem regressão** — suíte completa (baseline **1009/4058** + novos testes) + lint 0; `style_responsive_test.rb` (regra `.list-item img, .starter-item img max-width:100%` **mantida** — não remover regras) + `layout_test.rb` + `design_system_test.rb` (C1–C6) verdes; delimitadores `/* === Open Design System (0072) === */` estáveis.
- [ ] **G2 sem gems/schema/rede** — `git diff -- Gemfile* db/ server.rb lib/` vazio (sessão toca só `views/*.erb` + `public/style.css` + testes); testes sem rede (rotas com `PokeApiStub`).
- [ ] **G3 S4/S5** — `SESSIONS.md` (tabela linha "0073 open-design-home" — "Refinamento concluída, implementação/validação pendentes, NÃO Done" + "Próxima sessão" → **0074 open-design-battle**) atualizado no commit do refinamento; `./scripts/check_docs` + `./scripts/checar-sessao 0073` verdes; status de validação só após o usuário validar (fase 3 — **parar na fase 2 e aguardar**).

> **S1:** cada critério acima aponta o teste que o prova (arquivo + método). Nenhum critério é puramente manual (C10/G1 são verificações de ciclo, nota G1). **Parar ao fim da fase 2 e aguardar a validação do usuário (fase 3) — não marcar Done, não preencher a seção 7, não commitar conclusão.**

## 5. Decisões de refinamento (fechadas com o usuário em 2026-09-09)

- **A1 — index+team adaptativos (escolhida):** portar `index` (2 colunas time+catálogo) **e** `team` (roster) juntos, vestindo a view existente com as classes novas do design system, **preservando** comportamento/features testadas (**não** é clone literal do §5.2/§5.3). 
- **B2 — portar `.pcard` agora (escolhida):** incluir as classes de catálogo visual `.pcard`/`.pcard-add` (além das de roster/serviços/budget). Exige re-marcar `pokemon_list_item.erb` (`starter-item`/`list-item` → `.pcard`; botão → `.pcard-add`) e atualizar `test_index_renders_clickable_list` (27×`starter-item`/9×`list-item` → nova contagem de `.pcard`). O partial continua `pokemon_list_item` (não criar partial novo).
- **C1 — sem rota nova (escolhida):** **não** abrir `GET /team/center`/`/team/mart`/`/team/:id/manage`/`team=` (isso é 0076). Re-marcação de `index.erb`/`team.erb` conforme o §3 Produção (com os "obrigatórios" da C1: `#pokemon-list` como `<div class="catalog">` — NÃO `<ul>`; preservar `#pokemon-detail`/`#add-status`/`#filter-controls`/`#team-view`; não duplicar campo de busca — descartar `fsearch`; não incluir `team=` no "Limpar filtros").
- **ITEM 3 — Gerenciar → `budget-summary` (escolhida):** mover o botão "Gerenciar time" do `team.erb` para o `budget-summary` do `index.erb` (como no guia), apontando a `GET /team/manage` via htmx no `#team-view`. Atualizar o teste que amarra o Gerenciar em `team.erb` (`test_team_fragment_manage_link_is_on_its_own_line`, ~L120) para o novo local. **Sem duplicar** em `team.erb`.

## 6. Plano TDD (passos)

> Cada passo = `red` → `green` (suíte completa + lint 0) → commit. Parar ao fim da fase 2 e aguardar a validação do usuário (fase 3).

| Passo | Escopo (red → green) | Verificação |
| --- | --- | --- |
| 0 | **Refinamento** — este arquivo + `SESSIONS.md` (tabela linha "0073 open-design-home" + "Próxima sessão" → 0074) | commit `Sessao 0073: refinamento concluido — home (index+team adaptativo, .pcard do catalogo, Gerenciar em budget-summary), criterios C1-C10 amarrados a testes, prox sessao 0074 open-design-battle` |
| 1 | **red→green — C9 (style.css classes de tela)** — estender `test/design_system_test.rb` `test_design_system_home_screen_classes` (assert das classes novas) + anexar as classes de tela (home/catálogo/roster) ao `public/style.css` **dentro** do bloco delimitado (sem tocar nos marcadores; sem remover regras antigas) | `./scripts/test test/design_system_test.rb -n /home_screen_classes/` + suíte + lint 0; commit `Passo 1: classes de tela da home + catalog (.pcard) + roster no style.css (aditivo, dentro do bloco)` |
| 2 | **red→green — C1+C2 (index.erb shell)** — criar `test/home_view_test.rb` (C2: `.app-head`/`#team-view`/`.services-row`+Saldo) + atualizar `test_index_page_uses_full_width_list_body_class` (C1: `.grid-2-1`/`.catalog-pane`, mantém `page-list`) + re-marcar `views/index.erb` (shell §3; `#pokemon-list` como `<div class="catalog">`; preservar fragmentos; sem `fsearch`/`team=`) | `./scripts/test test/home_view_test.rb` + `./scripts/test -n /body_class|home_view/` + suíte + lint 0; commit `Passo 2: index.erb com shell home (app-head + budget-summary + grid-2-1 team/catalog) e #pokemon-list como .catalog` |
| 3 | **red→green — C4+C5+C6 (team.erb roster)** — re-marcar `views/team.erb` (`.card`/`.roster`/`.member`/`.bar`/…) **mantendo** ▲▼ (new_slot, aria), badge `#N`, Evoluir, `gameloop-cta battle`, restart, hardening do Remover, `disabled` fainted; `test_team_*` (slot/remove/fainted) preservados | `./scripts/test test/team_routes_test.rb` + suíte + lint 0; commit `Passo 3: team.erb como roster (member/bar) preservando slots, evolucao, batalhar, hardening do remover` |
| 4 | **red→green — B2 (.pcard no catálogo)** — re-marcar `views/pokemon_list_item.erb` (`<li class="pcard">`; botão `.pcard-add`) + atualizar `test_index_renders_clickable_list` (36×`<li class="pcard">`) **+ reconciliar** as contagens de `starter-item`/`list-item` em `pokemon_list_filters_test.rb`/`pokemon_list_cost_test.rb`/`team_routes_test.rb`/`team_fainted_routes_test.rb` (ver Observações §8) | `./scripts/test test/pokemon_routes_test.rb test/pokemon_list_filters_test.rb test/pokemon_list_cost_test.rb test/team_routes_test.rb test/team_fainted_routes_test.rb` + suíte + lint 0; commit `Passo 4: pokemon_list_item como .pcard/.pcard-add + contagens de .pcard atualizadas nos testes` |
| 5 | **red→green — ITEM 3 (Gerenciar em budget-summary)** — remover o link Gerenciar de `team.erb` + adicionar ".btn Gerenciar time" no `budget-summary` do `index.erb` + atualizar `test_team_fragment_manage_link_is_on_its_own_line` (GET `/`, assert `budget-summary` + `hx-get="/team/manage"`) | `./scripts/test -n /manage_link|budget_summary/` + suíte + lint 0; commit `Passo 5: Gerenciar time movido para budget-summary do index (sem duplicar em team.erb)` |
| 6 | **red→green — C10 (regressão + docs)** — suíte completa + lint 0 (baseline **1009/4058** preservado) + `style_responsive_test.rb`/`layout_test.rb`/`design_system_test.rb` verdes + `./scripts/check_docs` + `./scripts/checar-sessao 0073` | `./scripts/test` + `./scripts/lint` + `./scripts/check_docs` + `./scripts/checar-sessao 0073`; commit `Passo 6: regressao da suite e docs — home adaptativa sem quebrar juice/responsivo/layout/design-system` |
| — | **Fase 2 concluída** → **Revisor (2c)**: loop Implementador↔Revisor até veredito `Aprovado` (teto 3 rodadas, senão S3) → **PARAR** e aguardar a validação do usuário (fase 3). Não marcar Done, não preencher a seção 7, não commitar conclusão. | — |

## 7. Validação (executada pelo usuário — S2)

**Pendente.** A preencher na fase 3 pelo implementador com a tabela `critério | evidência automatizada | evidência manual | resultado (ok/nok)` — um resultado **por critério** (C1–C10 + G1–G3), nunca um bloco único.

## 8. Observações

- **B2 — raio de impacto da re-marcação (importante):** trocar `starter-item`/`list-item` → `.pcard` no `pokemon_list_item.erb` **colapsa** a classe e quebra asserções de contagem/substring em **vários** arquivos, além dos 2 citados. O implementador deve reconciliar (→ `.pcard`) também em: `test/pokemon_routes_test.rb` (linhas ~419/474/481-482/490/502/519-520/554/566/589), `test/pokemon_list_filters_test.rb:185`, `test/pokemon_list_cost_test.rb:218`, `test/team_routes_test.rb:872-924`, `test/team_fainted_routes_test.rb:48/61`. **`style_responsive_test.rb:37` segue verde** (afirma a **regra CSS** `.list-item img, .starter-item img max-width:100%` — manter essas regras no `style.css`; remoção é 0076). Mudar `assert_empty/assert_equal` de `list-item` para `.pcard`, e `assert_includes/refute_includes ... "starter-item"` para `.pcard`/ausência (harmonizar com a semântica de cada teste: paginação filtrar, OOB condicional, etc.).
- **`.pcard-add` é o botão, não o `<li>`:** o protótipo (`open-design/home-team.html` JS) usa `li.className='pcard'` e `<button class="pcard-add">+</button>`. Logo: `<li>` → `.pcard` (27 starters + 9 lista = **36×`<li class="pcard">`** no `test_index_renders_clickable_list`), botão do form `POST /team` → `.pcard-add`.
- **Não usar `<ul>` no shell do `index.erb` para `#pokemon-list`:** deve ser `<div class="catalog">` porque o `pokemon_list.erb` injeta o `.pokemon-grid` (que contém `<ul>`s) via `hx-swap="innerHTML"`; um `<ul>` externo quebraria o swap (C8).
- **`_filter_controls.erb` já tem `name="q"`** (campo de filtro). **Descartar** o `fsearch` do guia §5.2 (duplicaria o campo). O "Limpar filtros" continua **sem** `team=` (0076).
- **`gameloop-cta Batalhar` e restart de game-over ficam no `team.erb`** (o guia §5.3 os omite, mas A1 = preservar features testadas; `test_team_panel_shows_battle_cta_after_journey` exige `gameloop-cta battle` no fragmento `/team`).
- **Gerenciar sai do `team.erb`** (o `<p class="team-tools"><a hx-get="/team/manage">Gerenciar time</a></p>` sai). `team-tools` pode permanecer como container de outras "tools" (C4 mantém `team-tools`), mas **sem** o link Gerenciar; o assert do `test_team_fragment_manage_link_is_on_its_own_line` deve apontar para o `budget-summary` do index (GET `/`).
- **Delimitadores do bloco do design system** (`/* === Open Design System (0072): inicio/fim === */`) devem **permanecer estáveis** — `test_design_system_does_not_depend_on_sakura` extrai o bloco por regex. Anexar as classes de tela da 0073 **dentro** do bloco (não renomear o `(0072)` do marcador — senão o teste de autonomia quebra; não referenciar `sakura`/`@import` nesse bloco). Se precisasse de 2º bloco, manter os marcadores 0072 intactos.
- **Suíte de referência / nomes sem dígitos (gotcha 0070, `Naming/VariableNumber`):** nomear testes sem dígitos (`test_index_app_head_team_view_services_saldo`, `test_design_system_home_screen_classes`); se precisar de dígito no nome, `# rubocop:disable Naming/VariableNumber` inline.
- **`_center`/`_mart` / modais seguem em `team.erb`** (só renderizados quando `@journey_started`), mas não serão re-marcados para `.overlay`/`.modal` nesta sessão (isso é 0076). Manter os `erb :_center`/`erb :_mart` atuais intactos (não quebrar os testes que os cobrem).

## 9. Gotchas / Lições (memória — S6)

- **Re-marcação de classe no partial `pokemon_list_item.erb` tem efeito cascata em quase todos os testes que varrem o catálogo** (`list-item`/`starter-item` aparecem em 6 arquivos de teste, 24 linhas). Antes de commitar o Passo 4, rodar `grep -rn "starter-item\|list-item" test/` e reconciliar **todas** as asserções (não só a do `test_index_renders_clickable_list`).
- **`style_responsive_test.rb:37` é sobre CSS, não HTML:** trocar classes na view NÃO quebra esse teste; quebraria se **removêssemos** a regra `.list-item img, .starter-item img max-width:100%` do `style.css`. Por isso o Passo 1 anexa regras novas **sem** remover as antigas.
- **O `#pokemon-list` não pode virar `<ul>`**: o fragmento `pokemon_list.erb` injeta `<div class="pokemon-grid">` via `hx-swap="innerHTML"`; um `<ul>` wrapper no shell do index quebraria o `innerHTML` (nó aninhado inválido) e o C8.
- **Delimitadores do design system são contrato do C4** (0072): não renomear/mexer nos marcadores `Open Design System (0072): inicio|fim`; adicionar novas classes **dentro** do bloco.
- **`gameloop-cta battle`** é amarrado por `test_team_panel_shows_battle_cta_after_journey`/`test_team_panel_omits_battle_cta_before_journey`/`test_team_panel_hides_battle_cta_when_all_hp_zero` — ao vestir `team.erb`, **manter** a classe `gameloop-cta battle` (no `team.erb`, não movê-la para o index).
- **`name="new_slot"`/aria-labels** (▲▼) são amarrados pelos testes de slot control — preservar os atributos exatos ao re-marcar o `.member-actions`.
- **Hardening do form Remover** (`hx-sync="closest form:replace"`, `hx-disabled-elt`, `hx-indicator`, `hx-include=".list-state"`) é amarrado por `test_team_remove_s3_test.rb`/`team_fainted_routes_test.rb` — preservar todos os atributos (não simplificar para parecer com o guia §5.3, que usa só `hx-delete`/`hx-target`/`hx-include`).

### Confirmações no green (a preencher pelo implementador)

- **Passo 1 (C9):** classes de tela (`.grid-2-1`, `.catalog-pane`, `.catalog`, `.pcard`, `.pcard-add`, `.roster`, `.member`, `.bar-fill`, `.budget-summary`, `.services`) anexadas **dentro** do bloco delimitado do design system; marcadores `inicio`/`fim` intactos; nenhuma regra atual removida/reordenada (`style_responsive_test.rb` verde).
- **Passo 2 (C1+C2):** `index.erb` como shell home (`app-head` + `budget-summary` + `grid-2-1` team/services|catalog-pane); `#pokemon-list` como `<div class="catalog">` (não `<ul>`); `#pokemon-detail`/`#add-status`/`#filter-controls`/`#team-view` preservados; sem `fsearch`/`team=`; `test_index_page_uses_full_width_list_body_class` passa com `.grid-2-1`/`.catalog-pane` + `page-list`.
- **Passo 3 (C4–C6):** `team.erb` como roster (`.card`/`.roster`/`.member`/`.bar`/…) mantendo ▲▼ (`new_slot`/aria), badge `#N`, Evoluir por membro, `gameloop-cta battle`, restart, hardening do Remover, `disabled` fainted; `test_team_*` (slot/remove/fainted/battle) verdes.
- **Passo 4 (B2):** `pokemon_list_item.erb` `<li class="pcard">` + botão `.pcard-add`; `test_index_renders_clickable_list` = 36×`<li class="pcard">` (27+9); **todas** as asserções de `list-item`/`starter-item` em `pokemon_routes_test.rb`/`pokemon_list_filters_test.rb`/`pokemon_list_cost_test.rb`/`team_routes_test.rb`/`team_fainted_routes_test.rb` reconciliadas para `.pcard`; `style_responsive_test.rb` verde (regra CSS mantida).
- **Passo 5 (ITEM 3):** "Gerenciar time" no `budget-summary` do `index.erb` (`hx-get="/team/manage"`, `hx-target="#team-view"`), removido de `team.erb`; `test_team_fragment_manage_link_is_on_its_own_line` atualizado (GET `/`, assert `budget-summary` + `/team/manage`); `test_team_panel_shows_battle_cta_after_journey` segue verde.
- **Passo 6 (C10/G1/G2/G3):** suíte completa (baseline **1009/4058** preservado + novos testes `home_view_test.rb`/`design_system_test.rb` estendido) + `./scripts/lint` 0; `./scripts/check_docs` ok; `./scripts/checar-sessao 0073` ok; `git diff -- Gemfile* db/ server.rb lib/` **vazio** (G2); status de validação **pendente** (fase 3 — parar na fase 2 e aguardar o usuário).
