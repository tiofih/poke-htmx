# Sessão 0074 — open-design-battle (`battle` + `_fighter_panel` — arena/podium/log/result, juice portado pro ODS)

## Status

| Fase | Status |
| --- | --- |
| Refinamento | **Concluída** — decisões do usuário em 2026-09-09 (A1+A1a(i), B1+B1b, C1, `.ftags`, C1–C7+G1+1 manual) |
| Implementação | **Pendente** |
| Validação | **Pendente** (executada pelo usuário) |

---

## 1. Objetivo

Portar a tela de **batalha** (`battle.erb` + `_fighter_panel.erb`; wrapper `battle_page.erb` **intacto**) para o design system do `open-design/` (`migration-guide.md` §1.2/§2/§3 — protótipo `battle.html`), vestindo a view existente com a estrutura **`.arena`** (2× `ul.fighters` + `.podium` central com round-banner/controles/`.log`/`#result-box`) e as classes de batalha do §1.2, **preservando** os contratos de fragmento `#battle-view` (swap de `POST /battle/play`/`/battle/new`), o contexto injetado (`@engine`/presenters via `FighterPresenter`) e o juice 0063/resolver 0069 — portando o **juice para os seletores novos dentro do bloco ODS** (B1b). 3ª de 5 sessões da Onda open-design (base → home → battle → history → modals-filters).

## 2. Contexto (estado atual — diagnóstico)

- **0072/0073 entregues** (ambas validadas 2026-09-09, baseline **1011/4124**, lint 0): no `public/style.css` o **bloco delimitado** `/* === Open Design System (0072): inicio (linha 6) / fim (linha 479) === */` com `:root` (tokens oklch) + classes-núcleo + classes de tela da home/catálogo/roster (`.grid-2-1`/`.catalog-pane`/`.pcard`/`.roster`/`.member`/`.bar`/`.bar-fill`/`.hp-val`/…). `layout.erb` já dá `body page-battle` em `/battle`. **Sakura mantido como fallback até a 0076**.
- **`views/battle.erb` (107 linhas)** — marcação antiga: gates `@message` no topo (linhas 1-13: `.notice notice--info` + `gameloop-cta` de `@gate_cta`/`@gate_action`/`@gate_disabled_cta` — jornada não iniciada/derrota total/game-over/time vazio/erro); senão `@engine`: `<h1>Batalha</h1>` + `.round` + `BattleJuicePresenter` (HP inicial por replay) + `.battle-layout` (3 `.battle-column` com `data-side="0"/"1"`; esq: `_fighter_panel` + `.stock-items`; centro: `.battle-controls` com botão `Batalhar` `hx-post="/battle/play" hx-target="#battle-view" hx-swap="innerHTML" hx-indicator="#battle-loading"` + fim: `.winner.winner--pop`/`.xp-gained.xp-gained--pop`/`.evolution-news`/`.learned-news`/`.notice--error` game-over + `.gameloop-ctas` (Poke Center/Poke Mart `gameloop-cta`, "Novo confronto" `hx-post="/battle/new"` `disabled` quando derrota) + `ul.battle-log` com `li.battle-log__entry` `data-round`/`data-from-side`/`data-to-side`/`--log-delay`; dir: `_fighter_panel` oponente).
- **`views/battle_page.erb` (2 linhas)** — `<div id="battle-view"><%= @battle_content %></div>`. `server.rb:831-837` (`render_battle`) só envolve quando **NÃO** `htmx_request?`; `POST /battle/play`/`/battle/new` devolvem `erb :battle, layout: false` (fragmentos p/ swap no `#battle-view`). **Nenhuma rota muda nesta sessão (C1).**
- **`views/_fighter_panel.erb` (37 linhas)** — `.battle-pane` > `h2` + por fighter: `p.fighter` (+`fighter--flash`/`fighter--ko`/`fighter--shooting`, `data-hp-initial`, `data-damage`), `.projectile`, `img` (alt), `.fighter-name`, `.bar.hp-bar.hp-bar--<tier>` (role progressbar/aria/`--hp-initial-percent`), `.bar-fill`, `.hp-text`, `.item-used-badge`, "carrega: `<assigned_item_label>`", "segura: `<held_item_label>`", `ul.moves` > `li` (`span.pp`, `.bar.pp-bar.pp-bar--<tier>`).
- **CSS de batalha atual (fora do bloco, linhas ~651-912)**: `.battle-layout` (grid 3 col + colapso `@media max-width:900px`), `.battle-pane`, `.fighter` (2 blocos: layout ~693 e position ~790), `.hp-bar`/`.pp-bar`/`.bar-fill`/tiers, `.battle-log`/`.battle-log__entry` + `@keyframes battle-log-in`, **juice 0063** (`@keyframes juice-hp` + `.hp-bar .bar-fill`, `.fighter--flash`/`juice-flash`, `.fighter--flash::after` número de dano, `.fighter--ko`/`juice-ko`, `.battle-layout`/`juice-shake`, `.projectile` + `@media min-width:900px` com direção por `.battle-column[data-side="0"/"1"]`, toast/buttons/banner), bloco `prefers-reduced-motion`.
- **Testes que amarram a marcação antiga** (impacto da re-marcação): `test/battle_routes_test.rb` (912 linhas) — `:15` `test_battle_page_renders_full_page_with_battle_view` (C1), `:21` (`id="battle-view"`), `:122` (`hx-target="#battle-view"`), `:125-163` (HP/PP bars + `style="width: 25%"`/`HP 50/202`/`PP 30`), `:171-172` (`hx-post="/battle/play"`/`hx-indicator="#battle-loading"`), `:178` (`battle-log__entry`), `:256-270` (`data-round`/`data-from-side`/`data-to-side`/`fighter--flash`/`fighter--ko`/`fighter--shooting`/`class="projectile"`/`winner--pop`/`xp-gained--pop`), `:337` (`hx-post="/battle/new"`), `:346-347` (`gameloop-cta` Poke Center/Mart), `:848/:861` (gates: `gameloop-cta disabled` + `title="Recupere...`). `test/style_responsive_test.rb` (207 linhas) — `test_battle_layout_stacks_at_900`/`test_bars_are_fluid`/`test_juice_keyframes_present`/`test_juice_reduced_motion_disables`/`test_projectile_only_above_900px` (regras CSS antigas; **sem edição**). `test/design_system_test.rb` (111 linhas). `test/home_view_test.rb` (novo, estrutural — padrão para o `battle_view_test.rb`).
- **Baseline:** suíte **1011/4124**, lint 0 (0073). Fila (SESSIONS.md): **0074 open-design-battle** → 0075 open-design-history → 0076 open-design-modals-filters → 0077 escritas atômicas → 0078 CSRF → 0079 respiro.
- **Preservar:** `server.rb`/`lib/**`/`db/*` intocados (só CSS + views + testes); `style_responsive_test.rb`/`design_system_test.rb` verdes; delimitadores ODS estáveis (C4 de autonomia 0072 extrai o bloco por regex).

## 3. Escopo

### Produção

- **`public/style.css`** — anexar, **dentro** do bloco delimitado do design system (marcadores `inicio`/`fim` intactos), as classes de batalha do §1.2 traduzidas do `<style>` do protótipo `open-design/battle.html` (linhas ~105-156, com os breakpoints ~`@980`/`@700`): `.arena` (grid `1fr 1.06fr 1fr`; colapsa 1fr ≤980px), `.fighters` (ul, grid), `.fighter`/`.fainted`/`.engaged`/`.fighter-head`/`.fighter .sprite-tile`/`.fighter-id`/`.fname`/`.fmeta`/`.lvl`/`.ftags`/`.ftag`, `.hp`/`.hp-label`/`.bar`/`.bar-fill` (`.ok`/`.mid`/`.low`)/`.hp-val`, `.moves`/`.move`/`.ppnum`/`.dot`, `.podium` (sticky top:84px; static ≤700px), `.round-banner`/`.turn-status`/`.controls`, `.result`/`.winner-badge`/`.rewards`/`.ctas`, `.battle-log` (max-height + overflow-y), `.log-title`, `.log`/`.lside` (`.team`/`.opp`)/`.txt`.
  - **Reuso:** `.bar`/`.bar-fill`/`.hp-val`/`.hp`/`.sprite-tile` já existem no bloco (roster 0073) — harmonizar os estilos de batalha com eles em vez de duplicar (mesma nomenclatura do protótipo).
  - **Juice (B1b):** portar as regras de animação/reduced-motion **para os seletores novos, DENTRO do bloco ODS** (ex.: `fighter--ko`/`fighter--flash`/`fighter--shooting`, `hp-bar`/`pp-bar`, `winner--pop`/`xp-gained--pop` → seletores equivalentes no novo markup; `prefers-reduced-motion` **novo** no ODS — não regredir acessibilidade). O CSS antigo (fora do bloco) **permanece íntegro** → `style_responsive_test.rb` **sem edição** e verde (keyframes `juice-*`, `.projectile`, bloco reduce antigo continuam no arquivo).
  - **Colisão `.fighter` (B1):** o seletor `.fighter` antigo (fora do bloco, linhas ~693 e ~790) conflita com o `.fighter` novo do ODS → neutralizar por **escopo-legado morto**: `.fighter{...}` → `.battle-pane .fighter{...}` (o wrapper `.battle-pane` antigo some na re-marcação e a regra vira **órfã** — SEM deletar nada). **Única edição fora do bloco**, mínima. Verificar outros seletores antigos que colidam com os novos (`.hp-bar`/`.pp-bar`/`.moves`/`.log`/`.result`…) — preferir deixar o antigo íntegro e escopar só o que colidir de fato.
- **`views/battle.erb`** — re-marcar **in-place** (C1 — sem rota nova): estrutura do protótipo `battle.html` — `.arena` com **2× `ul.fighters`** (player `data-od-id="side-team"` / oponente `data-od-id="side-opponent"`, **mantendo `data-side="0"/"1"` nas colunas** — a direção do projectile juice ≥900 depende dele) + **`.podium`** central (round-banner com round/turn-status + `.controls` com o botão Batalhar + `.result`/`#result-box` no fim + `.log-title` "Registro da batalha" + `.log`), cobrindo **preparado/andamento/fim/game-over**. **Preservar:** `hx-post="/battle/play"` + `hx-target="#battle-view"` + `hx-swap="innerHTML"` + `hx-indicator="#battle-loading"` (C6); fim: `.result`/`.winner-badge`/`.rewards` (XP/dinheiro `@xp_gained`/`@money_gained` + `.evolution-news`/`.learned-news`)/`.ctas` (Poke Center/Poke Mart + "Novo confronto" `hx-post="/battle/new"` **disabled** quando derrota — C5); game-over (`.notice--error` + Vender itens/Recomeçar jornada); log com `data-round`/`data-from-side`/`data-to-side`/`--log-delay` (C4); juice attrs (`data-hp-initial`/`data-damage`/projectile). **Gates `@message`** (jornada não iniciada, time vazio/derrota total, erro) **ficam com a marcação atual** (`.notice`/`.gameloop-cta`) **até 0076** — NÃO ganham classes novas nesta sessão (A1).
- **`views/_fighter_panel.erb`** — re-marcar **in-place**: cada fighter vira `li.fighter` (dentro do `ul.fighters`) com `.fighter-head` (`.sprite-tile` + `.fighter-id` > `.fname` + `.fmeta` com `.lvl`) + `.ftags` + `.hp` (`.hp-label` + `.bar` + `.hp-val`) + `.moves` > `.move` (nome + `.ppnum` + `.dot`); manter juice (classes novas + `data-hp-initial`/`data-damage`/projectile) e badges (`item_used_badge`/`assigned_item_label` "carrega"/`held_item_label` "segura"). **`.ftags`:** preencher com os **badges reais** disponíveis no `FighterPresenter` (item usado/carrega/segura) **ou omitir** — **SEM tocar no presenter** (expor types seria API nova, fora de escopo).
- **`views/battle_page.erb`** — **intacto** (wrapper `#battle-view`).

### Testes

- **`test/battle_view_test.rb`** — **novo teste estrutural** (padrão `test/home_view_test.rb`): prova **C2** (`.arena` + 2× `ul.fighters` + `.podium`) e **C3** (contagens do card fighter: `.fighter`/`.fighter-id`/`.fmeta`/`.hp`/`.bar`/`.hp-val`/`.moves`/`.move`) e **parte do C4** (`.log`/`.lside`/`.log-title`).
- **`test/design_system_test.rb`** — **estender** (C7): novo método `test_design_system_battle_screen_classes` (assert das classes de batalha presentes no bloco: `.arena`, `.podium`, `.fighter`, `.fainted`, `.fighter-id`, `.fmeta`, `.ftags`, `.moves`, `.move`, `.log`, `.lside`, `.result`, `.winner-badge`, `.rewards`, `.ctas`).
- **Editar `test/battle_routes_test.rb`** (marcação antiga substituída — nomes sem dígitos, gotcha 0070):
  - **`test_battle_page_renders_full_page_with_battle_view`** (`:15`, `:21` — C1): **mantém verde** — `body.page-battle` (via layout) + `id="battle-view"` (wrapper intacto).
  - **`test_battle_renders_panels_with_team_and_opponent`** (`:122` — C6): **mantém** — `hx-target="#battle-view"`.
  - **`:125-163`** (C3 + B1b): `test_battle_panels_render_hp_bars_for_player_and_opponent`/`test_battle_hp_bar_width_reflects_hp_share`/`test_battle_panels_render_pp_bars_per_move` — `class="bar hp-bar hp-bar--high"`/`class="bar pp-bar pp-bar--high"` → **seletores novos**; `HP 50/202`/`style="width: 25%"`/`PP 30` mantidos (parte dos ~8 asserts de classe do B1b).
  - **`:171-172`** (C6): `hx-post="/battle/play"` + `hx-indicator="#battle-loading"` **mantidos** (+ `>Batalhar</button>`).
  - **`:178`** (C4): `battle-log__entry` → **seletor novo** do log (ex.: `log__entry` no `.log`); `data-round` mantido.
  - **`:256-270`** (C4 + B1b): `data-round`/`data-from-side`/`data-to-side` mantidos; `fighter--flash`/`fighter--ko`/`fighter--shooting`/`class="projectile"` → seletores novos; `:269-270` `class="winner winner--pop"`/`class="xp-gained xp-gained--pop"` → `.winner-badge`/`.rewards` (conforme B1b).
  - **`:337`** (C5): `hx-post="/battle/new"` **mantido** (estado ativo).
  - **`:346-347`** (C5): `<a class="gameloop-cta" href="/">Poke Center/Poke Mart</a>` → `.ctas`/`.btn`.
  - **`:848/:861`** (C5/gates — verificar): asserts do botão `gameloop-cta disabled` do gate derrota — como **A1 mantém `.gameloop-cta` nos gates** até 0076, devem **seguir verdes sem edição**; só ajustar se a re-marcação do fim tocar em label/título compartilhado.
- **`test/style_responsive_test.rb`** — **sem edição**, verde (G1): `test_battle_layout_stacks_at_900` (`.battle-layout` — CSS antigo íntegro), `test_bars_are_fluid` (`.hp-bar`/`.pp-bar`), `test_juice_keyframes_present` (`@keyframes juice-*`), `test_juice_reduced_motion_disables` (bloco reduce com `.battle-log__entry`/`.fighter--flash`/`.fighter--ko`/`.projectile`/`.battle-layout`/`.hp-bar`/`.winner--pop`/`.xp-gained--pop`…), `test_projectile_only_above_900px` (`.projectile` display:none + keyframes/animação só ≥900px).
- **Preservados (comportamento/features):** `test_battle_play_resolves_entire_battle_in_one_request` (`:223` — resolver 0069), `test_battle_log_shows_all_rounds_after_resolve` (`:234`), `test_battle_end_shows_winner_and_reset_button`/`test_finished_battle_shows_game_over_and_restart_when_broke`/`test_finish_screen_*` (fim/game-over), `test_battle_with_empty_team_shows_journey_gate`/`test_battle_gate_shows_game_over_fragment_when_stuck`/`ServerBattleHpGateTest`/`ServerJourneyGateBattleTest` (gates), `test_battle_start_loads_into_panel_without_clearing_nav`/`test_request_releases_thread_connections`, `test_battle_renders_remaining_stock_in_player_panel` (`stock-items`), `test_battle_play_debits_used_item_and_shows_heal_log`/`test_battle_panel_shows_item_used_badge_after_member_uses_item`/`test_battle_panel_has_no_item_used_badge_before_any_use` (badges), `style_responsive_test.rb`, `design_system_test.rb` (C1–C6 + home classes), `home_view_test.rb`.

### Fora de escopo (não abrir — RNF-04; anotar para 0075/0076)

- **history** → 0075.
- **Modais center/mart/membro + filtros `team=in|out` + remoção do CSS antigo (`.battle-layout`/`.battle-pane`/`gameloop-cta`/sakura) + re-marcação dos gates `@message`** → 0076.
- **`server.rb`/`lib/**`/`db/*`/presenters** — intocados (nenhuma API nova; `.ftags` usa só o que o `FighterPresenter` já expõe ou omite).
- **Rota nova** — nenhuma (C1: re-marcação in-place; `battle_page.erb` wrapper intacto; `POST /battle/play`/`/battle/new` continuam devolvendo `erb :battle` inteiro p/ swap em `#battle-view`).

## 4. Critérios de aceite

### Resultado (S1 — cada critério aponta o teste que o prova)

- [ ] **C1 `GET /battle` (preparado) renderiza página com `body.page-battle` + `#battle-view`** — wrapper `battle_page.erb` intacto + layout com `page-battle`. — prova: `test/battle_routes_test.rb` `test_battle_page_renders_full_page_with_battle_view` (**mantém verde**; `:15`/`:21`).
- [ ] **C2 fragmento novo tem `.arena` + 2× `ul.fighters` (player/oponente) + `.podium`** — estrutura do protótipo `battle.html` vestindo o `battle.erb` re-marcado (preparado/andamento). — prova: `test/battle_view_test.rb` `test_battle_arena_two_fighter_lists_and_podium` (**novo**, estrutural, padrão `home_view_test.rb`).
- [ ] **C3 card fighter novo traz identidade + HP/PP reais** — `.fighter`/`.fighter-id`/`.fmeta`/`.hp`/`.bar`/`.hp-val`/`.moves`/`.move` com os valores reais do presenter (`HP 50/202`, `width: 25%`, `PP 30`). — prova: `test/battle_view_test.rb` `test_battle_fighter_card_shows_identity_hp_pp_moves` (**novo**, contagens) + editar `test/battle_routes_test.rb` `:125-163` (`hp-bar`/`pp-bar` → seletores novos).
- [ ] **C4 log `.log`/`.lside`/`.log-title` preservando `data-round`/`--log-delay`** — entradas com `data-round`/`data-from-side`/`data-to-side` + `--log-delay` (stagger 0069) na marcação nova. — prova: `test/battle_view_test.rb` (`.log`/`.lside`/`.log-title`) + editar `test/battle_routes_test.rb` `:178` (`battle-log__entry` → seletor novo) e `:256` (`data-round` mantido).
- [ ] **C5 fim de batalha `.result`/`.winner-badge`/`.rewards`/`.ctas` + `hx-post="/battle/new"` (disabled quando derrota)** — banner vencedor/XP + CTAs (Poke Center/Poke Mart + Novo confronto). — prova: editar `test/battle_routes_test.rb` `:337,346-347` (`gameloop-cta`→`.btn`/`.ctas`) + asserts `.winner-badge`/`.rewards`; `:269-270` (`winner--pop`/`xp-gained--pop`) conforme B1b; `:848,861` verificar (gates mantêm `.gameloop-cta` — A1).
- [ ] **C6 contrato htmx: `hx-post="/battle/play"` + `hx-target="#battle-view"` + `hx-indicator`** — fragmento re-renderizado inteiro no `#battle-view`; resolver 0069 intacto. — prova: `test/battle_routes_test.rb` `:21,122,171-172` (**mantidos/ajustados**) + `test_battle_play_resolves_entire_battle_in_one_request` (**verde**, comportamento).
- [ ] **C7 CSS: classes de batalha presentes no bloco ODS + CSS antigo intocado** — `.arena`, `.podium`, `.fighter`, `.fainted`, `.fighter-id`, `.fmeta`, `.ftags`, `.moves`, `.move`, `.log`, `.lside`, `.result`, `.winner-badge`, `.rewards`, `.ctas` aditivas no bloco; `.fighter` antigo neutralizado por escopo-legado morto (`.battle-pane .fighter`). — prova: `test/design_system_test.rb` `test_design_system_battle_screen_classes` (**estendido**).

### Garantias (RNF)

- [ ] **G1 sem regressão** — suíte completa (baseline **1011/4124** + novos testes) + lint 0; `test/style_responsive_test.rb` **sem edição** e verde (`test_battle_layout_stacks_at_900`, `test_bars_are_fluid`, `test_juice_keyframes_present`, `test_juice_reduced_motion_disables`, `test_projectile_only_above_900px` — regras/`@keyframes`/bloco reduce antigos íntegros); delimitadores ODS estáveis.
- [ ] **G2 sem gems/schema/rede/presenters** — `git diff -- Gemfile* db/ server.rb lib/` vazio (sessão toca só `views/*.erb` + `public/style.css` + testes); testes sem rede (rotas com stub).
- [ ] **G3 S4/S5** — `SESSIONS.md` (tabela linha "0074 open-design-battle" + "Próxima sessão" → **0075 open-design-history**) atualizado no commit do refinamento; `./scripts/check_docs` + `./scripts/checar-sessao 0074` verdes; status de validação só após o usuário validar (fase 3 — **parar na fase 2 e aguardar**).

### Manual

- [ ] **M1 visual do arena/podium/juice no navegador** — stagger do log, KO fade/grayscale, projectile ≥900px, `prefers-reduced-motion` desliga tudo, colapso ≤~900px (`.arena` 1 coluna, `.podium` static ≤700px). — prova: `manual` (subir o app com `./scripts/run`, navegar `/battle`, jogar até o fim; conferir estados preparado/andamento/fim/game-over e os gates).

> **S1:** cada critério acima aponta o teste que o prova (arquivo + método); o único critério **puramente manual é o M1** (visual — evidência manual explícita). **Parar ao fim da fase 2 e aguardar a validação do usuário (fase 3) — não marcar Done, não preencher a seção 7, não commitar conclusão.**

## 5. Decisões de refinamento (fechadas com o usuário em 2026-09-09)

- **A1 + A1a(i) — portar battle inteira (escolhida):** `battle.erb` + `battle_page.erb` (wrapper intacto) + `_fighter_panel.erb` juntos, com a estrutura do protótipo (`.arena` com 2× `ul.fighters` + `.podium` central com round-banner/controles/`.log`/`#result-box`), cobrindo preparado/andamento/fim/game-over. **Gates `@message`** (jornada não iniciada, time vazio, erro) **ficam com a marcação atual** (`.notice`/`.gameloop-cta`) **até 0076** — NÃO ganham classes novas nesta sessão. Preservar: contrato de fragmento `#battle-view` (alvo de `POST /battle/play` e `/battle/new`), contexto injetado (`@engine`/`@fighters` via presenters), juice 0063, resolver 0069.
- **B1 + B1b — CSS (escolhida):** classes de batalha do §1.2 **aditivas no bloco ODS**, traduzidas do `<style>` do protótipo (`battle.html`) com seus breakpoints (~@980/@700). **Colisão `.fighter`:** neutralizar o `.fighter` antigo por **escopo-legado morto** (`.fighter{...}` → `.battle-pane .fighter{...}` — o wrapper antigo some na re-marcação e a regra vira órfã SEM deletar nada; única edição fora do bloco, mínima). **Juice (B1b):** renomear para os seletores novos portando as regras de animação/reduced-motion **DENTRO do bloco ODS** (ex.: `fighter--ko`/`fighter--flash`/`fighter--shooting`, `hp-bar`/`pp-bar` → seletores novos equivalentes), editando **~8 asserts de classe** em `battle_routes_test.rb`. `prefers-reduced-motion` novo entra no ODS (não regredir acessibilidade). CSS antigo permanece íntegro → `style_responsive_test.rb` **sem edição** e verde.
- **C1 — sem rota nova (escolhida):** re-marcar `battle.erb` + `_fighter_panel.erb` **in-place**; `battle_page.erb` (wrapper) fica intacto; `POST /battle/play`/`/battle/new` continuam devolvendo `erb :battle` inteiro p/ swap em `#battle-view`.
- **`.ftags` (escolhida):** preencher com os **badges reais disponíveis** no `FighterPresenter` (item usado/carrega/segura) **ou omitir** — **SEM tocar no presenter** (expor types é API nova, fora de escopo).
- **Manter `data-side` (escolhida):** nas colunas novas do `.arena` (direção do projectile juice ≥900 depende dele).

## 6. Plano TDD (passos)

> Cada passo = `red` → `green` (suíte completa + lint 0) → commit. Parar ao fim da fase 2 e aguardar a validação do usuário (fase 3).

| Passo | Escopo (red → green) | Verificação |
| --- | --- | --- |
| 0 | **Refinamento** — este arquivo + `SESSIONS.md` (tabela linha "0074 open-design-battle" + "Próxima sessão" → 0075) | commit `Sessao 0074: refinamento concluido — battle (arena+podium+_fighter_panel, juice portado pro ODS, .fighter antigo por escopo-legado), criterios C1-C7+G1+manual amarrados, prox sessao 0075 open-design-history` |
| 1 | **red→green — C7+B1b (style.css classes de batalha + juice no ODS)** — estender `test/design_system_test.rb` `test_design_system_battle_screen_classes` (assert das classes novas) + anexar as classes de batalha do §1.2 ao `public/style.css` **dentro** do bloco delimitado (sem tocar nos marcadores; sem remover regras antigas) + portar o juice para os seletores novos (B1b) com `prefers-reduced-motion` novo + neutralizar `.fighter` antigo por escopo-legado morto (`.battle-pane .fighter`) | `./scripts/test test/design_system_test.rb -n /battle_screen_classes/` + suíte + lint 0; commit `Passo 1: classes de batalha do §1.2 + juice portado para os seletores novos no bloco ODS (aditivo; .fighter antigo por escopo-legado)` |
| 2 | **red→green — C1+C2+C6 (battle.erb arena+podium)** — criar `test/battle_view_test.rb` (C2: `.arena`/2× `ul.fighters`/`.podium`; C3 contagens; C4 `.log`/`.lside`/`.log-title`) + re-marcar `views/battle.erb` (estrutura do protótipo; `.podium` com controles/log/result; manter gates `.notice`/`.gameloop-cta`, `hx-post="/battle/play"`/`hx-target="#battle-view"`/`hx-indicator`, juice attrs, `data-side`; `test_battle_page_renders_full_page_with_battle_view` e `:122`/`:171-172` mantidos) | `./scripts/test test/battle_view_test.rb` + `./scripts/test test/battle_routes_test.rb -n /battle_page|panels_with|battle_button/` + suíte + lint 0; commit `Passo 2: battle.erb como arena (2x ul.fighters + podium com controles/log/result), gates e contrato htmx preservados` |
| 3 | **red→green — C3+B1b (_fighter_panel card)** — re-marcar `views/_fighter_panel.erb` (`li.fighter` com `.fighter-head`/`.fighter-id`/`.fmeta`/`.ftags`/`.hp`/`.bar`/`.hp-val`/`.moves`/`.move`; juice classes novas + data attrs + badges; `.ftags` badges reais ou omitir) + editar `test/battle_routes_test.rb` `:125-163` (`hp-bar`/`pp-bar` → seletores novos; `HP 50/202`/`width: 25%`/`PP 30` mantidos) | `./scripts/test test/battle_view_test.rb` + `./scripts/test test/battle_routes_test.rb -n /hp_bar|hp_bar_width|pp_bars/` + suíte + lint 0; commit `Passo 3: _fighter_panel como card do arena (fighter-id/fmeta/ftags/hp/moves) com juice renomeado no ODS` |
| 4 | **red→green — C4+C5 (log + fim/ctas)** — log novo (`.log`/`.lside`/`.log-title` mantendo `data-round`/`--log-delay`; `battle-log__entry` → seletor novo em `:178`) + fim de batalha (`.result`/`.winner-badge`/`.rewards`/`.ctas`; `hx-post="/battle/new"` disabled derrota; game-over) + editar `:256` (`data-round`), `:269-270` (winner/xp → B1b), `:337,346-347` (`gameloop-cta`→`.btn`/`.ctas`); verificar `:848,861` (gates) | `./scripts/test test/battle_routes_test.rb` + suíte + lint 0; commit `Passo 4: log e resultado do fim de batalha na marcacao nova (result/winner-badge/rewards/ctas) preservando data-round e stagger` |
| 5 | **red→green — G1 (regressão + docs)** — suíte completa + lint 0 (baseline **1011/4124** preservado + `battle_view_test.rb`/`design_system_test.rb` estendido) + `style_responsive_test.rb` **sem edição** verde + `./scripts/check_docs` + `./scripts/checar-sessao 0074` | `./scripts/test` + `./scripts/lint` + `./scripts/check_docs` + `./scripts/checar-sessao 0074`; commit `Passo 5: regressao da suite e docs — arena/podium/result sem quebrar juice/responsivo/design-system` |
| — | **Fase 2 concluída** → **Revisor (2c)**: loop Implementador↔Revisor até veredito `Aprovado` (teto 3 rodadas, senão S3) → **PARAR** e aguardar a validação do usuário (fase 3). Não marcar Done, não preencher a seção 7, não commitar conclusão. | — |

## 7. Validação (executada pelo usuário — S2)

**Pendente.** *(Ao validar — S2: uma linha por critério, nunca bloco único; o critério **M1 é `manual`** — evidência via navegação.)*

| Critério | Evidência automatizada | Evidência manual | Resultado (ok/nok) |
| --- | --- | --- | --- |
| C1 (`GET /battle` → `page-battle` + `#battle-view`) | `test/battle_routes_test.rb` `test_battle_page_renders_full_page_with_battle_view` | navegação `/battle` — página completa com arena | |
| C2 (`.arena` + 2× `ul.fighters` + `.podium`) | `test/battle_view_test.rb` (novo, estrutural) | navegação `/battle` — layout 3 colunas | |
| C3 (card fighter identidade + HP/PP reais) | `test/battle_view_test.rb` + `test/battle_routes_test.rb` `:125-163` (editado) | navegação `/battle` — cards com HP/PP reais | |
| C4 (log `.log`/`.lside`/`data-round`/`--log-delay`) | `test/battle_view_test.rb` + `test/battle_routes_test.rb` `:178,256` (editado) | jogar até o fim — log escalonado | |
| C5 (fim `.result`/`.winner-badge`/`.rewards`/`.ctas` + `hx-post="/battle/new"` disabled) | `test/battle_routes_test.rb` `:337,346-347,269-270` (editado); `:848,861` verificado | jogar até o fim — resultado + CTAs; derrota → Novo confronto disabled | |
| C6 (contrato htmx `hx-post="/battle/play"`/`hx-target="#battle-view"`/`hx-indicator`) | `test/battle_routes_test.rb` `:21,122,171-172` (mantidos) + `test_battle_play_resolves_entire_battle_in_one_request` | clique em Batalhar — swap no `#battle-view` | |
| C7 (CSS classes de batalha no bloco ODS + antigo intocado) | `test/design_system_test.rb` `test_design_system_battle_screen_classes` (estendido) | inspeção visual do arena | |
| G1 (sem regressão) | `./scripts/test` + `./scripts/lint`; `style_responsive_test.rb` sem edição verde | navegação `/`, `/battle`, `/history` | |
| G2 (sem gems/schema/rede/presenters) | `git diff -- Gemfile* db/ server.rb lib/` vazio; testes com stub | — | |
| G3 (S4/S5 docs consistentes) | `./scripts/check_docs` + `./scripts/checar-sessao 0074` verdes | — | |
| M1 (visual arena/podium/juice) | — | `manual`: stagger do log, KO fade, projectile ≥900px, reduced-motion, colapso ≤~900px, estados preparado/andamento/fim/game-over + gates | |

> **S3:** ajuste identificado aqui = reabrir o critério, registrar a alteração com data e obter nova aprovação do usuário.

## 8. Observações

- **B1b — raio de impacto dos asserts de classe (importante):** renomear os seletores de juice no novo markup colapsa ~8 asserts de classe em `test/battle_routes_test.rb` (`:131-134` hp-bar ×2, `:158-159` pp-bar, `:178` battle-log__entry, `:263-266` fighter--flash/ko/shooting + projectile, `:269-270` winner--pop/xp-gained--pop). **`test/style_responsive_test.rb` segue verde** (afirma as **regras CSS antigas** — keyframes `juice-*`, `.projectile`, bloco `prefers-reduced-motion` com `.battle-log__entry`/`.fighter--flash`/`.fighter--ko`/`.hp-bar`/`.winner--pop`…); por isso o CSS antigo **permanece íntegro** e o reduced-motion **novo** do ODS cobre os seletores novos (acessibilidade não regride). Não remover o bloco reduce antigo nem os keyframes.
- **Colisão `.fighter` e o escopo-legado morto:** o `.fighter` antigo (fora do bloco, 2 blocos de regra) colide com o `.fighter` novo do ODS (o novo vem **depois** na cascata e é mais específico em alguns pontos, mas a regra antiga de `display:flex`/`position:relative` vazaria para os cards novos). Escopar o antigo para `.battle-pane .fighter{...}` — como o wrapper `.battle-pane` **some** da marcação nova, a regra vira órfã e o CSS antigo permanece no arquivo (nada deletado; `style_responsive_test.rb` não depende de `.fighter`). Verificar também `.hp-bar`/`.pp-bar`/`.moves`/`.battle-log`/`.log` — só escopar o que colidir de fato; o resto fica íntegro (remoção é 0076).
- **Gates `@message` ficam como estão (A1):** `battle.erb` linhas 1-13 (`.notice`/`.gameloop-cta`/`@gate_cta`/`@gate_action`/`@gate_disabled_cta`) **não** ganham classes novas nesta sessão — os testes de gate (`ServerBattleHpGateTest` `:838-862`, `ServerJourneyGateBattleTest`, `test_battle_with_empty_team_shows_journey_gate`, `test_battle_gate_shows_game_over_fragment_when_stuck`) devem **seguir verdes sem edição**; re-marcação é 0076. Cuidado: os asserts `:848/:861` casam o **título** "Recupere seus pokémons…" que também existe no botão disabled do **fim** — ao re-marcar o fim (`.ctas`), manter o título/label para não quebrar o assert do gate (ou ajustar cirurgicamente se o regex cruzar).
- **`data-side` é contrato do juice ≥900:** manter `data-side="0"/"1"` nas colunas novas do `.arena` (o projectile fly direction `--fly: 1/-1` depende de `.battle-column[data-side=…]` — se a coluna nova trocar de classe, portar o seletor do juice para a classe nova **dentro** do ODS; o `data-side` em si é preservado).
- **`#battle-view` e `battle_page.erb` não mudam:** o wrapper continua `<div id="battle-view">` e `render_battle` (server.rb:831-837) continua envolvendo só quando não-htmx; os fragmentos `POST /battle/play`/`/battle/new` continuam `erb :battle, layout: false` inteiros (swap `innerHTML` no `#battle-view`). C1/C6 são **mantidos**, não re-criados.
- **`home_view_test.rb` como padrão do teste estrutural:** `battle_view_test.rb` usa o mesmo padrão (stub da batalha + `get "/battle"` com `user_session("user-a")`, asserts no body); nomes de teste **sem dígitos** (gotcha 0070 — `Naming/VariableNumber`).
- **`.ftags` sem API nova:** `FighterPresenter` já expõe `item_used_badge`, `assigned_item_label` ("carrega:"), `held_item_label` ("segura:") — preencher os badges do `.ftags` com eles ou omitir; **não** adicionar métodos/types ao presenter (fora de escopo — G2).
- **Delimitadores do bloco ODS são contrato do C4 (0072):** não renomear/mexer nos marcadores `Open Design System (0072): inicio|fim`; anexar as classes de batalha **dentro** do bloco (linhas 6-479). O teste de autonomia extrai o bloco por regex.
- **Suíte de referência:** baseline **1011/4124**, lint 0 (0073). Fila: 0074 battle → **0075 open-design-history** → 0076 open-design-modals-filters → 0077 escritas atômicas → 0078 CSRF → 0079 respiro.

## 9. Gotchas / Lições (memória — S6)

- *(A preencher pelo implementador ao longo da fase 2 — expectativas registradas no refinamento:)*
- **Re-marcação do `battle.erb`/`_fighter_panel.erb` colapsa os seletores de juice no `battle_routes_test.rb`** (~8 asserts de classe: hp-bar/pp-bar, battle-log__entry, fighter--flash/ko/shooting, projectile, winner--pop/xp-gained--pop). Reconciliar **todos** os asserts de classe antes do green do Passo 3/4 — `style_responsive_test.rb` continua verde porque afirma as **regras CSS antigas** (mantê-las íntegras).
- **`style_responsive_test.rb` amarra o CSS antigo, não o HTML:** `test_juice_reduced_motion_disables` exige que o bloco `prefers-reduced-motion` contenha `.battle-log__entry`/`.fighter--flash`/`.fighter--ko`/`.projectile`/`.battle-layout`/`.hp-bar`/`.winner--pop`/`.xp-gained--pop` — **não** editar nem remover o bloco reduce antigo; o reduced-motion **novo** do ODS é aditivo (cobre os seletores novos).
- **Escopo-legado morto para o `.fighter` antigo:** `.fighter{...}` → `.battle-pane .fighter{...}` neutraliza a colisão sem deletar regra (o `.battle-pane` some na re-marcação). É a única edição fora do bloco ODS; o resto do CSS de batalha antigo permanece íntegro até a 0076.
- **Gates `@message` são contrato de teste:** os asserts `gameloop-cta disabled`/`title="Recupere seus pokémons…"` (defeated gate e fim) compartilham o mesmo título — ao re-marcar o fim, manter o título/label ou o assert do gate cruza/quebra.
- **`data-side`/`data-round`/`--log-delay`/`data-hp-initial`/`data-damage` são contratos do juice e do teste `test_battle_fragment_marks_juice_targets`** — preservar os atributos exatos no novo markup (só as **classes** mudam).

### Confirmações no green (a preencher pelo implementador)

- [ ] Passo 1 (C7+B1b) — commit `Passo 1: ...`
- [ ] Passo 2 (C1+C2+C6) — commit `Passo 2: ...`
- [ ] Passo 3 (C3+B1b) — commit `Passo 3: ...`
- [ ] Passo 4 (C4+C5) — commit `Passo 4: ...`
- [ ] Passo 5 (G1) — suíte **1011/4124+**, lint 0, docs verdes