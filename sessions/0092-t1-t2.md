# Sessão 0092 — T1+T2: flake do SeedScriptsTest e db:setup destrutivo

## Status

| Fase | Status |
| --- | --- |
| Refinamento | **Concluída** — decisões fechadas em 2026-09-21 |
| Implementação | **Pendente** |
| Validação | **Pendente** (revisão do PR) |

> Reprodução: script
> E2E: nao
>
> Detalhe: `./scripts/test` (sobe o `db` e roda a suíte) é o caminho até o estado do teste do T1; T2 não tem estado a montar (leitura de doc). E2E `nao`: T1 é ordem de execução de teste Minitest e T2 é documentação — sem comportamento de produto observável; o harness Playwright existe, mas nenhum spec aplica.

---

## 1. Objetivo

Fechar T1 e T2 do `TODO.md`: eliminar o flake order-dependente do `SeedScriptsTest` (`TestDatabase.team_row` sem filtro de `user_id`) e registrar a destrutividade do `rake db:setup`/`TestDatabase.setup!` com a proposta de remediação.

## 2. Contexto (estado atual — diagnóstico)

- `test/test_helper.rb:124-128`: `TestDatabase.team_row(name)` faz `SELECT * FROM team_pokemons WHERE name = $1` — sem `user_id` e sem `ORDER BY`. São 9 chamadores em 3 arquivos: `test/seed_scripts_test.rb:35,40`, `test/team_repository_test.rb:64,70`, `test/seed_team_test.rb:25,45,58,102,103`.
- Flake registrado na 0089 (`sessions/0089-limpeza-user-state-e2e.md:189`): `test_team_evolucao_seeds_near_evolution_thresholds` falhou num run completo (seed 29866: `Expected: 15, Actual: 5`) porque `team_row` devolveu o `charmander` de outro usuário; passou isolado e no run final. Ordem-dependente, pré-existente.
- `rake db:setup` (`db/schema.sql` + `db/migrations/*.sql`, via `TestDatabase.setup!`) aplica as migrações `0003`/`0007`, que rodam `TRUNCATE team_pokemons CASCADE` → truncam `team_pokemons`/`team_pokemon_progress` em **toda** execução (time do pokedex apagado). Hoje não está documentado em lugar nenhum (T2).
- O commit `dfbe217` ("test: cassettes VCR gravados pela suite local") existe **só** na branch `0091-t3-t4` (já mergeada) e **não** está em `main` — precisa entrar nesta sessão por cherry-pick.
- Baseline 2026-09-21 (pós-0091): suíte 1192/6389, lint 0.

## 3. Escopo

### Produção

- Nenhum arquivo de produção (`server.rb`, `lib/`, `views/`, `db/` intocados) — T1 é infra de teste, T2 é documentação.
- `docs/draft-backlog.md`: novo item em §2 "Arquitetura / Infra / Performance" registrando a destrutividade + proposta de remediação.

### Testes

- `test/test_helper.rb`: `TestDatabase.team_row` passa a filtrar por `user_id`.
- Atualizar os 9 chamadores nos 3 arquivos de teste para a nova assinatura.
- Teste novo (red) que prova o filtro por `user_id`.

### Fora de escopo (não abrir)

- T3/T4/T5 do `TODO.md` (fechados nas 0090/0091); qualquer mudança de comportamento além de T1+T2.
- Implementar o guard/once de migrações destrutivas — T2 é **documentar + propor**, a implementação é escopo futuro (D1).
- e2e novo, `REQUIREMENTS.md`, schema/migração, gems novas.

## 4. Critérios de aceite

### Resultado

- [ ] **C1** — `TestDatabase.team_row(name, user_id)` filtra por `user_id` e os 9 chamadores usam a nova assinatura — prova: `test/seed_scripts_test.rb` (novo `test_team_row_filtra_por_user_id` + `test_team_evolucao_seeds_near_evolution_thresholds`).
- [ ] **C2** — `test/seed_scripts_test.rb` passa sob qualquer seed da suíte completa (3 execuções com seeds distintos, sem falha do arquivo) — prova: `manual` (3× `./scripts/test`, conferindo que os prints `Randomized with seed` dos 3 runs são distintos).
- [ ] **C3** — `docs/draft-backlog.md` documenta que `rake db:setup` e `TestDatabase.setup!` truncam `team_pokemons`/`team_pokemon_progress` em toda execução — prova: `manual` (leitura do item novo em §2 "Arquitetura / Infra / Performance").
- [ ] **C4** — proposta de remediação registrada (guard/once para migrações destrutivas) — prova: `manual` (mesmo item do `docs/draft-backlog.md`).

### Garantias (RNF)

- [ ] **G1** — Suíte completa verde com **baseline preservado (1192/6389) + o teste novo de C1** + lint 0 em **todo** green; commit obrigatório por passo; 0 regressão.
- [ ] **G2** — Sem dependência nova / sem mudança de schema / testes sem rede / sem supressão de lint injustificada.
- [ ] **G3** — `SESSIONS.md` (tabela + "Próxima sessão") atualizado no mesmo commit do refinamento (S4); `REQUIREMENTS.md` intocado; status de validação só após o usuário revisar o PR.

> **S1:** cada critério acima aponta o teste que o prova; `manual` explícito em C2–C4 com a evidência esperada.

## 5. Decisões de refinamento (fechadas em 2026-09-21)

- **D1 (T2):** documentar + registrar proposta — **não** implementar o guard/once agora (aceite literal do `TODO.md`; a implementação vira sessão futura).
- **D2:** local da doc do T2 = `docs/draft-backlog.md` §2 "Arquitetura / Infra / Performance" (alternativa preterida: limitações do `REQUIREMENTS.md` — mais pesado e sem ganho).
- **D3 (T1):** assinatura `team_row(name, user_id)` com filtro **obrigatório** (alternativas preteridas: usar `team_id` — devolve só o id e forçaria 2 queries; filtro opcional com default — continua order-dependente quando esquecido).
- **D4:** cherry-pick de `dfbe217` (cassetes VCR, só na branch `0091-t3-t4`) como primeiro passo desta branch, rewordado para o formato `Passo 1:` do projeto (a mensagem original `test: ...` usa prefixo genérico, proibido pela regra de commit).
- **D5:** uma sessão só para T1+T2 — mesma área (infra/dívida) e porte pequeno.
- **D6:** declarações do modo PR: `> Reprodução: script` (`./scripts/test`) e `> E2E: nao` (T1/T2 sem comportamento de produto observável; harness existe mas nenhum spec aplica).

## 6. Plano TDD (passos)

> Cada passo = `red` → `green` (suíte completa + lint 0) → commit.

| Passo | Escopo (red → green) | Verificação |
| --- | --- | --- |
| 0 | **Refinamento** — este arquivo com critérios e plano fechados + `SESSIONS.md` | commit `docs(sessao 0092): refinamento concluido — criterios e plano TDD fechados` |
| 1 | **Pré-requisito: cherry-pick `dfbe217`** (cassetes VCR da branch `0091-t3-t4`, fora de `main`) + reword `Passo 1: cassettes VCR gravados pela suite local` | suíte verde + lint 0, commit `Passo 1:` |
| 2 | **T1 red:** novo `test_team_row_filtra_por_user_id` em `test/seed_scripts_test.rb` falha (linha de outro `user_id`) → **green:** `team_row(name, user_id)` em `test/test_helper.rb` + os 9 chamadores | suíte verde + lint 0 + 3× `./scripts/test` com seeds distintos sem falha do arquivo (C2), commit `Passo 2:` |
| 3 | **T2 (docs, sem red):** item novo em `docs/draft-backlog.md` §2 com a destrutividade (C3) + proposta guard/once (C4) | leitura C3/C4 + suíte verde + lint 0, commit `Passo 3:` |
| 4 | **Corpo do PR + revisor + abrir-pr** | `./scripts/checar-pr 0092` verde, commit `docs(pr 0092):` → Revisor `Aprovado` + `CORPO DO PR: publicável` → `./scripts/abrir-pr 0092 --open` |
| — | **Fase 2 concluída** → **Revisor (2c)**: loop Implementador↔Revisor até veredito `Aprovado` (teto 3 rodadas, senão S3) → corpo do PR + `abrir-pr` → handoff (S6) → **PARADA** — a fase 3 é a revisão do PR pelo usuário. | `> PR: <url>` na §7 |

## 7. Validação (revisão do PR pelo usuário)

> PR: https://github.com/tiofih/poke-htmx/pull/4

**Pendente.** *(Ao validar — S2: uma linha por critério, nunca bloco único.)*

| Critério | Evidência automatizada | Evidência manual | Resultado (ok/nok) |
| --- | --- | --- | --- |
| C1 | `./scripts/test test/seed_scripts_test.rb` (novo `test_team_row_filtra_por_user_id`) | leitura da assinatura e dos 9 chamadores | |
| C2 | `manual` — 3× `./scripts/test` com seeds distintos | prints `Randomized with seed` dos 3 runs | |
| C3 | `manual` — leitura de `docs/draft-backlog.md` | item novo em §2 presente | |
| C4 | `manual` — leitura de `docs/draft-backlog.md` | proposta guard/once registrada | |

> **S3:** ajuste identificado aqui = reabrir o critério, registrar a alteração com data
> e obter nova aprovação do usuário.

## 8. Observações

- Nenhuma suíte/lint/e2e rodada na fase 1 — baseline conhecida da 0091 (1192/6389, lint 0, 2026-09-21).
- Após o merge desta sessão o `TODO.md` fica sem itens (T1–T5 fechados: T5 na 0090, T3/T4 na 0091, T1/T2 aqui).
- Próximo passo sugerido: fase 2 começando pelo cherry-pick `dfbe217` (passo 1), antes do red do T1.

## 9. Gotchas / Lições (memória — S6)

- `dfbe217` **não** está em `main` (só na branch mergeada `0091-t3-t4`) — pular o cherry-pick derruba os cassetes VCR e a suíte pode tentar rede/new episodes.
- `team_row` também não tem `ORDER BY` — o aceite cobre o filtro por `user_id`; se aparecer nome duplicado dentro do mesmo user, upgrade é `ORDER BY slot LIMIT 1`.
- *(a preencher na fase 2.)*

<!-- sdd-pr:bloco -->
> **Modo PR ativo (`--with-pr`).** Bloco anexado pelo instalador. Para desligar o modo, remova
> este bloco junto com o bloco `SDD/PR` do `AGENTS.md` e os scripts `checar-pr`/`abrir-pr`.

## Declarações do modo PR (fechadas no refinamento)

Logo **abaixo** da tabela de `## Status`, escreva as duas linhas — e feche-as na fase 1, não
depois:

> Reprodução: seed|script|manual|nao-aplicavel
> E2E: sim|nao

- `Reprodução` responde: *como um terceiro chega ao estado inicial do teste?* Havendo script de
  seed/fixture no projeto, use `seed` ou `script` e cite o comando. Sem caminho automatizado,
  `manual`; sem cenário de estado a montar, `nao-aplicavel` **com a justificativa na mesma
  linha**.
- `E2E` responde: *o comportamento observável desta sessão tem teste de ponta a ponta?* Havendo
  harness, `sim`. Não havendo, `nao` — e então cada critério observável registra `manual` (S1) e
  o roteiro manual vai para o corpo do PR.

O `./scripts/checar-pr` confere que o `**Estado inicial:**` do corpo do PR bate com o valor
declarado aqui.

## A linha `Validação` do Status

No modo PR ela se refere à **revisão do PR**, não a uma execução local do usuário. Continua
`Pendente` até o usuário validar revisando o PR.

## Onde entra o link do PR (seção 7)

Ao abrir o PR (fim da fase 2, após o veredito `Aprovado`), registre na seção `## 7. Validação`:

> PR: <url>

A tabela por critério (S2) só é preenchida **depois do merge**. Um PR por sessão; o merge é do
usuário. Ajuste pedido na revisão reabre o critério (S3) e **atualiza o PR** — não abre um
segundo.
<!-- /sdd-pr:bloco -->
