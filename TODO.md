- T3: 0086: alinhar §6 (lista só Passos 0-4 e 23-32) com o Status (1-32); o Passo 28 não tem commit nenhum no histórico — a linha do §6 fica sem hash
  accept: A tabela do §6 da sessão 0086 cobre os Passos 1–32 (ou a lacuna fica registrada explicitamente no arquivo) e o Passo 28 — sem commit em `git log --all` — tem a ausência de hash registrada
- T4: 0086: triar working tree sujo (cassettes VCR + config e2e + docs untracked) sem commitar lixo
  accept: Cada item pendente (cassettes, e2e/playwright.config.ts, test/pokemon_routes_test.rb, ARCHITECTURE.md/GDD.md/PROJECT.md, reviews/, gotchas/, docs/type-effects-lab.html) está commitado ou explicitamente descartado/ignorado
- T6: 0086: dívidas do review retroativo — hooks `data-strategy` (`public/style.css:1957-1960`) moram no `.log__entry`, que não é ancestral de `.shot`/`.fighter.is-hit` (inertes ali; valores efetivos vêm do `.arena`, `:932-933`); regra legada `.arena[data-jx-shake="on"]` (`:996`) nunca casa (atributo só existe em `#jx-gates`); teste de C15 não prova a contenção dentro do `@media 900px`; `--log-delay` duplicado (`views/battle.erb:201-202`, `views/_strike_log_entry.erb:4,10`); `strike_side_delay` inócuo (`server.rb:1138-1142`) — ver `reviews/review-2026-09-14T11-56-30-retroativo-passos21-28.md`
  accept: cada item resolvido (hooks `data-strategy` movidos para ancestral comum ou removidos como redundantes; regra legada removida; C15 delimitado ao bloco `@media`; `--log-delay`/`strike_side_delay` deduplicados) e `./scripts/test` + `./scripts/lint` verdes
